"""Declarative plugin support for HPC Client GUI (Plugin API v1).

This package intentionally contains no Qt imports, no network transport, and
no executable-plugin mechanism. Plugins are declarative data (JSON/Markdown)
that is validated and loaded into typed objects.
"""
