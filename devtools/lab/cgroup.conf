# No systemd or dbus inside this container, so slurmd cannot create cgroup
# scopes. Disable the plugin outright and track steps by process tree
# instead (see ProctrackType/TaskPlugin in slurm.conf). Consequence: the
# lab does NOT enforce memory or CPU limits the way a real site does.
CgroupPlugin=disabled
