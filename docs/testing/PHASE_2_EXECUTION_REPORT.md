# Test Governance Phase 2 — Execution Report

Latest status: Packets A–M DONE; N NOT APPLICABLE; O DONE; Wave COMPLETE. The 2026-09-13 integration closeout remains `DEFECT_FOUND` and NOT READY TO MERGE: the authoritative runner reached the end of its broad child but found two failures, and therefore did not run later isolated groups or coverage.

The closeout worktree is `D:\Projeler\hpc-client-gui-integration-closeout` on `test-suite-governance-integration-closeout-20260913`, based on governance SHA `18e59fff3d8ebfa47666347a0d9cfc4d137b43c1`. Its current collection is 2,657, with zero-primary 0 and multi-primary 0. The exact closeout evidence, failure classification, and merge decision are in [TEST_SUITE_INTEGRATION_CLOSEOUT_2026-09-13.md](TEST_SUITE_INTEGRATION_CLOSEOUT_2026-09-13.md).

- Frozen baseline: `12ce79935bf076e1062c57dc7dbd148bad2bfae1`
- Governance branch: `test-suite-governance-20260912`
- Phase B commit: `058c83bb3318ddaee85bf98ba430079a4acd4a02`
- Phase C commit: `1b792ffc`

The original checkout at `D:\Projeler\hpc-client-gui` was read-only for this work. It was on `develop` at `12ce79935bf076e1062c57dc7dbd148bad2bfae1` at task start; successive read-only safety checks during finalization observed it advance to `ccfeb1b81c304b79b426b599d46ebf2b831b15fe`, then `1ff36589da5cb437cb831e12ba9c54304eca6137`, still dirty. No Phase 2 paths appeared in its status, and this work made no writes there. Work was done in the clean governance worktree at `D:\Projeler\hpc-client-gui-test-governance`. No push was performed.

## Packet A — frozen baseline and inventory

Frozen-baseline collection completed with exit code 0: **2,673 tests, 0 collection errors**. The full suite did not complete, so full-suite passed/failed/skipped/xfail/xpass totals and duration remain unknown.

The full-suite limitation is specific: `scripts/ci.py full` could not enter its normal pytest coverage run because `pytest-cov` was missing in the audit environment; the release runner reproducibly hung during setup at `tests/test_connection_advanced_settings.py::ParallelismSourceOfTruthTests::test_settings_dialog_has_no_global_parallelism_editor`; and seven isolated baseline failures were reproduced. These are evidence about the frozen baseline, not a complete full-suite result.

The seven known isolated baseline failures are:

- `tests/test_macos_ci.py::test_macos_ci_matrix_covers_both_native_architectures`
- `tests/test_macos_ci.py::test_macos_ci_has_no_release_upload_or_signing_step`
- `tests/test_macos_release_workflow.py::test_release_preflight_shares_the_ci_test_suite`
- `tests/test_wave9_ci_unicode_matrix.py::TestResultsVerification::test_wx_unicode_smoke_cannot_fail_open`
- `tests/test_workflow_action_pins.py::test_every_workflow_exists`
- `tests/test_wx_packaged_smoke.py::test_packaged_wx_smoke_gate_reports_critical_stages`
- `tests/test_gui_audit_screenshots.py::test_manifest_exists_and_commit_current`

The archive is [audit/archive/12ce7993/test-suite-baseline](../../audit/archive/12ce7993/test-suite-baseline/). It contains `README.md`, `baseline.json`, `nodeids.txt`, `inventory-enriched.json`, `proposed-taxonomy-heuristic.json`, `lane-inventory.json`, `duration-partial.json`, and `static-findings.json`. The baseline JSON is schema version 1 and leaves unavailable full-suite totals null.

## Packet B — taxonomy and REPORT checker

`pyproject.toml` registers these primary markers: `unit`, `integration`, `gui`, `e2e`, `runtime_smoke`, `contract`, `audit`, `reporting`, and `release`. It registers the qualifiers `semantic`, `regression`, `performance`, `resource`, `concurrency`, `slow`, `subprocess`, `windows`, `linux`, `macos`, `hardware`, `synthetic_hardware`, `license`, `acceptance`, `artifact_dependent`, `wx`, `qt`, and the existing `packaging` marker. Strict marker enforcement remains off; no existing test was mass-marked.

The tracked specifications are [TEST_ARCHITECTURE.md](TEST_ARCHITECTURE.md) and [TEST_SUITE_CLEANUP_WAVE_2026-09-12.md](TEST_SUITE_CLEANUP_WAVE_2026-09-12.md). The checker is [scripts/check_test_taxonomy.py](../../scripts/check_test_taxonomy.py), with focused tests in [tests/test_test_taxonomy_checker.py](../../tests/test_test_taxonomy_checker.py).

REPORT counts actual pytest markers; it never assigns a primary by filename or heuristic. At Packet B, collection was 2,682: 9 `audit`, 2,673 zero-primary, and 0 multi-primary. The other eight primary categories were 0. The only nonzero qualifier was `packaging` at 2; other qualifier counts were 0. Phase 1 proposed taxonomy remained a heuristic proposal, not marker truth.

## Packet C — ratchet and lane comparison

The local RATCHET mode compares actual marker state against [taxonomy-ratchet.json](../../audit/archive/12ce7993/test-suite-baseline/taxonomy-ratchet.json), refreshed only after reviewed classification and deletion work. Its exact legacy zero-primary allowlist is currently 791 nodes after 1,703 reviewed Packet M nodes (1,691 classified and 12 deliberate false/skip gates), anchored to the pre-K committed state; it fails on any new zero-primary nodeid or any multi-primary item. Classified and deleted nodeids are removed from the allowlist. REPORT remains non-gating, and RATCHET is not wired into CI.

The selector comparison is [lane-comparison-phase2.json](../../audit/archive/12ce7993/test-suite-baseline/lane-comparison-phase2.json). It applies recorded file and marker selectors to pytest collection records and their actual markers. All frozen lane counts reproduced. Current broad release selectors collect 2,682 nodes: 2,671 frozen non-packaging nodes plus 11 checker audit tests. The explicit CI and archived lanes retain their frozen counts, including 54 nodes in each release macOS architecture list.

| Selector | Frozen | Current | Zero-primary | Audit |
| --- | ---: | ---: | ---: | ---: |
| `scripts/ci.py packaging` | 1 | 1 | 1 | 0 |
| `scripts/ci.py compat` | 202 | 202 | 202 | 0 |
| `scripts/ci.py cli` | 164 | 164 | 164 | 0 |
| `scripts/ci.py ssh` | 45 | 45 | 45 | 0 |
| `scripts/ci.py windows` pytest selector | 6 | 6 | 6 | 0 |
| `scripts/ci.py macos` | 55 | 55 | 55 | 0 |
| `scripts/ci.py contract` | 10 | 10 | 10 | 0 |
| shared release suite; workflow Linux/Windows; archived GUI | 2,671 | 2,682 | 2,671 | 11 |
| release workflow macOS arm64 / x86_64 | 54 / 54 | 54 / 54 | 54 / 54 | 0 |
| archived CI compat / cli / ssh_sftp | 286 / 164 / 45 | same | same | 0 |
| archived CI macos / windows / contract / packaging / wx-smoke | 69 / 41 / 10 / 1 / 59 | same | same | 0 |

The active release workflow is `workflow_dispatch` only. Its pytest job union leaves two packaging-marked nodes uncovered: `tests/test_wheel_packaging.py::test_built_wheel_contains_required_assets` and `tests/test_wx_packaged_smoke.py::test_packaged_wx_smoke_gate_reports_critical_stages`. The local `scripts/ci.py` selector union plus the shared release suite leaves the wx packaged smoke node uncovered. The Windows lane also invokes `unittest discover`; those commands are outside this pytest nodeid comparison. No CI files or selectors were changed. Automatic PR/push CI remains absent.

## Packet D — exact duplicates and false gates

The seven duplicate groups were reviewed against their assertions and canonical owners. The cleanup changed only test code and the taxonomy ratchet. No production behavior, CI selector, migration-completion ledger, or protected local guidance changed.

- D1 removed `tests/test_wave1_unicode_core_policy.py::TestEncodingBoundaryJustification::test_ssh_client_decode_justified`; the equivalent canonical owner remains `tests/test_wave0_unicode_baseline.py::TestEncodingBoundaryInventory::test_errors_replace_in_ssh`.
- D2 removed the two source-text-only SFTP boundary claims. Their runtime owner is the new `tests/test_ssh_files_byte_preservation.py::test_sftp_download_upload_roundtrip_preserves_arbitrary_bytes`, which exercises `SSHFilesBackend.download` and `upload`, checks exact byte preservation, and checks SFTP channel cleanup.
- D3 corrected `test_cluster_servers_visible_without_selected_job_when_provider_supports` to keep the job unselected while refreshing and asserting visible cluster status. The separate `test_raw_server_status_opens` triggers the raw-status action and checks the `lssrv` result at the viewer seam.
- D4 now gives `TestSchemaV4Audit::test_v4_optional_sections_ok` actual job-details, accounting, and cluster-status sections; `test_v4_valid` retains the minimal-schema case.
- D5 and D6 removed the duplicate Wave80 audit nodes while retaining `TestFilesBehavior::test_context_menu_labels_localized` and `TestOutputsBehavior::test_standard_output_localized` as canonical owners.
- D7 moved typed-password precedence to the marked unit/regression owner `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_typed_password_precedes_saved_secret`. Equivalent 71.2 and 71.3 duplicates were removed. The broader `tests/test_wx_connection_hardening.py::test_typed_password_precedence` remains because it also checks ssh_info, GUI connection, and storage invariants.

Relative to Packet C, collection removed exactly these seven legacy nodes and added the two replacement tests:

```text
REMOVED
tests/test_wave0_unicode_baseline.py::TestRiskClassification::test_p0_sftp_roundtrip_risks_documented
tests/test_wave1_unicode_core_policy.py::TestEncodingBoundaryJustification::test_files_ssh_utf8_justified
tests/test_wave1_unicode_core_policy.py::TestEncodingBoundaryJustification::test_ssh_client_decode_justified
tests/test_wave80_files_outputs.py::TestWave80Audit::test_files_context_menu_localized
tests/test_wave80_files_outputs.py::TestWave80Audit::test_outputs_standard_output_error_localized
tests/test_wx_connection_71_2.py::test_typed_password_precedence
tests/test_wx_connection_71_3.py::test_typed_password_precedence

ADDED
tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_typed_password_precedes_saved_secret
tests/test_ssh_files_byte_preservation.py::test_sftp_download_upload_roundtrip_preserves_arbitrary_bytes
```

There were no other old-node removals. The collection total is now 2,679. Actual marker state is 2 `unit`, 11 `audit`, 2,666 zero-primary, and 0 multi-primary; all other primary counts are 0. Qualifier counts are `regression=2`, `packaging=2`, and 0 for the other qualifiers. The ratchet allowlist was reduced by exactly the seven removed zero-primary nodeids and now contains 2,666 entries.

Focused validation passed: the two Wave78 behavior tests, the D1/D2/D4/D5/D6 owner tests, and the D7 service test all passed; taxonomy checker tests passed (11). Ruff passed on changed Python tests. REPORT, RATCHET, full collection (2,679), and `git diff --check` passed. The full suite remains incomplete for the Packet A blockers; no baseline blocker was modified.

## Packet E — weak lifecycle tests

All eight Phase 1 high-risk nodeids were rewritten while preserving their nodeids. The three wx updater cases now drive cancellation, close-veto, and a progress callback queued before close and dispatched afterward. The terminal fallback cases now exercise the visible legacy terminal and the WebView panel's diagnostic/non-parity path. The About dialog test constructs the real Qt object in an isolated subprocess with socket and desktop URL seams intercepted, and verifies version display plus the repository-link action. The manual updater test invokes the real orchestration method with recording scheduling seams. The packaged smoke test now explicitly supplies a missing artifact and verifies the CLI fails closed with persisted JSON listing all required stages as failed; the frozen-baseline missing-artifact failure remains recorded in the archive.

The carry-forward Wave78 no-selection and Wave80 Files canonical localization nodes were not changed in E. All 8 targeted candidates passed together; the About dialog also passed alone after isolation. REPORT remains 2,679 collected with 2 `unit`, 11 `audit`, 2,666 zero-primary, and 0 multi-primary. No new or removed nodeids were introduced by E. RATCHET and full collection are revalidated below.

## Packet F — GUI truthfulness (focused defect remediation and scoped review complete)

The GUI audit's real wx Files test reproduced the defect before the production change: the context-menu Rename event renamed the Unicode-named file and refreshed the visible row, but selection was cleared. The root cause was the common listing renderer in `src/hpc_gui/wx_local_files.py`: it deleted and recreated rows without carrying selection identity through refresh. The focused remediation passes selected logical paths into the renderer and remaps the renamed source path to the new path, then selects rows by that path. It uses neither timing sleeps nor row-index restoration. The local `wx_app` test fixture now calls `wx.YieldIfNeeded()` and processes pending events after destroying top-level windows; instrumentation showed one live Files frame before this drain and none after it, eliminating the `UnregisterClass` warning for this module. Rename conflict/error handling and local/remote separation are unchanged.

Validation after remediation: exact node **1 passed**; the earlier full Wave2 module **19 passed** with no teardown warning; adjacent file-action/local-file group **145 passed, 2 baseline nodes deselected**. An undeselected adjacent run produced **145 passed, 2 failed** in `test_wx_file_action_policy.py`; both exact action-matrix failures were rerun at frozen SHA `12ce79935bf076e1062c57dc7dbd148bad2bfae1` and reproduced with the same missing expected actions. Ruff passed for `src/hpc_gui/wx_local_files.py` and `tests/test_wave2_wx_ui_parity.py`. These failures are supplemental baseline evidence, not effects of the selection fix.

### Packet F closeout — 2026-09-13

Reviewed `test_wave2_wx_ui_parity.py`, parity/reporting, GUI feature inventory, Qt-removal audit, Wave9 workflow/result policy, Wave10 release evidence, and Wave76 terminal bridge evidence. The current Wave2 module passes **17 tests**. Context-menu Edit/Copy/Cut/Copy Path/Rename/New Folder/Refresh now dispatch through actual wx menu events; Copy Path checks clipboard text; invalid rename checks its user-visible error and preserved row; path entry and Parent button verify the new path and visible listing; toolbar and file-list assertions require actual controls/rows. Deleted the false Forward-button test (the control does not exist) and no-op status-bar test (the product has no status feedback control). Renamed five Wave2 nodes and removed two invalid nodes after live reference/selector searches.

The ten nodes in `test_wx_terminal_parity_evidence.py` instantiate the wx terminal panel but replace its JavaScript runner and SSH backend. They verify bridge payloads, not xterm DOM rendering. Reworded and renamed all ten tests, classified four as `contract` and six as `unit`, and retained `wx`/`subprocess` qualifiers. Updated the reporting generator’s test-name list, but did not run it because it writes the protected `docs/v2/GUI_TERM_001_EXECUTION_EVIDENCE.json`. The five other reviewed files retain audit/reporting/release/integration categories and make no GUI runtime claim.

Combined Packet F scope validation: **89 passed, 2 deselected**. The deselections are the known frozen Wave9 CI-result failure and the protected migration-evidence writer. Ruff passed on the full `src tests scripts` set after one `pytest` local-import shadowing issue in the repaired lifecycle test was removed. The focused Wave2 rename selection assertion verifies old path absent, new path present, and selection on the new logical path. Rename is offered only for a single-file context selection; multi-file Rename is intentionally absent.

## Packet G — historical/Wave ownership review

The reviewed ownership map is in the Wave document. The older files retain separate Unicode fixture/config-policy, local-browser model, wx interaction, remote SSH/SFTP, CI-policy, and release-evidence concerns. Waves 78–80 remain active collected tests despite the migration/parity ledger ending at Wave 77. Wave79 audit and provider-contract files both contain parser/schema validation, while the latter additionally owns production adapter and jobs-callback wiring; no matching pair passed the deletion gate in this packet, so no node was removed. Wave80 translation-key assertions are catalog contracts, not proof of visible GUI event behavior. No test or migration-ledger files changed in G.

## Packet H — settings/config isolation

The known hanging node `tests/test_connection_advanced_settings.py::ParallelismSourceOfTruthTests::test_settings_dialog_has_no_global_parallelism_editor` was repaired at the test seam: `Path.home()` now resolves to a real temporary directory instead of a default `MagicMock`, preventing the `Path.exists()` truthiness loop in backup-name selection. The test retains a real `SettingsDialog` and checks visible text-bearing controls for a global parallelism editor, replacing its private-attribute absence check. `tests/test_config_storage_atomic.py` and `tests/test_transfer_parallelism_migration.py` scope config paths to `tmp_path`; `tests/test_profile_transfer_settings.py` tests pure coercion/model values. The repaired node and adjacent modules passed: **13 passed**. The frozen-baseline hang evidence remains archived; the current test setup no longer hangs. No production change was made.

## Packet I — source-text behavior rewrites

The About-dialog and manual-updater candidates were already converted to behavior checks in E. The Command Palette regression test no longer reads `main_window.py`; it inspects the real Qt menu tree, confirms Help Center is the F1 action, triggers it, and observes the help handler. The job-context parser's source scan remains an explicit static security-policy audit, and the same node now also feeds a hostile directive value that would create a marker file if executed; parsing leaves the CPU value unknown and the file absent. The connection-controller node checks bytearray wiping at runtime and separately documents its static no-Qt architecture claim. The editor-model and lint-engine source scans remain explicitly scoped static architecture/security audits; lint value execution also has the existing adversarial runtime test.

The macOS signing source scan was replaced with a runtime failure-path test: signing tools and secrets are intercepted, no signing identity is returned, and the test confirms the temporary keychain is deleted and the certificate password is absent from output. The renamed node is immediately marked `release` and `regression`. Packet D already replaced the D1/D2 SFTP source claims with canonical runtime byte-preservation behavior. The named candidates are reviewed; the remaining repository-wide static scan candidates were not blanket-rewritten because genuine audit/contract checks are allowed. Validation: **57 targeted tests passed**, Ruff passed, REPORT collected **2,679** tests with 2 `unit`, 11 `audit`, 1 `release`, 2,665 zero-primary, and 0 multi-primary; RATCHET passed.

## Packet J — resource/concurrency cleanup

The SFTP worker test now exercises `SFTPChannelManager.open_transfer_sftp()` with only Paramiko's channel-construction seam replaced. Two barrier-synchronized workers receive distinct channels, each gets the production 60-second bound, and each closes its own channel. The selected-job concurrency test now checks all 60 selections, listener snapshots, bounded thread completion, and that each unsubscribe prevents later notifications. The real FTP overlap test records both distinct connection identities; transfer-dialog parallel-limit behavior was already observable and remained unchanged. Cancellation now signals when upload reaches the blocked operation rather than relying on a sleep before release.

The wx Jobs blocked-read test now calls the normal coalescing refresh path (its old helper forced refreshes), checks reads do not overlap, and records that actual UI reads run off the GUI thread. The separate direct-thread test `test_wx_jobs_stress_backend_workers_and_reads_are_bounded` was removed after its unique assertions were moved to that production path; its exact node reference search found no CI or script selector. The 100-reconnect terminal test now checks every replaced SSH object loses its subscriber and that close removes the final subscriber. Validation: **7 targeted tests passed**, Ruff passed, collection is **2,678**, actual marker state is 2 `unit`, 11 `audit`, 1 `release`, 2,664 zero-primary, 0 multi-primary; RATCHET passed. Frozen-baseline comparison contains exactly 14 recorded additions and 9 authorized removals, with no unexpected node changes.

## Packet K — reporting/E2E reclassification

`test_plugin_e2e.py::test_full_clean_user_lifecycle` crosses local registry fetch, plugin installation and storage, template loading and saved-profile survival, lint execution, version update, and failed-update rollback; it is marked `e2e` and `acceptance`. The five screenshot-manifest evidence checks, three reproducibility-bundle export checks, and four capability-report status/serialization checks are `reporting`, not E2E. The three release-manifest generator checks are `release`.

Wave79 parser registry, scontrol/sacct/lssrv schema parsing, profile schema/backward compatibility, and provider adapter/parser compatibility checks are `contract`; pure error formatting and `RawCommandResult` value checks are `unit`; Wave78 translation-key existence checks are `contract`. The screenshot current-commit validator's frozen known failure remains assertion-for-assertion untouched; this packet adds only its `reporting` marker, and it was excluded from the 88 passing targeted nodes. The ratchet allowlist now removes the 89 nodes classified in K plus the two exact retired Packet I/J nodeids. Validation: **88 targeted tests passed**, Ruff passed, REPORT is 2,678 total with 13 `unit`, 1 `e2e`, 62 `contract`, 11 `audit`, 12 `reporting`, 4 `release`, 2,575 zero-primary, and 0 multi-primary. RATCHET passed; no collected nodeids changed in K.

## Packet L — legacy static/migration ownership review

The parity-matrix renderer and its missing-map/justification gates are `reporting`; the feature-parity baseline document inventory and Qt-removal source/dependency/packaging/evidence gate are `audit`. Wave2 is marked `gui`/`wx` because its tests drive real optional wx controls and events; Packet F's failing selection assertion remains intact. Wave9's coverage inventory, test-source checks, and workflow-result policy are repository `audit`; the known missing-CI-workflow failure node received marker metadata only.

Wave76 terminal nodes that construct a real wx WebView/xterm panel and check visible rendering, input, resize, or close behavior are `gui`/`wx`; the `test_generate_parity_evidence` JSON writer is `reporting` and was not run because it writes the tracked migration evidence file. Its fake SSH adapter is not a real PTY, so the migration ledger's PARTIAL status and packaged-runtime blockers remain unchanged. Wave10 now marks config and encrypted navigation persistence as `integration`, i18n preservation as `contract`, pure Slurm directive/DocumentModel checks as `unit`, local OS Unicode-path probes as `runtime_smoke`, and source/document scans as `audit`. The method named `test_favorites_history_persistence` only imports a service and asserts the symbol exists; it remains zero-primary for later rewrite/classification review rather than being treated as persistence proof.

No test behavior, migration ledger, workflow, production file, or collected nodeid changed in L. Targeted validation: **63 passed / 1 known baseline node deselected** across parity, removal-gate, Wave9, and Wave10; **18 passed / F defect node deselected** in Wave2; **10 passed / evidence-writer node intentionally not run** in Wave76. Ruff passed. REPORT is 2,678 total: 15 `unit`, 3 `integration`, 29 `gui`, 1 `e2e`, 3 `runtime_smoke`, 63 `contract`, 62 `audit`, 16 `reporting`, 4 `release`, 2,482 zero-primary, and 0 multi-primary. The reviewed ratchet allowlist is 2,482 and matches actual zero-primary debt.

## Packet M progress — full-suite classification (IN_PROGRESS)

Forty-three behavior-reviewed tranches are classified; the heuristic proposal remains navigation only. The 53 nodes in `tests/test_wave79_provider_contract.py` cover parser/schema/provider compatibility (`contract`), isolated raw-result/error formatting (`unit`), and production jobs callback wiring (`integration`). All 53 passed.

All 48 nodes in `tests/test_plugin_installer.py` are classified by behavior: registry/manifest/API and SemVer constraints are `contract`, cache/fetch/install/update/rollback/staging/storage flows are `integration`, and isolated latest-version/download decisions are `unit`. All 48 passed.

All 41 nodes in `tests/test_plugin_core.py` are classified: default values, path formatting, and small conversion helpers (9) are `unit`; active-index and local-plugin loader composition (4) are `integration`; saved-profile compatibility, app/plugin API constraints, and malformed/missing plugin schema cases (28) are `contract`. All 41 passed; Ruff passed.

All 22 nodes in `tests/test_plugin_security.py` are classified: plugin and command validation policies (10) are `contract`; URL policy/final-response behavior at the isolated fetcher seam (3) is `unit`; installer failure rollback, disable/activate/remove state, and install logging across storage/loader/installer (9) are `integration`. All 22 passed; Ruff passed.

All 10 nodes in `tests/test_plugin_contract.py` are classified: official registry/manifest compatibility (3) is `contract`, install/load/lint/template/update flows (6) are `integration`, and external README metadata evidence (1) is `audit`. The module is marked `artifact_dependent`; all 10 were skipped because `HPC_GUI_CONTRACT_REPO` is unset. The adjacent official plugin repository is dirty, so it was not used or modified.

All 10 nodes in `tests/test_plugin_integrity.py` are `integration`: installation, integrity anchors, tamper detection, legacy record migration, state recovery, and rollback cross the real installer, filesystem storage, integrity verifier, and loader. All 10 passed; Ruff passed.

`tests/test_plugin_entrypoint_errors.py` has two `unit` checks of the MainWindow error/logging method through explicit UI reporting seams; they do not claim visible GUI behavior. The three `tests/test_plugin_v2.py` nodes split into a `contract` validator policy, `unit` no-execution check, and `integration` installer rejection. All five passed; Ruff passed.

All seven `tests/test_plugin_profile_v2.py` and `tests/test_plugin_profile_v3.py` nodes are `contract`: they cover typed profile schemas, version compatibility, runtime-shape preservation, and rejection of unsafe or unknown fields. All seven passed; Ruff passed.

The nine `tests/test_plugin_template_integration.py` nodes split into six `gui` tests that drive real Qt ConnectionDialog menus/actions and observe fields/saved profiles, plus three `integration` tests of plugin-to-template adaptation; all nine passed. The nine Fluent plugin nodes and the four v2 integration nodes are marked `integration`; the 13 that require published plugin artifacts carry `artifact_dependent`. Fluent nodes were not executed because the adjacent plugin checkout is dirty; the four v2 integration nodes skipped under their unset-repo guard.

All 37 `tests/test_plugin_manager_ui.py` nodes are marked individually: 32 drive real PluginManagerDialog/MainWindow widgets and inspect visible state/actions (`gui`), four isolate helper/message behavior (`unit`), and the translation-key catalog check is `contract`. All 37 passed; Ruff passed. Review found that `test_rollback_requires_confirmation_and_runs_off_gui_thread` runs a worker synchronously through `_FakePool`, so it does not prove the thread claim in its name. It remains classified by the real observable dialog behavior, without the `concurrency` qualifier, and is recorded for later truthful rewrite.

All 21 `tests/test_ui_contributions.py` nodes are classified: plugin contribution shape, action/condition policy, localization fallback, and constraints are `contract` (16); installed state and version rebuild behavior are `integration` (4); deterministic contribution ordering is `unit` (1). All 21 passed; Ruff passed.

The three wx plugin manager tests and three wx lifecycle tests exercise pure Python models/controllers without real wx objects, so they are `unit`; the three wx settings model tests exercise settings serialization, profile boundaries, shortcut compatibility, and macOS defaults, so they are `contract`. All nine passed; Ruff passed.

All 20 nodes in `tests/test_wave4_favorites_history_persistence.py` are classified: 3 i18n catalog/encoding checks are `contract`; 7 in-memory favorites/history behavior checks are `unit`; 10 persistence, serialization, profile isolation, encryption/tamper, delete, and full workflow checks are `integration`. All 20 passed; Ruff passed.

All 11 nodes in `tests/test_remote_navigation_store.py` are classified: nine file-backed store, profile isolation, encryption/tamper, and deletion checks are `integration`; shared-instance and unavailable-secure-storage guards are `unit`. All 11 passed; Ruff passed. Favorites-toggle and history behavior overlaps Wave4 coverage; no duplicate cleanup was performed, and canonical ownership remains for a separately scoped review.

All nine `tests/test_sftp_channel_manager.py` nodes are `unit`, since Paramiko transport/channel creation stays at a fake external seam. Six tests also carry `resource`: each actually obtains a channel handle and checks timeout, reuse, release, or idempotent close. All nine passed; Ruff passed.

Thirteen transfer nodes are classified. The three `tests/test_transfer_integrity.py` file-digest/cancellation/progress tests are `unit`. In `tests/test_transfer_cancel_recovery.py`, the controller batch invariant is `unit`/`concurrency`, while the two TransferDialog/controller cancellation/retry flows are `integration`/`concurrency`. All seven `tests/test_upload_pipelining.py` nodes are `unit`; the five that obtain and close a fake SFTP channel carry `resource`. All 13 passed; Ruff passed.

The 28 overwrite/resume nodes in `tests/test_transfer_resume_semantics.py` are all `unit`/`semantic`: FTP and SFTP fakes record protocol verbs, offsets, truncation/appending, no-op and invalid-size cases; each outcome is compared by exact bytes. All 28 passed; Ruff passed.

All 10 nodes in `tests/test_transfer_concurrency.py` are classified: the FTP capability flag is `contract`; isolated backend-close lifecycles (4) are `unit`/`resource`; true FTP/SFTP backend concurrency/isolation (2) is `integration`/`resource`/`concurrency`; visible transfer-limit labels (2) are `gui`. All 10 passed; Ruff passed. The node named `test_cancelled_transfer_releases_isolated_backend` does not call the dialog cancellation path: it sleeps 50 ms, releases a fake backend, and checks cleanup after a simulated exception. It is not counted as concurrency evidence and remains queued for a truthful rewrite.

The four `tests/test_transfer_performance_scenarios.py` nodes are split across visible row bounds (`gui`/`performance`), deterministic performance-report output (`reporting`/`performance`), progress throttling (`unit`/`performance`), and four-worker dialog completion (`integration`). The four `tests/test_transfer_controller.py` nodes are `unit`; only the enqueue-while-running case carries `concurrency`, because its start/release events establish ordering. All eight passed; Ruff passed. The controller-cancel test uses a fixed 20 ms sleep rather than an entered event and is not treated as controlled concurrency evidence.

All 22 `tests/test_file_filter_registry.py` nodes are reviewed: core matching, overlap, edge cases, and deterministic order (18) are `unit`; provider-filter registration/coexistence interface (4) is `contract`. All 22 passed; Ruff passed.

All 25 `tests/test_output_channel_resolver.py` nodes are split into isolated output model/resolver behavior (11 `unit`) and legacy Slurm/provider-format compatibility (14 `contract`). All 25 passed; Ruff passed.

The six modules `test_crash_logging_filter.py`, `test_shortcut_preferences.py`, `test_profile_storage_areas.py`, `test_storage_policy.py`, `test_storage_policy_validation.py`, and `test_config_storage_atomic.py` add 19 reviewed nodes: isolated logging-filter, shortcut service, profile-model and storage-policy evaluator behavior is `unit`; keymap migration and storage-model validation are `contract`; atomic configuration writes against a temporary filesystem are `integration`/`resource`. The crash-filter cases are qualified `regression`, and storage-policy evaluation is qualified `semantic`. All 19 passed; Ruff passed.

The modules `test_paths.py`, `test_platform.py`, `test_installation_context.py`, `test_secret_store.py`, `test_log_redaction.py`, and `test_updater_helper.py` add 39 behavior-reviewed nodes: path selection and progress monotonicity are `unit`; real legacy-store copy/migration and installation-context composition are `integration`; platform naming, keychain adapter behavior, and supported storage contracts are `contract`; OS-specific updater install/rollback cases are `release`. Migration copies and updater backup/detach lifecycle cases carry `resource`; log-redaction assertions carry `semantic`. Thirty-eight passed; the symlink rejection case skipped because this Windows environment cannot create the required symlink. Ruff passed.

The three lint modules `test_lint_engine.py`, `test_linter_tools_v2.py`, and `test_editor_v2_lint.py` add 52 reviewed nodes. Rule evaluation and diagnostic adaptation/formatting are `unit`; malformed rule and supported-suffix compatibility is `contract`; the source scan is `audit`; installed-plugin and editor lint orchestration is `integration`; the real results-dialog widgets and actions are `gui`. Qt cases carry `qt`, and the tool temp-copy allocation/removal paths carry `resource`. All 52 passed; Ruff passed.

The version consistency and version-sync tests, local documentation-link/governance scans, branding and local-terminal asset checks, Qt license-source manifest and third-party version manifest writers, and About dialog checks add 11 nodes. Version/installer metadata and license manifests are `release`; repository/source/document policy scans are `audit`; the About dialog's actual Qt state and repository-button action are `gui`/`qt`, with its subprocess and semantic qualifiers recorded. All 11 passed; Ruff passed. The offscreen About instantiation check remains a weak lifecycle test for later review; this tranche did not rewrite it.

The command registry/palette/router, editor shortcut safety, geometry policy, gesture-help contract, keyboard contract, and Help-versus-palette action checks add 15 nodes. Registry and gesture-help compatibility are `contract`; the pure command, focus-routing, geometry, and terminal key conversion logic is `unit`; static contract-to-baseline comparison is `audit`; actual editor/window state and triggered actions are `gui`/ `qt`. All 15 passed; Ruff passed.

The four provider quota parsers, quota monitor/result registry, provider path context and override resolution, local provider form, path resolver, capability projection, and profile-diff logic add 24 nodes. External provider output/config shapes are `contract`; isolated resolution/projection/diff behavior is `unit`; the local provider form is `gui`/ `qt`; the quota monitor's real worker coalescing and close behavior is `integration`/ `concurrency`/ `resource`. All 24 passed; Ruff passed.

The terminal bridge/PTY/transport boundary, graphics settings, SSH byte-stream handling, and output follower modules add 22 nodes. Signal forwarding and bounded parsing are `unit`; the disposable SSH server PTY exchange is `integration`/ `resource`; static dependency/XSS-boundary checks are `audit`; real terminal header state is `gui`/ `qt`. UTF-8/stream behavior has `semantic`; output-follower stale-read invalidation is `concurrency` because it coordinates a blocked read and reassignment with events. All 22 passed; Ruff passed.

Update signature/artifact verification and SSH host-key/authentication option behavior are `contract`; isolated progress/state helpers are `unit`; local atomic download and remote download-plan composition are `integration`; actual transfer queue cancellation/key-release behavior is `gui`/ `qt`/ `concurrency`/ `resource`; installer/release asset and handoff plans are `release`; the install-handoff source substring check is `audit` and is not runtime proof. Thirty-seven tests passed; Ruff passed. The previously noted weak splash-before-worker test remains classified as a unit check and was not rewritten.

The Slurm job-output parsers, SBATCH directive reader/writer, script path and output-placeholder parser add 17 nodes. Each asserts compatibility with external scheduler output or directive syntax, so all are `contract`. All 17 passed; Ruff passed.

The Slurm array and dependency directive suites add 14 nodes; they verify accepted syntax, formatting, update/removal behavior, invalid-value rejection, and connection-scoped dependency compatibility, so all are `contract`. All 14 passed; Ruff passed.

The offline Slurm compatibility matrix and legacy transfer-parallelism config migration add 23 nodes: Slurm `squeue`/`sacct`/`scontrol` output shape is `contract`, the populated-job detail formatter is `unit`, missing scheduler command handling through the backend is `integration`, and the one-time persisted config migration is `contract`/`regression`. All 23 passed; Ruff passed.

Profile identity/storage, per-profile transfer settings, duplication and patch preservation, and profile import/export add 26 nodes. Persisted config compatibility and schema import/export are `contract`; isolated settings/duplication logic is `unit`; disk-backed profile ID/private state flows are `integration`; actual form-field state and save behavior are `gui`/`qt`. Regression and semantic qualifiers reflect preserved fields and user-visible profile data. The `test_invalid_schema_and_cancelled_preview_do_not_write` test only asserts that `import_profile` exists for the alleged cancellation path; it is marked `contract` for its real invalid-schema assertion and recorded for a later truthful rewrite. All 26 passed; Ruff passed.

The latest 94-node tranche covers job context, selected-job state, output widgets, record/provenance storage, tracking, failure explanations, history summaries, and job templates. Scheduler/Fluent syntax checks are `contract` (with the source-only no-product-constants check as `audit`); SelectedJobContext and its store are `unit`; all 20 real Qt output-widget interactions are `gui`/`qt`; SQLite record/provenance flows are `integration`; tracking and classification logic are `unit`; template package loading is `contract`, rendering is `unit`, and the published Fluent template flow is `integration`. The source-level no-Qt check is `audit`. Focused validation passed 93 tests; the one published Fluent template test was intentionally deselected because it reads the dirty adjacent plugin repository. Review found that the selected-job stale-response cases do not dispatch a delayed response, and the concurrent subscribe/select case checks only that calls do not crash; they remain queued for later truthful concurrency review.

The next 20-node tranche in `tests/test_connection_advanced_settings.py` adds 17 truthful primary markers: 14 real dialog/profile-state checks are `gui`/`qt`, the static old-API absence check is `contract`, and two isolated coercion checks are `unit`. All 20 passed, including the previously hanging settings-dialog node. Three tests remain zero-primary after review because they assert test-local computed policy or fake-object identity without invoking production logic; they are recorded as false gates for later cleanup rather than assigned a misleading category.

The 20-node directory-comparison tranche adds 19 primary markers: 13 pure comparison/snapshot checks are `unit`, three composed snapshot/widget flows are `integration`/`qt`, and three actual control/visibility interactions are `gui`/`qt`. The 20,000-entry timing probe carries `performance`. All 20 passed. `test_parent_row_blank_and_sort_roles_intact` remains zero-primary because it conditionally checks a parent row, does not assert sorting behavior, and ends with `assertTrue(True)`; it is a false gate for later cleanup.

The 16-node Wave78 jobs-details tranche has 12 real wx panel interaction tests marked `gui`/`wx`, the parser-failure state and stale-result callback paths marked `integration`/`wx` (the latter also `concurrency`), and the result-model conversion marked `unit`. All 16 passed, including the no-selection cluster-status test, which now leaves the job genuinely unselected and checks visible status/table state. `test_cluster_servers_hidden_for_unsupported_provider` remains zero-primary because it only asserts tab count; raw-details/accounting tests still permit empty stdout and are recorded for later strengthening.

The 17-node corrective Wave78 tranche classifies eight visible wx workflow interactions as `gui`/`wx`, two callback/state flows as `integration`/`wx`, five localization/source-interface checks as `contract` (wx-qualified where they inspect the wx viewer interface), and RawCommandResult conversion as `unit`. All 17 passed. The local refresh-callback test and the “each viewer” test only exercise their own test-created values without calling the production viewer, so both remain zero-primary for later rewrite.

The 45-node Wave80 Files/Outputs tranche classifies five tests that inspect real wx notebooks, control labels, or a follow action that populates an output channel as `gui`/`wx`; the remaining 40 translation-key and parser compatibility checks are `contract`. All 45 passed. The retained high-risk `test_context_menu_labels_localized` checks translation strings only, so it is accurately marked `contract`; it still does not prove that a real Files context menu displays them and remains queued for Packet F-style GUI review.

The 16-node Wave50 Files/Outputs tranche drives real wx panel selection, file-list, output-follow, pause, reconnect, detached-window and scroll behavior; all 16 are `gui`/`wx`. Four actual stale-result races carry `concurrency`, because the tests verify the later job/path remains visible after a delayed earlier response. All 16 passed. The close-in-flight test only checks a closed flag after fixed waits, and the pause/resume test only checks internal state; they remain queued for resource/concurrency truthfulness review.

The 25-node wx Jobs tranche classifies three pure model tests as `unit`, two source-boundary checks as `audit`/`wx`, and 17 real wx panel tests as `gui`/`wx`; two event-driven stale-result assertions also carry `concurrency`. The focused run had 22 passes and three unconditional skips. Those three tests call `pytest.skip` before exercising their bodies because of a “flaky” note; they remain zero-primary for later skip-policy cleanup. The “does not overlap remote reads” test only proves worker-thread use and output completion, so it is not marked `concurrency`.

The 28-node wx connection-profile tranche classifies ten real controls/dialog actions as `gui`/`wx`, five storage/service flows as `integration`, six isolated model/service checks as `unit`, five config/i18n shapes as `contract`, and one source-policy check as `audit`. The no-op cancel test remains zero-primary: it only reads an empty store twice without invoking cancellation or production code. The 20-node connection-hardening tranche has seven `gui`/`wx` actions, six secret/storage/self-test `integration` flows, three `unit` model checks, two `audit` source checks, and two config/i18n `contract` checks. All 48 passed and Ruff passed. `test_save_and_connect_invokes_connect_once` separately saves and calls a model, so it is only classified for the model's isolated unit behavior; it does not prove the composed workflow. The MFA “not logged” claims are queued for review because their assertions do not establish the logging level or a production log event.

The 17-node remote-directory listing/sorting/create tranche classifies 12 observable RemoteDirPanel tree/tab behaviors as `gui`/`qt`, three stale/cache/backend collaboration checks as `integration`/`qt`, three create/path-validation helpers as `unit`/`qt`, and two SSH listing-channel lifecycle checks as `unit`/`resource`. Stale-navigation cancellation and burst coalescing assert that obsolete targets are abandoned and only the final destination is listed, so both carry `concurrency`. All 17 passed; Ruff passed.

After 1,279 reviewed nodes (1,268 classified, 11 deliberately left zero-primary), actual REPORT state is 2,678 collected: 359 `unit`, 169 `integration`, 397 `gui`, 1 `e2e`, 3 `runtime_smoke`, 423 `contract`, 85 `audit`, 17 `reporting`, 24 `release`, 1,200 zero-primary, and 0 multi-primary. Qualifiers include `semantic=103`, `regression=37`, `performance=4`, `resource=41`, `concurrency=54`, `subprocess=2`, `license=2`, `artifact_dependent=22`, `qt=132`, and `wx=278`. The exact ratchet allowlist contains 1,200 zero-primary nodeids; RATCHET passes with no new debt. The 12-node `test_remote_entry_helpers.py` tranche is `unit`/`semantic` throughout and covers size formatting, file-type labels, entry categories, and natural sort keys; all 12 passed and Ruff passed. The 12-node `test_wx_file_actions_behavior.py` tranche marks six real wx row/key/delete/context interactions `gui`/`wx`; six platform reveal command-selection cases are `unit` because OS and process seams are patched. All 12 passed and Ruff passed. The 25-node wx editor cross-view/tabs tranche marks all real editor panels, buttons, notebook events, and visible document/session state `gui`/`wx`; five worker-ownership and in-flight session-pinning tests also carry `concurrency`. All 25 passed and Ruff passed. The 22-node local editor-flow tranche adds 14 `gui`/`qt` EditorWidget user-state/action checks; in `test_local_edit_flow.py`, five EditorWidget/LocalDirPanel interactions are `gui`/`qt`, one UTF-8 read-boundary assertion is `contract`/`qt`, and two plugin-helper checks are `unit`. All 22 passed and Ruff passed. The 14-node `test_luna_fm_integration.py` tranche has eight file-backed profile persistence/compatibility checks (`integration`), three FtpWidget session/comparison/traffic checks (`integration`/`qt`), two SSH source-policy checks (`audit`), and one jump-host schema boundary (`contract`). All 14 passed and Ruff passed. The 14-node `test_wx_editor_window_parity.py` tranche classifies 13 real frame/window identity, dirty-state, save/replace, and close behaviors `gui`/`wx`, and the shell-to-editor-manager wiring check `integration`. The asynchronous dirty-save-before-replace case carries `concurrency`; the 25-cycle frame open/close leak check carries `resource`. All 14 passed and Ruff passed. The 14-node `test_wx_editor.py` tranche separates one `WxEditorModel` logic test (`unit`), two source-level optional-wx architecture checks (`audit`), and 11 real editor-frame actions/states (`gui`/`wx`). Eight async save/submit/run ordering, worker-thread, duplicate-click, failure-gating, and close-late-callback checks also carry `concurrency`. All 14 passed and Ruff passed. The 17-node `test_wx_file_keyboard_parity.py` tranche is `gui`/`wx`: real local/remote key events drive selection, copy/cut/paste, rename/delete, refresh, tab scoping, undo, and parent-navigation state. All 17 passed and Ruff passed. The active-tab refresh test builds a partial tab/control by hand, and remote Ctrl+V accepts two destinations with an `or` assertion; record both for stronger behavioral assertions later. The 26-node `test_wx_file_browser_tabs.py` tranche is `gui`/`wx`: local and remote tab creation/switch/close, middle-click actions, last-tab guards, and visible path/rows are real framework behavior. Three blocked stale-result tests that assert old results stay out of a newly active tab carry `concurrency`. All 26 passed and Ruff passed. Four close-in-flight cases assert tab count/path after completion but do not inspect stale rows; their concurrency wording remains a later strengthening candidate. The ten-node `test_wx_file_context_matrix.py` module is `gui`/`wx`: actual local/remote context events verify unselected-row targeting, selection retention, active-directory paste/create destinations, rename, new-tab, and clipboard actions. All ten passed and Ruff passed. The 28-node `test_wx_remote_file_actions_behavior.py` tranche classifies real remote-file actions, selection, clipboard, navigation, dialogs, backend effects, and visible rows as `gui`/`wx`; one backend thread-ownership test also carries `concurrency`. Twenty-seven passed; the filter-selection assertion failed twice with an empty listing instead of `run.log`. Relative to the frozen SHA, the test and production view logic are unchanged except marker metadata, so this is recorded as a supplemental baseline-signature failure, not a Packet M regression. Ruff passed.  `test_wx_editor_tabs.py::test_wx_editor_save_clears_dirty_marker` calls the model save-state method directly rather than exercising the UI save action; its GUI claim is limited to the resulting tab label and the save-action gap remains queued for review. The 19-node local directory/profile tranche adds seven `gui`/`qt` local-panel interactions, one filesystem-backed `integration` listing check, one path-fallback `unit` check, six `contract` config-shape checks, and four `gui`/`qt` FtpWidget session-state checks. All 19 passed and Ruff passed. The repository-root listing assertion is not filesystem-isolated, and the test named `test_missing_local_folder_keeps_global_behavior_without_modal` does not assert the modal claim; both are queued for later review. The file-action policy (16 nodes) is `unit`/`semantic`; the wx file-action lifecycle module (16 nodes) is `gui`/`wx`/`concurrency`. Its focused run passed 30 nodes; two remote exact-action matrix cases failed because returned actions exceed the expected sets. Test logic and production service are unchanged by Packet M; both findings remain for later review. Packet M remains in progress.

## Validation and current state

- Packet M local tests passed: **706 earlier passes + 93 profile-tranche passes + 20 connection-settings + 20 directory-comparison + 16 Wave78 jobs-details + 17 corrective Wave78 + 45 Wave80 Files/Outputs + 16 Wave50 Files/Outputs + 22 wx Jobs + 28 connection-profile + 20 hardening + 17 remote-directory listing/sorting/create + 30 file-action/lifecycle + 12 remote-entry helpers + 19 local directory/profile + 12 wx file-action behavior + 25 wx editor cross-view/tabs + 22 local editor-flow + 27 wx remote-file actions + 10 wx context matrix + 26 wx file-browser tabs + 17 wx keyboard parity + 14 wx editor + 14 wx editor window parity + 14 Luna file-manager integration = 1,262 observed focused passes**. One published-template integration node was intentionally deselected because it reads the dirty adjacent plugin repository. Eighteen nodes were skipped: ten plugin-contract nodes and four v2 integration nodes by repo-path guards, the Windows symlink-creation guard, and three unconditional flaky-test skips in `tests/test_wx_jobs_behavior.py`. Nine Fluent nodes and the published-template integration node were not run because they read from the dirty adjacent plugin repository. Ruff passed on reviewed modules. The file-action/lifecycle run had two failures: `tests/test_wx_file_action_policy.py::test_remote_policy_exact_matrix[one_file-selection1-expected1]` and `tests/test_wx_file_action_policy.py::test_remote_policy_exact_matrix[one_dir-selection2-expected2]`. Expected sets omit returned actions (`chmod`, `favorite`, `follow_track`, `submit_slurm`, and `new_file` across both cases). These are outside the original seven isolated baseline references; Packet M changed only markers, not tests or service. A third supplemental failure is `tests/test_wx_remote_file_actions_behavior.py::test_wx_remote_navigation_sort_and_provider_filter_are_visible`: the selected Logs filter yields an empty row list instead of `run.log`. The isolated rerun reproduced the same signature; frozen-baseline comparison shows only marker metadata changed in that test module and no change to `src/hpc_gui/wx_remote_files_view.py`.
- `python scripts/check_test_taxonomy.py --mode report --json-out <path>` — **exit 0**; actual state: 2,678 total, 359 `unit`, 169 `integration`, 397 `gui`, 1 `e2e`, 3 `runtime_smoke`, 423 `contract`, 85 `audit`, 17 `reporting`, 24 `release`, 1,200 zero-primary, 0 multi-primary; `semantic=103`, `concurrency=54`, `resource=41`, `qt=132`, `wx=278`.
- `python scripts/check_test_taxonomy.py --mode ratchet --baseline audit/archive/12ce7993/test-suite-baseline/taxonomy-ratchet.json` — **exit 0, RATCHET PASS**; 0 new zero-primary, 0 multi-primary; allowlist 1,200.
- `python -m pytest tests --collect-only -q` — latest full collection remains **2,678**; nodeid comparison to frozen inventory is still the approved cumulative delta: **14 added, 9 removed**, with no additional node changes from Packet M.
- Earlier focused checks remain: Packet E lifecycle candidates **8 passed**; Packet H settings/config group **13 passed**; Packet I source-text candidates **57 passed**; Packet J resource/concurrency group **7 passed**. Packet F truthful rename-selection test remains a product-defect failure and Packet F is stopped as required.
- Packet I/J Ruff checks and Packet M Ruff checks passed. `git diff --check` will be rerun before the next commit/closeout.
- The full suite is still not claimed as passing. Frozen audit limitations remain archived; Packet E repaired the packaged-smoke assertion and Packet H removed the current settings-test setup hang.

## Packet M latest checkpoint — terminal and core modules

The 19-node `test_wx_terminal_behavioral.py` tranche is now classified: seven real event/render interactions are `gui`, eight VT/search/Unicode interface behaviors are `contract`, three lifecycle/buffering checks are `unit`, and stale-output rejection after reconnect is `integration`; all carry `wx`, with applicable subprocess, semantic, and resource qualifiers. One full-run WebView2 readiness setup error did not reproduce on an isolated retry; the full module rerun passed 19/19. It is recorded as transient, not as a frozen-baseline failure.

The 22-node core tranche covers Ansys tool presentation/allowlist (integration/contract), AsyncCall deleted-signal handling (unit/resource), CLI entrypoint/import/session behavior (contract/unit/integration), cluster self-test statuses (unit), self-test copy formatting (unit), Qt keyboard telemetry (gui/qt), diagnostic bundle creation/redaction (integration), pointer-contract document mapping (audit), and help catalog/link behavior (integration/unit). All 22 passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; 375 `unit`, 175 `integration`, 404 `gui`, 1 `e2e`, 3 `runtime_smoke`, 434 `contract`, 86 `audit`, 17 `reporting`, 24 `release`; 1,159 zero-primary and 0 multi-primary. Qualifiers: `semantic=113`, `resource=43`, `subprocess=19`, `qt=133`, `wx=297`; other qualifier counts remain as in the checker JSON. After 1,320 reviewed nodes, 1,309 are classified and eleven reviewed false/skip gates intentionally remain zero-primary. The exact allowlist was refreshed and RATCHET passes. Focused successful test executions total 1,303, alongside the three separately recorded repeated baseline-signature failures and one transient WebView setup episode.

## Packet M checkpoint — real-wire cancellation and editor controller

The four `tests/test_download_cancel_wire.py` nodes are meaningful composed workflows over a disposable local SSH/SFTP server, the real transfer controller, and the remote file panel. They are marked `e2e`/`semantic`/`concurrency`/`slow`; the partial-retention/session-usability case also checks that cancellation does not leak a channel (`resource`). The three editor-controller nodes cover dirty tracking and command selection (`unit`) plus the model/Qt import boundary (`audit`). All seven passed, and Ruff passed.

Latest actual REPORT state is 2,678 collected: 377 `unit`, 175 `integration`, 404 `gui`, 5 `e2e`, 3 `runtime_smoke`, 434 `contract`, 87 `audit`, 17 `reporting`, 24 `release`; 1,152 zero-primary, 0 multi-primary. Qualifiers: `semantic=117`, `resource=44`, `concurrency=58`, `slow=4`, `subprocess=19`, `qt=133`, `wx=297`. After 1,327 reviewed nodes, 1,316 are classified and eleven deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 1,152-node allowlist. Focused successful executions total 1,310; three separately recorded repeatable baseline-signature findings remain, along with one transient WebView2 setup error that passed on retry and on a full module rerun.

## Packet M checkpoint — SSH jump-host connection and persistence

The 24-node `test_ssh_jump_host.py` module now distinguishes direct/legacy configuration and socket typing (`contract`), jump/target connection ordering, direct-tcpip routing, shared transport and channel use (`integration`), independent host-key roles (`contract`), failure/close cleanup (`integration`/`resource`), and persisted profile/dialog flows (`integration`, with config roundtrips also `regression`). All 24 passed; Ruff passed. No hardware or real remote host is involved; Paramiko clients are fakes.

Latest actual REPORT state: 2,678 collected; unit `377`, integration `192`, gui `404`, e2e `5`, runtime_smoke `3`, contract `441`, audit `87`, reporting `17`, release `24`; 1,128 zero-primary and 0 multi-primary. Qualifiers: `semantic=117`, `regression=39`, `performance=4`, `resource=51`, `concurrency=58`, `slow=4`, `subprocess=19`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=133`, `wx=297`. After 1,351 reviewed nodes, 1,340 have one primary marker and eleven deliberate false/skip gates remain zero-primary. The exact allowlist was refreshed and RATCHET passes. Focused successful executions total 1,334. Three repeatable supplemental baseline-signature failures and one transient WebView2 setup episode remain tracked separately from the frozen seven known isolated failures.## Packet M checkpoint — wx terminal WebView

The 29-node `test_wx_terminal_webview.py` module classifies real frame/page/event and terminal state as `gui`/`wx`, asset and byte-boundary rules as `contract`, the source-level WebView/Qt architecture checks as `audit`, and fake native WebView close behavior as `unit`/`resource`. Subprocess, semantic, concurrency, and resource qualifiers are limited to the matching behavior. The focused run completed with 27 passed and 2 XPASS in 31.16 seconds; the two expected-failure markers remain unchanged and need later xfail cleanup review. Ruff passed.

Latest actual REPORT state: 2,678 collected; unit `378`, integration `192`, gui `427`, e2e `5`, runtime_smoke `3`, contract `444`, audit `89`, reporting `17`, release `24`; 1,099 zero-primary and 0 multi-primary. Qualifiers: `semantic=128`, `regression=39`, `performance=4`, `resource=54`, `concurrency=59`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=133`, `wx=326`. After 1,380 reviewed nodes, 1,369 are classified and eleven false/skip gates deliberately remain zero-primary. RATCHET passes with an exact 1,099-node allowlist. Focused successful passes total 1,361, with the two XPASS outcomes recorded separately; three repeatable supplemental baseline-signature failures and one transient WebView2 setup episode remain separate from the frozen seven baseline failures.## Packet M checkpoint — wx file transfer integration

The 29-node file-transfer module has 27 `integration` owners for transfer session snapshots, operation cleanup, conflict decisions, direction-specific resume, and FTP/SFTP byte results; the visible progress surface and its cancel button are `gui`/`wx`. Concurrency and resource qualifiers are limited to mutable session, cancellation, and active-session cleanup assertions; semantic qualifies conflict outcomes and exact transferred bytes. All 29 passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit `378`, integration `219`, gui `429`, e2e `5`, runtime_smoke `3`, contract `444`, audit `89`, reporting `17`, release `24`; 1,070 zero-primary and 0 multi-primary. Qualifiers: `semantic=148`, `regression=39`, `performance=4`, `resource=59`, `concurrency=63`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=133`, `wx=355`. After 1,409 reviewed nodes, 1,398 have a primary and eleven false/skip gates remain deliberately zero-primary. RATCHET passes with the exact 1,070-node allowlist. Focused successful passes total 1,390; the two XPASS outcomes from the prior terminal module and three repeated supplemental baseline-signature failures remain separately recorded.## Packet M checkpoint — wx transfer window lifecycle

The 14-node `test_wx_transfer_ui_lifecycle.py` module exercises real wx transfer windows, close/cancel actions, late callbacks, visible success/failure/progress state, runtime translation, and file-browser-to-transfer workflows. All are `gui`/`wx`; active-session release cases carry `resource`, and close/cancel interleavings carry `concurrency`. The focused run passed 14/14 in 5.55 seconds. wxWidgets printed an `UnregisterClass` warning during app teardown, but pytest exited 0; the module suppresses Ruff checks with its existing file-level directive.

Latest actual REPORT state: 2,678 collected; unit `378`, integration `219`, gui `443`, e2e `5`, runtime_smoke `3`, contract `444`, audit `89`, reporting `17`, release `24`; 1,056 zero-primary and 0 multi-primary. Qualifiers: `semantic=148`, `regression=39`, `performance=4`, `resource=68`, `concurrency=66`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=133`, `wx=369`. After 1,437 reviewed nodes, 1,426 have a primary and eleven false/skip gates remain deliberately zero-primary. RATCHET passes with the exact 1,056-node allowlist. Focused successful passes total 1,404, plus two prior XPASS results. The teardown warning is recorded for the later resource review.## Packet M checkpoint — wx updater dialog

The 18-node `test_wx_updater_spec.py` module exercises actual wx updater/install dialog controls and visible state, so all nodes are `gui`/`wx`. The cancel-before-install, close-in-flight, and queued-late-callback cases carry `concurrency`; close-related paths also carry `resource`. All 18 passed in 7.83 seconds; Ruff passed. No test body changed in this tranche.

Latest actual REPORT state: 2,678 collected; unit `378`, integration `219`, gui `461`, e2e `5`, runtime_smoke `3`, contract `444`, audit `89`, reporting `17`, release `24`; 1,038 zero-primary and 0 multi-primary. Qualifiers: `semantic=148`, `regression=39`, `performance=4`, `resource=70`, `concurrency=69`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=133`, `wx=387`. After 1,455 reviewed nodes, 1,444 have one primary and eleven false/skip gates deliberately remain zero-primary. RATCHET passes with the exact 1,038-node allowlist. Focused successful passes total 1,422, plus two earlier XPASS results and the previously recorded teardown warning.## Packet M checkpoint — shell notifications and transfer conflict dialog

The 13-node `test_wx_shell_p0.py` module splits real language/menu/jobs-window state (`gui`/`wx`), shell-to-job/tray notification flows (`integration`/`wx`), and the isolated job-state model (`unit`). Session-reconnect and blocked-poll ownership checks carry `concurrency`; close cleanup carries `resource`. The 11-node transfer-conflict dialog module is `gui`/`wx`, with `semantic` on rename validation and decision results. Ruff passed. The combined focused run had 23 passed and one failure; `test_wx_shell_completion_states_and_deduplication` timed out waiting for the `COMPLETING` row in both the combined and isolated runs. Its test body and relevant production files match frozen baseline; only markers changed, so this is a fourth supplemental baseline-signature finding, outside the original seven.

Latest actual REPORT state: 2,678 collected; unit `379`, integration `225`, gui `478`, e2e `5`, runtime_smoke `3`, contract `444`, audit `89`, reporting `17`, release `24`; 1,014 zero-primary and 0 multi-primary. Qualifiers: `semantic=158`, `regression=39`, `performance=4`, `resource=72`, `concurrency=71`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=133`, `wx=411`. After 1,479 reviewed nodes, 1,468 have one primary and eleven false/skip gates remain deliberately zero-primary. RATCHET passes with the exact 1,014-node allowlist. Focused successful passes total 1,445; two prior XPASS outcomes and the wx teardown warning remain separately recorded.## Packet M checkpoint — Wave2 local directories and files

The 48-node local-file model suite now has 44 `unit` owners for listing, CRUD, context policy, clipboard, sorting, path navigation, search, errors, and conflict behavior; 2 `contract` nodes cover URL encoding; and the composed Unicode create/navigate/rename/copy/delete workflow is `integration`. All 48 passed and Ruff passed. `TestClipboardOperations.test_paste_into_itself_guard` remains zero-primary because it calls the guard path without asserting a result or filesystem effect; it is the twelfth deliberate false gate.

Latest actual REPORT state: 2,678 collected; unit `423`, integration `226`, gui `478`, e2e `5`, runtime_smoke `3`, contract `446`, audit `89`, reporting `17`, release `24`; 967 zero-primary and 0 multi-primary. Qualifiers: `semantic=158`, `regression=39`, `performance=4`, `resource=72`, `concurrency=71`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=133`, `wx=411`. After 1,527 reviewed nodes, 1,515 are classified and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 967-node allowlist. Focused successful passes total 1,493; two XPASS and four repeated supplemental baseline-signature findings remain recorded separately.## Packet M checkpoint — FtpWidget transfer activity (first slice)

The first 25 transfer-activity nodes in `test_ftp_widget.py` cover visible queue/history/progress/priority and button state (`gui`/`qt`), four isolated transfer-item/plan conversions (`unit`), and worker-gating/remote-size planning collaboration (`integration`). Queue-worker, cancel, and overlapping-controller cases carry `concurrency`. All 25 passed across a 24-node focused batch and the final housekeeping-row node; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit `427`, integration `228`, gui `497`, e2e `5`, runtime_smoke `3`, contract `446`, audit `89`, reporting `17`, release `24`; 942 zero-primary and 0 multi-primary. Qualifiers: `semantic=158`, `regression=39`, `performance=4`, `resource=72`, `concurrency=74`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=152`, `wx=411`. After 1,552 reviewed nodes, 1,540 are classified and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 942-node allowlist. Focused successful passes total 1,518; two XPASS, four repeated supplemental baseline signatures, one transient WebView2 setup episode, and the wx teardown diagnostic remain separately tracked.## Packet M checkpoint — FtpWidget transfer planning and worker semantics

The next 18 `test_ftp_widget.py` nodes classify transfer-activity rows, modeless progress, and capability hint state as `gui`/`qt`; worker ownership, plan ordering, channel lifetime, and resume behavior as `integration`; and mixed mutation order as `unit`. Tests that assert worker limits, dependent operation order, SSH channel isolation, and nonblocking worker ownership carry `concurrency`; channel allocation/release carries `resource`; large queue publication and nonblocking delete carry `performance`; resume behavior carries `semantic`. All 18 passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit `428`, integration `238`, gui `504`, e2e `5`, runtime_smoke `3`, contract `446`, audit `89`, reporting `17`, release `24`; 924 zero-primary and 0 multi-primary. Qualifiers: `semantic=160`, `regression=39`, `performance=6`, `resource=73`, `concurrency=81`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=159`, `wx=411`. After 1,570 reviewed nodes, 1,558 have one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 924-node allowlist. Focused successful passes total 1,536; two XPASS, four repeatable supplemental baseline signatures, the transient WebView2 setup episode, and the wx teardown diagnostic remain logged separately.## Packet M checkpoint — upload preflight and folder transfer

The nine-node preflight/folder tranche marks the real preflight plan rows and embedded transfer activity `gui`/`qt`; confirmation persistence and disabled-dialog branching are isolated `unit`; executing local upload, gating cancellation, creating the empty-folder plan, and single-connection multi-folder workflow are `integration`. The cancellation gate and sequential connection ownership carry `concurrency`. All nine passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit `430`, integration `242`, gui `507`, e2e `5`, runtime_smoke `3`, contract `446`, audit `89`, reporting `17`, release `24`; 915 zero-primary and 0 multi-primary. Qualifiers: `semantic=160`, `regression=39`, `performance=6`, `resource=73`, `concurrency=83`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=162`, `wx=411`. After 1,579 reviewed nodes, 1,567 are classified and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 915-node allowlist. Focused successful passes total 1,545; prior XPASS, four repeated supplemental baseline signatures, transient WebView2 setup, and wx teardown diagnostic remain separately recorded.## Packet M checkpoint — transfer-conflict decisions

Three FtpWidget conflict-policy nodes now have one `gui`/`qt` test for visible action/check state, one `unit` test for conditional-decision normalization, and one `integration` test for the process-local “always use” behavior across panel instances. All three passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit `431`, integration `243`, gui `508`, e2e `5`, runtime_smoke `3`, contract `446`, audit `89`, reporting `17`, release `24`; 912 zero-primary and 0 multi-primary. Qualifiers: `semantic=160`, `regression=39`, `performance=6`, `resource=73`, `concurrency=83`, `slow=4`, `subprocess=42`, `license=2`, `acceptance=1`, `artifact_dependent=22`, `qt=163`, `wx=411`. After 1,582 reviewed nodes, 1,570 are classified and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 912-node allowlist. Focused successful passes total 1,548; prior XPASS, four supplemental baseline signatures, transient WebView2 startup, and wx teardown diagnostic remain separately tracked.No production files, CI/workflow selectors, migration completion ledgers, or protected local guidance files changed. Packet F remains `DEFECT_FOUND`; G–L are `DONE`; M is `IN_PROGRESS`; N–O are `NOT STARTED`. After the user reconnected Google Drive, the native Doc was created and populated; it will be refreshed after each classification checkpoint: https://docs.google.com/document/d/1RveXRBKAbk6RamanBxND6fjadwQ2IJ2SOyl0jznoRic/edit

## Packet M checkpoint — FtpWidget panel routing, submit candidates, and shell result

Two remote-panel signal-forwarding tests are integration; four submit/shell candidate and command-quoting tests are unit/semantic; the real Qt scrollable, screen-bounded shell result dialog is gui/qt/semantic. All seven passed, and Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 435, integration 245, gui 509, e2e 5, runtime_smoke 3, contract 446, audit 89, reporting 17, release 24; 905 zero-primary and 0 multi-primary. Qualifiers: semantic=165, regression=39, performance=6, resource=73, concurrency=83, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=164, wx=411, packaging=2; all other registered qualifiers are zero. After 1,589 reviewed nodes, 1,577 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 905-node allowlist. Focused successful passes total 1,555; four supplemental baseline signatures, two XPASS results, transient WebView2 startup, and the wx teardown diagnostic remain separately tracked. No collection nodes changed.
## Packet M checkpoint — MainWindow FTP action routing and batch submit

Three MainWindow/worker tests are integration/semantic: remote FTP events route to the existing directory handlers, batch shell actions select the login terminal and preserve command order, and the batch submit worker continues after one failed script while preserving per-file results. The top-level Files tab test is gui/qt and checks the real tab widget identity. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 435, integration 248, gui 510, e2e 5, runtime_smoke 3, contract 446, audit 89, reporting 17, release 24; 901 zero-primary and 0 multi-primary. Qualifiers: semantic=168, regression=39, performance=6, resource=73, concurrency=83, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=165, wx=411, packaging=2; all other registered qualifiers are zero. After 1,593 reviewed nodes, 1,581 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 901-node allowlist. Focused successful passes total 1,559. No collection nodes changed.
## Packet M checkpoint — job-output follow modes and settings

The follow-mode destination and active-job binding behaviors are gui/qt/semantic because they operate on real Qt widgets and check selected-tab state. Helper selection for each configured mode is unit/semantic; the legacy follow-setting migration is contract/regression; actual single-file and combined output/error follower windows and the SettingsDialog mode/tooltips/apply interaction are gui/qt/semantic. All seven passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 436, integration 248, gui 515, e2e 5, runtime_smoke 3, contract 447, audit 89, reporting 17, release 24; 894 zero-primary and 0 multi-primary. Qualifiers: semantic=174, regression=40, performance=6, resource=73, concurrency=83, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=170, wx=411, packaging=2; all other registered qualifiers are zero. After 1,600 reviewed nodes, 1,588 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 894-node allowlist. Focused successful passes total 1,566. No collection nodes changed.
## Packet M checkpoint — scratch/home profile defaults and settings

The two panel-root/default-path behaviors and LoginWidget profile refresh are integration/semantic; the connected SettingsDialog remote-default reset, apply/close lifecycle, upload-confirmation setting, and inactive-profile control state are gui/qt (semantic where the test asserts a behavior transition). All seven passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 436, integration 251, gui 519, e2e 5, runtime_smoke 3, contract 447, audit 89, reporting 17, release 24; 887 zero-primary and 0 multi-primary. Qualifiers: semantic=180, regression=40, performance=6, resource=73, concurrency=83, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=174, wx=411, packaging=2; all other registered qualifiers are zero. After 1,607 reviewed nodes, 1,595 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 887-node allowlist. Focused successful passes total 1,573. No collection nodes changed.
## Packet M checkpoint — local transfer routing and asynchronous folder planning

Local upload/download routing and the local-panel selected-folder route are integration/semantic. The real queued folder-upload preflight/controller attachment is gui/qt/semantic/concurrency, and asynchronous remote-folder download planning is integration/semantic/concurrency. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 436, integration 254, gui 520, e2e 5, runtime_smoke 3, contract 447, audit 89, reporting 17, release 24; 883 zero-primary and 0 multi-primary. Qualifiers: semantic=184, regression=40, performance=6, resource=73, concurrency=85, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=175, wx=411, packaging=2; all other registered qualifiers are zero. After 1,611 reviewed nodes, 1,599 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 883-node allowlist. Focused successful passes total 1,577. No collection nodes changed.
## Packet M checkpoint — off-GUI-thread upload/download planning

Four asynchronous planner tests are integration/concurrency. Slow-probe early-return and multi-folder concurrency bounds carry performance; the held multi-folder transfer dialog has a resource qualifier, and conflict-aware download planning also carries semantic. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 436, integration 258, gui 520, e2e 5, runtime_smoke 3, contract 447, audit 89, reporting 17, release 24; 879 zero-primary and 0 multi-primary. Qualifiers: semantic=185, regression=40, performance=10, resource=74, concurrency=89, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=175, wx=411, packaging=2; all other registered qualifiers are zero. After 1,615 reviewed nodes, 1,603 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 879-node allowlist. Focused successful passes total 1,581. No collection nodes changed.
## Packet M checkpoint — transfer completion, activation, and duplicate-plan guards

Transfer completion/cache invalidation and double-click action routing/debounce are integration/semantic; the active-plan duplicate guard is unit/semantic; the resizable real Qt transfer splitter is gui/qt. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 437, integration 261, gui 521, e2e 5, runtime_smoke 3, contract 447, audit 89, reporting 17, release 24; 874 zero-primary and 0 multi-primary. Qualifiers: semantic=189, regression=40, performance=10, resource=74, concurrency=89, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=176, wx=411, packaging=2; all other registered qualifiers are zero. After 1,620 reviewed nodes, 1,608 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 874-node allowlist. Focused successful passes total 1,586. No collection nodes changed.
## Packet M checkpoint — connection-dialog modes and advanced profile fields

The SettingsDialog transfer-mode choices/default, generic Slurm menu action, advanced-section collapse/restore, and minimum dialog width are gui/qt (semantic where a user action changes state). The advanced profile form-to-configuration roundtrip is contract/regression because it verifies the persisted field interface. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 437, integration 261, gui 525, e2e 5, runtime_smoke 3, contract 448, audit 89, reporting 17, release 24; 869 zero-primary and 0 multi-primary. Qualifiers: semantic=192, regression=41, performance=10, resource=74, concurrency=89, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=180, wx=411, packaging=2; all other registered qualifiers are zero. After 1,625 reviewed nodes, 1,613 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 869-node allowlist. Focused successful passes total 1,596. No collection nodes changed.
## Packet M checkpoint — saved-password preference and credential migration

The no-prompt password preference is gui/qt/semantic; legacy encrypted-profile migration to the protected credential field is contract/regression; reuse of a saved master password across settings, secret unprotection, and prompt suppression is integration/semantic. The Windows storage and secret cryptography boundaries are mocked, so this is not claimed as real Windows or hardware coverage. All three passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 437, integration 262, gui 526, e2e 5, runtime_smoke 3, contract 449, audit 89, reporting 17, release 24; 866 zero-primary and 0 multi-primary. Qualifiers: semantic=194, regression=42, performance=10, resource=74, concurrency=89, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=181, wx=411, packaging=2; all other registered qualifiers are zero. After 1,628 reviewed nodes, 1,616 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 866-node allowlist. Focused successful passes total 1,599. No collection nodes changed.
## Packet M checkpoint — user system-template save and update rules

Saving dialog fields through the template seam is integration/semantic; synchronizing edited nested provider fields is contract/regression; case-insensitive name persistence/update behavior is unit/semantic. All three passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 438, integration 263, gui 526, e2e 5, runtime_smoke 3, contract 450, audit 89, reporting 17, release 24; 863 zero-primary and 0 multi-primary. Qualifiers: semantic=196, regression=43, performance=10, resource=74, concurrency=89, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=181, wx=411, packaging=2; all other registered qualifiers are zero. After 1,631 reviewed nodes, 1,619 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 863-node allowlist. Focused successful passes total 1,602. No collection nodes changed.
## Packet M checkpoint — transfer-mode conversion, resume, and progress

The pure transfer-mode policy/byte conversion test is unit/semantic. Four backend-plus-filesystem workflows are integration/semantic: resume a partial download, reject stale download/upload metadata, and forward upload progress. The three partial-file cases carry resource; the two stale-metadata cases also carry regression. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 439, integration 267, gui 526, e2e 5, runtime_smoke 3, contract 450, audit 89, reporting 17, release 24; 858 zero-primary and 0 multi-primary. Qualifiers: semantic=201, regression=45, performance=10, resource=77, concurrency=89, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=181, wx=411, packaging=2; all other registered qualifiers are zero. After 1,636 reviewed nodes, 1,624 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 858-node allowlist. Focused successful passes total 1,607. No collection nodes changed.
## Packet M checkpoint — local and remote file-drop events

Four real Qt drag/drop handlers are gui/qt/semantic: local files dropped onto remote views/panel body or the DirectoriesWidget route to the current remote directory, and remote path payloads dropped onto the local tree route to downloads. The separate async upload-planning test is integration/semantic/concurrency and confirms the plan completes after the request returns. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 439, integration 268, gui 530, e2e 5, runtime_smoke 3, contract 450, audit 89, reporting 17, release 24; 853 zero-primary and 0 multi-primary. Qualifiers: semantic=206, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=185, wx=411, packaging=2; all other registered qualifiers are zero. After 1,641 reviewed nodes, 1,629 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 853-node allowlist. Focused successful passes total 1,612. No collection nodes changed.
## Packet M checkpoint — remote file context-menu routes

These four Qt-backed panel/context-menu routing tests are integration/semantic/qt. They keep real RemoteDirPanel selection and signal behavior but substitute a fake QMenu, so they verify menu construction/dispatch and outward routes without claiming native-menu rendering or pointer interaction. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 439, integration 272, gui 530, e2e 5, runtime_smoke 3, contract 450, audit 89, reporting 17, release 24; 849 zero-primary and 0 multi-primary. Qualifiers: semantic=210, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=189, wx=411, packaging=2; all other registered qualifiers are zero. After 1,645 reviewed nodes, 1,633 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 849-node allowlist. Focused successful passes total 1,616. No collection nodes changed.
## Packet M checkpoint — keyboard clipboard and file operations

Three real Qt key-event workflows cover Ctrl+C/Ctrl+X clipboard state, cross-tab remote copy/paste, and local Ctrl+V download; they are gui/qt/semantic. The in-memory MockFilesBackend CRUD/copy/move behavior is unit/semantic. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 440, integration 272, gui 533, e2e 5, runtime_smoke 3, contract 450, audit 89, reporting 17, release 24; 845 zero-primary and 0 multi-primary. Qualifiers: semantic=214, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=192, wx=411, packaging=2; all other registered qualifiers are zero. After 1,649 reviewed nodes, 1,637 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 845-node allowlist. Focused successful passes total 1,620. No collection nodes changed.
## Packet M checkpoint — directory navigation and keyboard rename

The local parent-row selection/navigation, local F2 filesystem rename, remote F2 action, and remote path-field Enter/Backspace navigation are gui/qt/semantic. These tests send real Qt key events or drive actual widget signals and assert directory, selection, or filesystem effects. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 440, integration 272, gui 537, e2e 5, runtime_smoke 3, contract 450, audit 89, reporting 17, release 24; 841 zero-primary and 0 multi-primary. Qualifiers: semantic=218, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, license=2, acceptance=1, artifact_dependent=22, qt=196, wx=411, packaging=2; all other registered qualifiers are zero. After 1,653 reviewed nodes, 1,641 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 841-node allowlist. Focused successful passes total 1,624. No collection nodes changed.
## Packet M checkpoint — local file open, platform dispatch, and associations

The exact local context-menu label list is a contract check. Explorer/file-manager dispatch and Open With persistence route through platform/process/storage seams and are integration/semantic; only the Explorer test is Windows-constrained, while the Linux-named test uses a Posix stub. The actual SettingsDialog file-association list and clear action is gui/qt/semantic. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 440, integration 275, gui 538, e2e 5, runtime_smoke 3, contract 451, audit 89, reporting 17, release 24; 836 zero-primary and 0 multi-primary. Qualifiers: semantic=222, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=197, wx=411, packaging=2. After 1,658 reviewed nodes, 1,646 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 836-node allowlist. Focused successful passes total 1,629. No collection nodes changed.
## Packet M checkpoint — directory tabs and folder creation

The local context-menu new-tab route and remote create/open-directory routes are integration/qt/semantic because QMenu is faked while real panels/tabs and filesystem backend effects are observed. Local create-directory-and-enter is gui/qt/semantic. The remote menu label list is a contract check. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 440, integration 278, gui 539, e2e 5, runtime_smoke 3, contract 452, audit 89, reporting 17, release 24; 831 zero-primary and 0 multi-primary. Qualifiers: semantic=226, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=201, wx=411, packaging=2. After 1,663 reviewed nodes, 1,651 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 831-node allowlist. Focused successful passes total 1,634. No collection nodes changed.
## Packet M checkpoint — remote file permissions

The context-menu permission change and accepted/rejected mode handling are integration/semantic/qt through a fake menu/dialog and MockFilesBackend; the permission bits on the backend entry are asserted. Checkbox/mode-field synchronization is gui/qt/semantic with the real PermissionsDialog. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 440, integration 281, gui 540, e2e 5, runtime_smoke 3, contract 452, audit 89, reporting 17, release 24; 827 zero-primary and 0 multi-primary. Qualifiers: semantic=230, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=205, wx=411, packaging=2. After 1,667 reviewed nodes, 1,655 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 827-node allowlist. Focused successful passes total 1,638. No collection nodes changed.
## Packet M checkpoint — directory tabs and middle-click decisions

Remote directory-tab/filter-tab state is gui/qt/semantic. Four middle-click handler tests use synthetic event objects and patch item hit-testing/tab-open seams, so they are unit/semantic/qt policy checks rather than GUI interaction evidence. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 444, integration 281, gui 541, e2e 5, runtime_smoke 3, contract 452, audit 89, reporting 17, release 24; 822 zero-primary and 0 multi-primary. Qualifiers: semantic=235, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=210, wx=411, packaging=2. After 1,672 reviewed nodes, 1,660 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 822-node allowlist. Focused successful passes total 1,643. No collection nodes changed.
## Packet M checkpoint — remote directory cache and refresh

Four remote-cache policy cases are unit/semantic/qt, the F5 key event forcing refresh is gui/semantic/qt, and completion-triggered invalidation of source/destination caches across panels is integration/semantic/qt. Both TTL scenarios remain classified separately; no duplicate cleanup was performed. All six passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 448, integration 282, gui 542, e2e 5, runtime_smoke 3, contract 452, audit 89, reporting 17, release 24; 816 zero-primary and 0 multi-primary. Qualifiers: semantic=241, regression=45, performance=10, resource=77, concurrency=90, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=216, wx=411, packaging=2. After 1,678 reviewed nodes, 1,666 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 816-node allowlist. Focused successful passes total 1,649. No collection nodes changed.
## Packet M checkpoint — remote-panel shutdown and deferred deletion

Four shutdown policy tests are unit/semantic/qt, with concurrency on the active-thread wait, worker cancellation, and still-running thread-ownership assertions. Two real deferred-delete cases are integration/qt/resource and verify registry cleanup/identity after the Qt event loop processes deletion. The stale-panel registry mutation path is integration/semantic/qt; its deleted-C++ error is simulated. All seven passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 452, integration 285, gui 542, e2e 5, runtime_smoke 3, contract 452, audit 89, reporting 17, release 24; 809 zero-primary and 0 multi-primary. Qualifiers: semantic=248, regression=45, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=223, wx=411, packaging=2. After 1,685 reviewed nodes, 1,673 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 809-node allowlist. Focused successful passes total 1,656. No collection nodes changed.
## Packet M checkpoint — download/upload conflicts, resume, and duplicate plans

Five conflict/resume flows are integration/semantic/qt: existing-target skip, one-plan download/upload resume, and per-file folder conflict decisions for both directions. Active-download duplicate suppression is unit/semantic/qt. The fake backends and dialog decisions are seams; temporary local files are isolated. All six passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 453, integration 290, gui 542, e2e 5, runtime_smoke 3, contract 452, audit 89, reporting 17, release 24; 803 zero-primary and 0 multi-primary. Qualifiers: semantic=254, regression=45, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=229, wx=411, packaging=2. After 1,691 reviewed nodes, 1,679 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 803-node allowlist. Focused successful passes total 1,662. No collection nodes changed.
## Packet M checkpoint — mock FTP download/upload and environment gate

Three MockFilesBackend file workflows are integration/semantic/qt, including nested binary roundtrips and Turkish path/content preservation (regression). The environment variable enabling the mock connection is contract/semantic/qt. These exercise real local files and Qt widgets around an in-memory remote seam; they are not real-network or E2E claims. All four passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 453, integration 293, gui 542, e2e 5, runtime_smoke 3, contract 453, audit 89, reporting 17, release 24; 799 zero-primary and 0 multi-primary. Qualifiers: semantic=258, regression=46, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=233, wx=411, packaging=2. After 1,695 reviewed nodes, 1,683 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 799-node allowlist. Focused successful passes total 1,666. No collection nodes changed.
## Packet M checkpoint — CLI version and profile commands

The fixed timeout exit code and JSON version shape are contract tests. Profile-show secrecy and invalid/missing input handling are unit/semantic; profile create/update roundtrips through the CLI and isolated config file are integration/semantic, with update preservation also marked regression. All eight passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 457, integration 295, gui 542, e2e 5, runtime_smoke 3, contract 455, audit 89, reporting 17, release 24; 791 zero-primary and 0 multi-primary. Qualifiers: semantic=264, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, qt=233, wx=411, packaging=2. After 1,703 reviewed nodes, 1,691 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 791-node allowlist. Focused successful passes total 1,674. No collection nodes changed.
## Packet M checkpoint — CLI profile update and deletion guards

Five remaining profile update/delete nodes are unit/semantic: they assert argument validation, missing-profile handling, explicit confirmation, and the expected service-call boundary. The positive delete case verifies one call and its JSON result; the negative cases verify no write/delete call. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 462, integration 295, gui 542, e2e 5, runtime_smoke 3, contract 455, audit 89, reporting 17, release 24; 786 zero-primary and 0 multi-primary. Qualifiers: semantic=269, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,708 reviewed nodes, 1,696 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 786-node allowlist. Focused successful passes total 1,679. No collection nodes changed.

## Packet M checkpoint — CLI environment, list, and usage paths

Seven command paths cover the isolated doctor environment JSON, two file-list invocations through a fake session, the version command’s successful text response, a refused remote delete, top-level parse failure, and unsupported doctor handling. The version output remains a contract; the others are unit checks, with semantic qualifiers on the observable error/output/no-session behaviors. All seven passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 468, integration 295, gui 542, e2e 5, runtime_smoke 3, contract 456, audit 89, reporting 17, release 24; 779 zero-primary and 0 multi-primary. Qualifiers: semantic=274, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,715 reviewed nodes, 1,703 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 779-node allowlist. Focused successful passes total 1,686. No collection nodes changed.

## Packet M checkpoint — CLI connection and operation failures

Two connection/operation failure paths are unit/semantic, JSON error-shape parity is contract/semantic, and timeout/key-path assembly through the real CLISession and SSHConnInfo objects is integration/semantic with SSH construction isolated at its external seam. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 470, integration 297, gui 542, e2e 5, runtime_smoke 3, contract 457, audit 89, reporting 17, release 24; 774 zero-primary and 0 multi-primary. Qualifiers: semantic=279, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,720 reviewed nodes, 1,708 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 774-node allowlist. Focused successful passes total 1,691. No collection nodes changed.

## Packet M checkpoint — CLI credential and debug policy

Six saved-secret/stdin/key-path precedence checks are unit/semantic against a stub secret-store seam. Strict-host-key propagation and verbose SSH logging/redaction traverse CLI session and SSH-client code around mocked SSH dependencies, so they are integration/semantic. All eight passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 476, integration 299, gui 542, e2e 5, runtime_smoke 3, contract 457, audit 89, reporting 17, release 24; 766 zero-primary and 0 multi-primary. Qualifiers: semantic=287, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,728 reviewed nodes, 1,716 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 766-node allowlist. Focused successful passes total 1,699. No collection nodes changed.

## Packet M checkpoint — file metadata and profile-test results

`files stat` versus `ls` exercises the real SSHFilesBackend through the CLI around a fake SFTP seam, so it is integration/semantic. Empty and missing listings use fake Files objects and are unit/semantic. Four profile-test success/failure, JSON/text parity, and missing-profile cases are contract/semantic for their public result shape, except the no-open-on-missing guard, which is unit/semantic. All eight passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 479, integration 300, gui 542, e2e 5, runtime_smoke 3, contract 461, audit 89, reporting 17, release 24; 758 zero-primary and 0 multi-primary. Qualifiers: semantic=295, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,736 reviewed nodes, 1,724 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 758-node allowlist. Focused successful passes total 1,707. No collection nodes changed.

## Packet M checkpoint — CLI file error and timeout mapping

Fake-Files access denial, timeout exit mapping, and SSH run timeout conversion are unit/semantic. Three actual SSHFilesBackend paths through CLI translate SFTP missing/permission errors for list/stat and are integration/semantic. All six passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 482, integration 303, gui 542, e2e 5, runtime_smoke 3, contract 461, audit 89, reporting 17, release 24; 752 zero-primary and 0 multi-primary. Qualifiers: semantic=301, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,742 reviewed nodes, 1,730 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 752-node allowlist. Focused successful passes total 1,713. No collection nodes changed.

## Packet M checkpoint — remaining CLI filesystem failures

Eight CLI filesystem cases cover SFTP permission/not-found translation for checksum, download, copy, move and delete, an empty remote listing, and generic mkdir shell failure. The CLI and SSHFilesBackend are real; SFTP/SSH responses are controlled seams, so these are integration/semantic. All eight passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 482, integration 311, gui 542, e2e 5, runtime_smoke 3, contract 461, audit 89, reporting 17, release 24; 744 zero-primary and 0 multi-primary. Qualifiers: semantic=309, regression=47, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,750 reviewed nodes, 1,738 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 744-node allowlist. Focused successful passes total 1,721. No collection nodes changed.

## Packet M checkpoint — CLI timeout and localized output contracts

The Paramiko connect/run timeout checks and the CLI non-directory mapping are unit/semantic. Nine file CLI tests cover Turkish JSON/text names and public JSON error-shape parity for not-found, permission, not-a-directory, timeout, and empty-list output. They are contract/semantic; the Turkish-preservation cases and Turkish not-found detail carry regression. All 13 collected nodes passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 486, integration 311, gui 542, e2e 5, runtime_smoke 3, contract 470, audit 89, reporting 17, release 24; 731 zero-primary and 0 multi-primary. Qualifiers: semantic=322, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,763 reviewed nodes, 1,751 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 731-node allowlist. Focused successful passes total 1,734. No collection nodes changed.

## Packet M checkpoint — upload/download collision policies

Twelve CLI transfer-policy cases use real temporary local files and a fake remote Files backend, so the cross-boundary skip, rename, overwrite, resume, verification, and recursive behavior is integration/semantic. The text-mode no-op output is contract/semantic. All 12 passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 486, integration 322, gui 542, e2e 5, runtime_smoke 3, contract 471, audit 89, reporting 17, release 24; 719 zero-primary and 0 multi-primary. Qualifiers: semantic=334, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,775 reviewed nodes, 1,763 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 719-node allowlist. Focused successful passes total 1,746. No collection nodes changed.

## Packet M checkpoint — connection diagnostics stages

Five direct diagnostic-service tests use controlled socket, SSH and checksum seams to assert stage order, fail-closed transitions and wrapper closure; they are integration/semantic. Three doctor output tests assert the public text/JSON stage contract and exit codes; the secret-detail redaction path exercises diagnostics through CLI and is integration/semantic. All nine passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 486, integration 328, gui 542, e2e 5, runtime_smoke 3, contract 474, audit 89, reporting 17, release 24; 710 zero-primary and 0 multi-primary. Qualifiers: semantic=343, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=22, wx=411, qt=233, packaging=2. After 1,784 reviewed nodes, 1,772 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 710-node allowlist. Focused successful passes total 1,755. No collection nodes changed.

## Packet M checkpoint — SFTP smoke stages and CLI artifacts

Nine direct `run_sftp_smoke` scenarios are unit/semantic stage-orchestration checks with a fake backend. Doctor smoke cleanup-flag/session routing is unit/semantic, text/JSON stage-set parity is contract/semantic, artifact write/result cases are integration/semantic/artifact_dependent, and sensitive-detail filtering through the real smoke service and CLI is integration/semantic. All 17 collected nodes passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 498, integration 332, gui 542, e2e 5, runtime_smoke 3, contract 475, audit 89, reporting 17, release 24; 693 zero-primary and 0 multi-primary. Qualifiers: semantic=360, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,801 reviewed nodes, 1,789 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 693-node allowlist. Focused successful passes total 1,772. No collection nodes changed.

## Packet M checkpoint — jobs list, status, and accounting

Twelve scheduler CLI nodes cover public help and the squeue/scontrol/sacct command and JSON/text contracts, plus unit username fallback, missing job ID, connection failure and error-prefix handling. Scheduler stdout is supplied by a fake SSH seam; command construction and interface behavior are the claims. All 12 passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 504, integration 332, gui 542, e2e 5, runtime_smoke 3, contract 481, audit 89, reporting 17, release 24; 681 zero-primary and 0 multi-primary. Qualifiers: semantic=372, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,813 reviewed nodes, 1,801 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 681-node allowlist. Focused successful passes total 1,784. No collection nodes changed.

## Packet M checkpoint — site-status job command

Five `jobs lssrv` paths exercise configured command/JSON output contracts, missing-command and connection failure handling, and nonzero SSH exit translation. The production SSHSlurmBackend is real around a controlled SSH seam for the configured and nonzero cases; these are contract, unit, and integration by behavior. All five passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 506, integration 333, gui 542, e2e 5, runtime_smoke 3, contract 483, audit 89, reporting 17, release 24; 676 zero-primary and 0 multi-primary. Qualifiers: semantic=377, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,818 reviewed nodes, 1,806 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 676-node allowlist. Focused successful passes total 1,789. No collection nodes changed.

## Packet M checkpoint — Slurm job submission and cancellation

Eight job submit/cancel cases cover the explicit `--yes` gate, safe/unsafe IDs, accepted array-task IDs, exact scheduler commands, and JSON result envelopes. Command and result shapes are CLI contracts; refusal and input validation are unit/semantic. All eight passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 509, integration 333, gui 542, e2e 5, runtime_smoke 3, contract 488, audit 89, reporting 17, release 24; 668 zero-primary and 0 multi-primary. Qualifiers: semantic=385, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,826 reviewed nodes, 1,814 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 668-node allowlist. Focused successful passes total 1,797. No collection nodes changed.

## Packet M checkpoint — CLI settings and external-access gate

The settings default/round-trip test writes and reads a real isolated config file, so it is integration/semantic. Five parameterized deny cases, the JSON denial shape, the exemption policy, and the enabled-gate path test CLI access policy with a fake session boundary; they are unit/semantic except the public JSON error contract. All nine collected nodes passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 516, integration 334, gui 542, e2e 5, runtime_smoke 3, contract 489, audit 89, reporting 17, release 24; 659 zero-primary and 0 multi-primary. Qualifiers: semantic=394, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,835 reviewed nodes, 1,823 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 659-node allowlist. Focused successful passes total 1,806. No collection nodes changed.

## Packet M checkpoint — saved profile selection and SSH options

Twelve saved-profile tests exercise profile fallback/override, stale-default errors, keepalive parsing/clamps, and username selection through isolated configuration seams. They test the CLI session/config helper logic as unit/semantic. All 12 passed; Ruff passed.

Latest actual REPORT state: 2,678 collected; unit 528, integration 334, gui 542, e2e 5, runtime_smoke 3, contract 489, audit 89, reporting 17, release 24; 647 zero-primary and 0 multi-primary. Qualifiers: semantic=406, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,847 reviewed nodes, 1,835 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 647-node allowlist. Focused successful passes total 1,818. No collection nodes changed.

## Packet M checkpoint — command inventory and help; CLI file complete

The final four `test_cli.py` nodes assert the JSON command inventory/exit-code map and full help for unknown groups and root examples; they are contract/semantic. The entire `tests/test_cli.py` module then passed: **156 passed**, Ruff passed, and fresh REPORT contains zero zero-primary nodes from that module.

Latest actual REPORT state: 2,678 collected; unit 528, integration 334, gui 542, e2e 5, runtime_smoke 3, contract 493, audit 89, reporting 17, release 24; 643 zero-primary and 0 multi-primary. Qualifiers: semantic=410, regression=52, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,851 reviewed nodes, 1,839 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 643-node allowlist. Focused successful newly reviewed-node passes total 1,822; the separate whole-file rerun passed 156. No collection nodes changed.

## Packet M checkpoint — Unicode baseline and fixtures

All 42 `test_wave0_unicode_baseline.py` nodes now have behavioral primary markers. Repository/source presence and encoding-boundary scans are audit; runtime/dependency and localized content contracts are contract; pure encoding/fixture checks are unit; real temporary filesystem name roundtrips are contract/semantic/regression. **42 passed**, Ruff passed, and REPORT has no zero-primary nodes from this module.

Latest actual REPORT state: 2,678 collected; unit 542, integration 334, gui 542, e2e 5, runtime_smoke 3, contract 508, audit 102, reporting 17, release 24; 601 zero-primary and 0 multi-primary. Qualifiers: semantic=437, regression=71, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=1, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=233, packaging=2. After 1,893 reviewed nodes, 1,881 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 601-node allowlist. Focused successful newly reviewed-node passes total 1,864. Collection remains 2,678.

## Packet M checkpoint — synchronized browsing mappings and widget state

All 33 `test_synchronized_browsing.py` nodes now have markers: path mappings and root containment are unit/semantic; sync config normalization/preservation is contract/semantic; eight real Qt/widget state and navigation workflows plus root reset are gui/qt/semantic, while inactive-panel callback filtering remains unit/qt/semantic. Validation: **32 passed, 1 platform-conditional skip**, Ruff passed. REPORT has no zero-primary nodes from this module.

Latest actual REPORT state: 2,678 collected; unit 563, integration 334, gui 551, e2e 5, runtime_smoke 3, contract 511, audit 102, reporting 17, release 24; 568 zero-primary and 0 multi-primary. Qualifiers: semantic=470, regression=71, performance=10, resource=79, concurrency=93, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=411, qt=243, packaging=2. After 1,926 reviewed nodes, 1,914 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 568-node allowlist. Focused successful newly reviewed-node passes total 1,896. Collection remains 2,678.

## Packet M checkpoint — SSH/SFTP Unicode boundaries and mapping helpers

All 32 Wave3 nodes are classified by the actual behavior; the class name `TestIntegration` contains only MockFilesBackend checks, so those remain unit tests. Counts: 25 unit, 4 contract (RemoteEntry/timeout/dataclass shape), 2 import/runtime smoke, and 1 source audit for shell-quoting policy. Decoder and invalid-UTF-8 tests carry regression; stale-generation handling carries concurrency. **32 passed**, Ruff passed, and the module has no zero-primary nodes.

Latest actual REPORT state: 2,678 collected; unit 588, integration 334, gui 551, e2e 5, runtime_smoke 5, contract 515, audit 103, reporting 17, release 24; 536 zero-primary and 0 multi-primary. Qualifiers: semantic=495, regression=77, performance=10, resource=79, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 1,958 reviewed nodes, 1,946 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 536-node allowlist. Focused successful newly reviewed-node passes total 1,928. Collection remains 2,678.

## Packet M checkpoint — Linux release planning and artifact definitions

All 27 `test_linux_release.py` nodes are release/semantic checks for version consistency, Ubuntu host gating, required release files, AppImage/DEB/Flatpak metadata, build planning and checksums. The focused module passed: **27 passed**, Ruff passed, and REPORT has no unclassified nodes from this file.

Latest actual REPORT state: 2,678 collected; unit 588, integration 334, gui 551, e2e 5, runtime_smoke 5, contract 515, audit 103, reporting 17, release 51; 509 zero-primary and 0 multi-primary. Qualifiers: semantic=522, regression=77, performance=10, resource=79, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 1,985 reviewed nodes, 1,973 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 509-node allowlist. Focused successful newly reviewed-node passes total 1,955. Collection remains 2,678.

## Packet M checkpoint — Slurm script and job metadata Unicode

All 23 Wave5 nodes are classified: 20 unit/parser/directive checks, one composed script workflow that joins multiple production helpers (integration), one `OutputMetadata` public data contract, and one source audit for SSH quoting policy. The method named `test_unicode_scontrol_parsing` remains unit because it exercises only that parser. **23 passed**, Ruff passed, no zero-primary nodes remain in the module.

Latest actual REPORT state: 2,678 collected; unit 608, integration 335, gui 551, e2e 5, runtime_smoke 5, contract 516, audit 104, reporting 17, release 51; 486 zero-primary and 0 multi-primary. Qualifiers: semantic=544, regression=95, performance=10, resource=79, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 2,008 reviewed nodes, 1,996 have exactly one primary and 12 deliberate false/skip gates remain zero-primary. RATCHET passes with the exact 486-node allowlist. Focused successful newly reviewed-node passes total 1,978. Collection remains 2,678.


Latest Packet M checkpoint: The 22 `tests/test_wave7_editor_terminal_logs.py` nodes were reviewed; three `DocumentModel` value cases and `TerminalModel.receive` are `unit`, the WebView JSON serializer is `contract`, and truncation/output-follower helpers are `unit` (all `semantic`). All 22 passed; Ruff passed. Fifteen nodes remain zero-primary because they only exercise `pathlib`/`zipfile`/`codecs` directly, or duplicate the model-only terminal assertion; they do not establish an independent application behavior. Actual REPORT: 2,678 collected; unit 614, integration 335, gui 551, e2e 5, runtime_smoke 5, contract 517, audit 104, reporting 17, release 51; 479 zero-primary and 0 multi-primary. Qualifiers: semantic=551, regression=95, performance=10, resource=79, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 2,030 reviewed nodes, 2,003 have one primary and 27 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 479-node allowlist. Focused successful newly reviewed-node passes total 2,000. Collection remains 2,678.

Unclassified carry-forward nodeids (15; standard-library-only probes or duplicate terminal model flow):
- `tests/test_wave7_editor_terminal_logs.py::TestScriptEditor::test_editor_file_roundtrip_unicode`
- `tests/test_wave7_editor_terminal_logs.py::TestScriptEditor::test_editor_bom_handling`
- `tests/test_wave7_editor_terminal_logs.py::TestScriptEditor::test_editor_no_bom_before_shebang`
- `tests/test_wave7_editor_terminal_logs.py::TestTerminal::test_shell_session_incremental_decoder`
- `tests/test_wave7_editor_terminal_logs.py::TestTerminal::test_shell_session_split_bytes`
- `tests/test_wave7_editor_terminal_logs.py::TestLogs::test_logs_model_unicode_read`
- `tests/test_wave7_editor_terminal_logs.py::TestLogs::test_logs_no_mojibake_patterns`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_unicode_filename_roundtrip`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_extract_unicode`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_nested_unicode`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_path_traversal_protection`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_conflict_handling`
- `tests/test_wave7_editor_terminal_logs.py::TestIntegration::test_full_editor_workflow_unicode`
- `tests/test_wave7_editor_terminal_logs.py::TestIntegration::test_full_terminal_unicode_flow`
- `tests/test_wave7_editor_terminal_logs.py::TestIntegration::test_full_archive_unicode_flow`


Latest Packet M checkpoint: All 20 `tests/test_wave8_i18n_ui_ergonomics.py` nodes are classified by behavior: 11 translation-data/placeholder/resource compatibility checks are `contract`, six `i18n` load-and-lookup checks are `unit`, and three source-text string-policy checks are `audit`. The class named `TestIntegration` is one i18n service workflow and the module drives no GUI object or event. All 20 passed; Ruff passed. Actual REPORT: 2,678 collected; unit 620, integration 335, gui 551, e2e 5, runtime_smoke 5, contract 528, audit 107, reporting 17, release 51; 459 zero-primary and 0 multi-primary. Qualifiers: semantic=568, regression=95, performance=10, resource=79, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 2,050 reviewed nodes, 2,023 have one primary and 27 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 459-node allowlist. Focused successful newly reviewed-node passes total 2,020. Collection remains 2,678.


Latest Packet M checkpoint: All 20 `tests/test_transfer_wave17.py` nodes exercise `transfer_mode` service logic at its fake external backend seam with isolated local files, so they are `unit`/`semantic`; the class name and backend fake do not claim production-backend integration. Two bounded-read tests also carry `performance`. Seven tests that verify the actual `.part`/temporary-file lifecycle carry `resource`, and six boundary/failure cases carry `regression`. All 20 passed; Ruff passed. Actual REPORT: 2,678 collected; unit 640, integration 335, gui 551, e2e 5, runtime_smoke 5, contract 528, audit 107, reporting 17, release 51; 439 zero-primary and 0 multi-primary. Qualifiers: semantic=588, regression=101, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 2,070 reviewed nodes, 2,043 have one primary and 27 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 439-node allowlist. Focused successful newly reviewed-node passes total 2,040. Collection remains 2,678.


Latest Packet M checkpoint: Sixteen of the 19 `tests/test_wave1_unicode_core_policy.py` nodes now have primary markers: 13 translation/config/API compatibility checks are `contract`/`semantic`, while three source-text encoding-policy scans are `audit`. Three `NormalizationPolicy` checks remain zero-primary because they only assert Python `unicodedata`/`pathlib` behavior and do not call application logic. All 19 passed; Ruff passed. Actual REPORT: 2,678 collected; unit 640, integration 335, gui 551, e2e 5, runtime_smoke 5, contract 541, audit 110, reporting 17, release 51; 423 zero-primary and 0 multi-primary. Qualifiers: semantic=601, regression=104, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 2,089 reviewed nodes, 2,059 have one primary and 30 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 423-node allowlist. Focused successful newly reviewed-node passes total 2,059. Collection remains 2,678.

Unclassified carry-forward nodeids (3; standard-library-only behavior checks):
- `tests/test_wave1_unicode_core_policy.py::TestNormalizationPolicy::test_nfc_nfd_distinct`
- `tests/test_wave1_unicode_core_policy.py::TestNormalizationPolicy::test_turkish_i_casing`
- `tests/test_wave1_unicode_core_policy.py::TestNormalizationPolicy::test_pathlib_preserves_unicode`


Latest Packet M checkpoint: Fifteen of 17 `tests/test_wave6_plugin_provider_unicode.py` nodes are classified: plugin model/path-resolution cases are `unit`, manifest/profile/provider-shape cases are `contract`, and the full manifest-validation-profile-settings path is `integration`. All marked behavior carries `semantic`; the unknown-placeholder and ASCII-compatibility boundaries also carry `regression`. The two UI-contribution nodes remain zero-primary: one passes `menu` where the production validator requires `plugins_menu`, the other passes top-level `items`, and both filter only for errors mentioning `unicode`. They can pass while the supplied shape is rejected, so these are false gates queued for truthful rewrite. All 17 passed; Ruff passed. Actual REPORT: 2,678 collected; unit 649, integration 336, gui 551, e2e 5, runtime_smoke 5, contract 546, audit 110, reporting 17, release 51; 408 zero-primary and 0 multi-primary. Qualifiers: semantic=616, regression=106, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=243, packaging=2. After 2,106 reviewed nodes, 2,074 have one primary and 32 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 408-node allowlist. Focused successful newly reviewed-node passes total 2,076. Collection remains 2,678.

Unclassified carry-forward nodeids (2; rejected-shape Unicode UI-contribution probes):
- `tests/test_wave6_plugin_provider_unicode.py::TestPluginUIContributionsUnicode::test_ui_contributions_label_unicode`
- `tests/test_wave6_plugin_provider_unicode.py::TestIntegration::test_ui_contributions_unicode_labels`


Latest Packet M checkpoint: All 13 `tests/test_lssrv_auto_refresh.py` nodes are classified: seven real Qt timer/tab/text-state checks are `gui`/`qt`; four widget-to-backend refresh and busy-gate checks plus two isolated config-storage checks are `integration` (the widget cases also carry `qt`). All carry `semantic`. The busy-query case is not labeled `concurrency`: it does not block the fake backend to establish overlap/ownership under contention. All 13 passed; Ruff passed. Actual REPORT: 2,678 collected; unit 649, integration 342, gui 558, e2e 5, runtime_smoke 5, contract 546, audit 110, reporting 17, release 51; 395 zero-primary and 0 multi-primary. Qualifiers: semantic=629, regression=106, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=254, packaging=2. After 2,132 reviewed nodes, 2,100 have one primary and 32 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 395-node allowlist. Focused successful newly reviewed-node passes total 2,102. Collection remains 2,678.


Latest Packet M checkpoint: All 13 `tests/test_release_gate.py` nodes are `release`/`semantic`: they exercise signed/unsigned gate policy, required job outcomes, security metadata, CLI metadata emission, and inclusion of security metadata in generated manifests. All 13 passed; Ruff passed. Actual REPORT: 2,678 collected; unit 649, integration 342, gui 558, e2e 5, runtime_smoke 5, contract 546, audit 110, reporting 17, release 64; 382 zero-primary and 0 multi-primary. Qualifiers: semantic=642, regression=106, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=254, packaging=2. After 2,145 reviewed nodes, 2,113 have one primary and 32 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 382-node allowlist. Focused successful newly reviewed-node passes total 2,115. Collection remains 2,678.


Latest Packet M checkpoint: All 12 `tests/test_menu_redesign.py` nodes are classified: 11 source-level menu ownership, plugin wiring, About-dialog architecture, and hardcoded-ID checks are `audit`; the translation-key inventory is `contract`/`semantic`. The file creates no Qt menus or actions, so its static source assertions do not claim visible GUI behavior. All 12 passed; Ruff passed. Actual REPORT: 2,678 collected; unit 649, integration 342, gui 558, e2e 5, runtime_smoke 5, contract 547, audit 121, reporting 17, release 64; 370 zero-primary and 0 multi-primary. Qualifiers: semantic=643, regression=106, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=254, packaging=2. After 2,157 reviewed nodes, 2,125 have one primary and 32 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 370-node allowlist. Focused successful newly reviewed-node passes total 2,127. Collection remains 2,678.


Latest Packet M checkpoint: Eleven of 12 `tests/test_security_hardening_wave.py` nodes are classified: six isolated hardening helpers are `unit`, four filesystem/backend trust-boundary flows are `integration`, and unknown declarative-engine rejection is `contract`; each is `semantic`/`regression`. The remaining `test_plugin_runtime_has_no_dynamic_source_execution` only checks that the approved `create_plugin(` string is present and does not assert absence of dynamic execution, so it remains zero-primary. Validation: 10 passed, 2 expected platform skips (symlink creation unavailable; POSIX-only permission semantics), Ruff passed. Actual REPORT: 2,678 collected; unit 655, integration 346, gui 558, e2e 5, runtime_smoke 5, contract 548, audit 121, reporting 17, release 64; 359 zero-primary and 0 multi-primary. Qualifiers: semantic=654, regression=117, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=412, qt=254, packaging=2. After 2,169 reviewed nodes, 2,136 have one primary and 33 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 359-node allowlist. Focused successful newly reviewed-node passes total 2,137. Collection remains 2,678.

Unclassified carry-forward nodeid (1; source scan does not prove the stated security policy):
- `tests/test_security_hardening_wave.py::test_plugin_runtime_has_no_dynamic_source_execution`


Latest Packet M checkpoint: Ten of 12 `tests/test_hardening_additional.py` nodes are classified: plugin-template filtering and valid/invalid plugin isolation are `integration`; explicit menu-action dispatch is `unit`; strict contribution schema checks are `contract`; two real offscreen Qt menu states are `gui`/`qt`; four source-level Qt/wx adapter and menu-policy checks are `audit`. The remaining localized-sort test applies its own copied sort expression rather than the production owner. The wx separator lifecycle node unconditionally skips before executing its body with a “flaky subprocess heap” reason, which violates the documented skip policy and provides no runtime evidence. Validation: 11 passed, 1 unconditional skip, Ruff passed. Actual REPORT: 2,678 collected; unit 656, integration 348, gui 560, e2e 5, runtime_smoke 5, contract 549, audit 125, reporting 17, release 64; 349 zero-primary and 0 multi-primary. Qualifiers: semantic=660, regression=119, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=416, qt=258, packaging=2. After 2,181 reviewed nodes, 2,146 have one primary and 35 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 349-node allowlist. Focused successful newly reviewed-node passes total 2,148. Collection remains 2,678.

Unclassified carry-forward nodeids (2; false sort owner and unconditional skip):
- `tests/test_hardening_additional.py::test_localized_sort`
- `tests/test_hardening_additional.py::test_wx_separator_lifecycle_offscreen`


Latest Packet M checkpoint: All 12 `tests/test_macos_release.py` nodes are classified: ten architecture-specific release planning, host gates, DMG budget, packaging specification, and lock-policy tests are `release`; bundle-size aggregation is `reporting`; the source-text symlink-copy policy is `audit`/`packaging`. All 12 passed; Ruff passed. The known-failure module `test_macos_release_workflow.py` was not touched. Actual REPORT: 2,678 collected; unit 656, integration 348, gui 560, e2e 5, runtime_smoke 5, contract 549, audit 126, reporting 18, release 74; 337 zero-primary and 0 multi-primary. Qualifiers: semantic=671, regression=119, performance=12, resource=86, concurrency=94, slow=4, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=416, qt=258, packaging=4. After 2,193 reviewed nodes, 2,158 have one primary and 35 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 337-node allowlist. Focused successful newly reviewed-node passes total 2,160. Collection remains 2,678.


Latest Packet M checkpoint: All 11 `tests/test_wx_file003_final_stress.py` nodes are marked: eight real wx interaction/state scenarios are `gui`/`wx`, file-transfer orchestration and reconnect session-snapshot checks are `integration`/`wx`, and the measured-invariant scoreboard is `reporting`/`wx`. All 11 carry `semantic`; ten behavior cases are `regression`; seven measured groups over 10 seconds also carry `slow`; proven overlap, blocked completion, or session-ownership cases carry `concurrency`; frame/session leak checks carry `resource`. Full module validation: 11 passed in 201.27s; Ruff passed. It exercised 1,045 operations across the stress scenarios and measured zero wrong-target, stale-UI, destroyed-control, worker/window/session leak, duplicate/lost transfer, mixed-session, or lost remote mutation invariants; local and remote mutation peaks were each 1. Actual REPORT: 2,678 collected; unit 656, integration 350, gui 568, e2e 5, runtime_smoke 5, contract 549, audit 126, reporting 19, release 74; 326 zero-primary and 0 multi-primary. Qualifiers: semantic=682, regression=129, performance=12, resource=89, concurrency=101, slow=11, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=427, qt=258, packaging=4. After 2,204 reviewed nodes, 2,169 have one primary and 35 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 326-node allowlist. Focused successful newly reviewed-node passes total 2,171. Collection remains 2,678.


Latest Packet M checkpoint: Ten of 11 `tests/test_wx_jobs_stress.py` nodes are classified: nine real wx interaction/state tests are `gui`/`wx`, and the stress model update is `unit`/`wx`; all ten carry `semantic` and `regression`. The repeated open/close timer/window lifecycle test also carries `resource` and `slow`; measured duration was 49.74 seconds. The remaining `test_wx_jobs_stress_missing_output_recovery_pressure` only exercises `MockHPCJobs.read_output` and its own mock error behavior, so it remains zero-primary for a later truthful-owner review. Validation: 10 passed, 1 failed in the module; Ruff passed. `test_wx_jobs_stress_pause_resume_state_never_desynchronizes` failed at its first transition expecting a manually refreshed `pause-0` output after pausing. The same isolated node failed on the exact frozen SHA worktree with the same assertion (1 failed in 6.83s); current and frozen test bodies are AST-identical after ignoring taxonomy decorators, and production sources are unchanged from baseline. This is an additional reproduced baseline-signature failure, separate from the seven original archived failures; no behavior was rewritten. Actual REPORT: 2,678 collected; unit 657, integration 350, gui 577, e2e 5, runtime_smoke 5, contract 549, audit 126, reporting 19, release 74; 316 zero-primary and 0 multi-primary. Qualifiers: semantic=692, regression=139, performance=13, resource=92, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=437, qt=258, packaging=4. After 2,215 reviewed nodes, 2,179 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 316-node allowlist. Focused successful newly reviewed-node passes total 2,181. Collection remains 2,678.

Unclassified carry-forward nodeid (1; mock verifies only its own behavior):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`


Latest Packet M checkpoint: All six nodes in `tests/test_quota_monitor.py` exercise the quota gate/profile-state service with plain configuration records, so they are `unit`/`semantic`. This does not claim provider or GUI integration. All six passed; Ruff passed. Actual REPORT: 2,678 collected; unit 663, integration 350, gui 577, e2e 5, runtime_smoke 5, contract 549, audit 126, reporting 19, release 74; 310 zero-primary and 0 multi-primary. Qualifiers: semantic=698, regression=139, performance=13, resource=92, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=437, qt=258, packaging=4. After 2,221 reviewed nodes, 2,185 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 310-node allowlist. Focused successful newly reviewed-node passes total 2,187. Collection remains 2,678.


Latest Packet M checkpoint: All six nodes in `tests/test_linux_x11.py` drive isolated `X11Runner.preflight` and `ensure_x_server_running` branches with patched SSH/server probes and controlled `DISPLAY` state. They are `unit`/`semantic`; `linux` is not assigned because the host platform is simulated through a patched branch. All six passed; Ruff passed. Actual REPORT: 2,678 collected; unit 669, integration 350, gui 577, e2e 5, runtime_smoke 5, contract 549, audit 126, reporting 19, release 74; 304 zero-primary and 0 multi-primary. Qualifiers: semantic=704, regression=139, performance=13, resource=92, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=437, qt=258, packaging=4. After 2,227 reviewed nodes, 2,191 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 304-node allowlist. Focused successful newly reviewed-node passes total 2,193. Collection remains 2,678.


Latest Packet M checkpoint: All five nodes in `tests/test_wx_file_context_i18n.py` drive real wx list/notebook/dialog objects and context-menu events, then inspect generated labels or visible control state after runtime language changes. They are `gui`/`wx`/`semantic`; no duplicate cleanup was performed. All five passed in 2.60s; Ruff passed. wxWidgets printed an `UnregisterClass` warning about open windows during teardown despite pytest exit 0; preserve it for Packet J resource review. Actual REPORT: 2,678 collected; unit 669, integration 350, gui 582, e2e 5, runtime_smoke 5, contract 549, audit 126, reporting 19, release 74; 299 zero-primary and 0 multi-primary. Qualifiers: semantic=709, regression=139, performance=13, resource=92, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=258, packaging=4. After 2,232 reviewed nodes, 2,196 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 299-node allowlist. Focused successful newly reviewed-node passes total 2,198. Collection remains 2,678.


Latest Packet M checkpoint: Five of six nodes in `tests/test_wx_help.py` exercise `WxHelpModel` search, navigation, shortcut binding, and language-refresh behavior, so they are `unit`/`semantic`. The remaining test checks the model's source-level toolkit-import boundary and is `audit`; despite the filename, no wx GUI object is created. All six passed; Ruff passed. Actual REPORT: 2,678 collected; unit 674, integration 350, gui 582, e2e 5, runtime_smoke 5, contract 549, audit 127, reporting 19, release 74; 293 zero-primary and 0 multi-primary. Qualifiers: semantic=714, regression=139, performance=13, resource=92, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=258, packaging=4. After 2,238 reviewed nodes, 2,202 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 293-node allowlist. Focused successful newly reviewed-node passes total 2,204. Collection remains 2,678.


Latest Packet M checkpoint: `tests/test_startup_changelog.py` has three classified nodes: the small-screen startup test exercises a real `MainWindow` and observable Qt geometry, so it is `gui`/`qt`/`semantic`/`resource`; the changelog ordering function and once-per-version startup decision are isolated logic, so they are `unit`/`semantic`. All three passed; Ruff passed. Actual REPORT: 2,678 collected; unit 676, integration 350, gui 583, e2e 5, runtime_smoke 5, contract 549, audit 127, reporting 19, release 74; 290 zero-primary and 0 multi-primary. Qualifiers: semantic=717, regression=139, performance=13, resource=93, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=259, packaging=4. After 2,241 reviewed nodes, 2,205 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 290-node allowlist. Focused successful newly reviewed-node passes total 2,207. Collection remains 2,678.


Latest Packet M checkpoint: All three nodes in `tests/test_ui_errors.py` are classified by the behavior they assert: the exact translated diagnostic label is `contract`/`semantic`, while exception-to-user-message mapping and technical-detail preservation are `unit`/`semantic`. Validation: 3 passed with 5 subtests; Ruff passed. Actual REPORT: 2,678 collected; unit 678, integration 350, gui 583, e2e 5, runtime_smoke 5, contract 550, audit 127, reporting 19, release 74; 287 zero-primary and 0 multi-primary. Qualifiers: semantic=720, regression=139, performance=13, resource=93, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=259, packaging=4. After 2,244 reviewed nodes, 2,208 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 287-node allowlist. Focused successful newly reviewed-node passes total 2,210. Collection remains 2,678.


Latest Packet M checkpoint: All three `tests/test_platform_keymap.py` nodes check the user-facing keyboard-binding interface across platform aliases, terminal interrupt boundaries, conflict rules, and macOS display glyphs. They are `contract`/`semantic`, not GUI tests. All three passed; Ruff passed. Actual REPORT: 2,678 collected; unit 678, integration 350, gui 583, e2e 5, runtime_smoke 5, contract 553, audit 127, reporting 19, release 74; 284 zero-primary and 0 multi-primary. Qualifiers: semantic=723, regression=139, performance=13, resource=93, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=259, packaging=4. After 2,247 reviewed nodes, 2,211 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 284-node allowlist. Focused successful newly reviewed-node passes total 2,213. Collection remains 2,678.


Latest Packet M checkpoint: All three nodes in `tests/test_walltime_suggestions.py` exercise isolated `suggest_walltime` filtering, resource-match, outlier, and deterministic-format behavior, so they are `unit`/`semantic`. The mixed-record case confirms that eligible completed history can still yield a suggestion when a failed record is present. All three passed; Ruff passed. Actual REPORT: 2,678 collected; unit 681, integration 350, gui 583, e2e 5, runtime_smoke 5, contract 553, audit 127, reporting 19, release 74; 281 zero-primary and 0 multi-primary. Qualifiers: semantic=726, regression=139, performance=13, resource=93, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=259, packaging=4. After 2,250 reviewed nodes, 2,214 are classified and 36 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 281-node allowlist. Focused successful newly reviewed-node passes total 2,216. Collection remains 2,678.


Latest Packet M checkpoint: Two of three nodes in `tests/test_transfer_directory_controllers.py` call isolated transfer/navigation service controllers and assert state transitions, so they are `unit`/`semantic`. The source-import assertion remains zero-primary: it checks for a `PySide` key in the imported module dictionary, which would not detect `from PySide6 import ...`; current controller source is framework-neutral, but the test does not establish the claimed architecture contract. All three passed; Ruff passed. Actual REPORT: 2,678 collected; unit 683, integration 350, gui 583, e2e 5, runtime_smoke 5, contract 553, audit 127, reporting 19, release 74; 279 zero-primary and 0 multi-primary. Qualifiers: semantic=728, regression=139, performance=13, resource=93, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=259, packaging=4. After 2,253 reviewed nodes, 2,216 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 279-node allowlist. Focused successful newly reviewed-node passes total 2,219. Collection remains 2,678.

Unclassified carry-forward nodeid (1; module-dictionary check does not establish import absence):
- `tests/test_transfer_directory_controllers.py::test_controllers_have_no_qt_imports`


Latest Packet M checkpoint: All four nodes in `tests/test_macos_x11.py` test isolated XQuartz preflight, SSH selection, launch construction, and password-only rejection branches using patched OS/external probes. They are `unit`/`semantic`; no `macos` qualifier is used because the host platform is simulated. All four passed; Ruff passed. Actual REPORT: 2,678 collected; unit 687, integration 350, gui 583, e2e 5, runtime_smoke 5, contract 553, audit 127, reporting 19, release 74; 275 zero-primary and 0 multi-primary. Qualifiers: semantic=732, regression=139, performance=13, resource=93, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=259, packaging=4. After 2,257 reviewed nodes, 2,220 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 275-node allowlist. Focused successful newly reviewed-node passes total 2,223. Collection remains 2,678.


Latest Packet M checkpoint: All five `tests/test_wx_semantics.py` nodes are source-text checks of wx shell menu composition, version presentation wiring, shared contribution ownership, and a disabled feature gate. They are `audit` only; the assertions do not instantiate a wx window or establish runtime GUI behavior. All five passed; Ruff passed. Actual REPORT: 2,678 collected; unit 687, integration 350, gui 583, e2e 5, runtime_smoke 5, contract 553, audit 132, reporting 19, release 74; 270 zero-primary and 0 multi-primary. Qualifiers: semantic=732, regression=139, performance=13, resource=93, concurrency=105, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=442, qt=259, packaging=4. After 2,262 reviewed nodes, 2,225 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 270-node allowlist. Focused successful newly reviewed-node passes total 2,228. Collection remains 2,678.


Latest Packet M checkpoint: All five `tests/test_wx_shell.py` nodes are classified: three source-text shell structure checks are `audit`; the default directory-download handoff and remote-operation session-snapshot flows are `integration`/`wx`/`semantic`; the latter carries `concurrency` because it blocks a backend operation while swapping sessions and verifies ownership. No source check is treated as GUI runtime proof. All five passed; Ruff passed. Actual REPORT: 2,678 collected; unit 687, integration 352, gui 583, e2e 5, runtime_smoke 5, contract 553, audit 135, reporting 19, release 74; 265 zero-primary and 0 multi-primary. Qualifiers: semantic=734, regression=139, performance=13, resource=93, concurrency=106, slow=12, subprocess=42, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=444, qt=259, packaging=4. After 2,267 reviewed nodes, 2,230 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 265-node allowlist. Focused successful newly reviewed-node passes total 2,233. Collection remains 2,678.


Latest Packet M checkpoint: All four nodes in `tests/test_wheel_packaging.py` are classified by behavior: two runtime dependency metadata checks are `contract`/`semantic`; the fresh-interpreter import check is `runtime_smoke`/`subprocess`; and the actual wheel asset/metadata inspection is `release`/`packaging`. The wheel build took 30.79s, so it also carries `slow`. All four passed in 31.73s; Ruff passed. Actual REPORT: 2,678 collected; unit 687, integration 352, gui 583, e2e 5, runtime_smoke 6, contract 555, audit 135, reporting 19, release 75; 261 zero-primary and 0 multi-primary. Qualifiers: semantic=736, regression=139, performance=13, resource=93, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=444, qt=259, packaging=4. After 2,271 reviewed nodes, 2,234 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 261-node allowlist. Focused successful newly reviewed-node passes total 2,237. Collection remains 2,678.


Latest Packet M checkpoint: All four `tests/test_local_transfer_gate.py` nodes exercise the local, offline release-gate path; its round trip uses a temporary filesystem stand-in, and its SFTP smoke matrix uses an in-memory backend with no real network. The transfer gate and artifact preconditions are `release`/`semantic`; the schema/payload artifact node is `reporting`/`semantic`; refusal to overwrite a pre-existing artifact also carries `resource`. All four passed; Ruff passed. Actual REPORT: 2,678 collected; unit 687, integration 352, gui 583, e2e 5, runtime_smoke 6, contract 555, audit 135, reporting 20, release 78; 257 zero-primary and 0 multi-primary. Qualifiers: semantic=740, regression=139, performance=13, resource=94, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=444, qt=259, packaging=4. After 2,275 reviewed nodes, 2,238 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 257-node allowlist. Focused successful newly reviewed-node passes total 2,241. Collection remains 2,678.


Latest Packet M checkpoint: All four nodes in `tests/test_wx_remote_files.py` are classified: two isolated `WxRemoteDirectoryModel` cache/navigation/operation-state checks are `unit`/`wx`/`semantic`; the toolkit-boundary and worker/UI-thread source architecture checks are `audit`/`wx`. The static checks do not claim runtime GUI event proof. All four passed; Ruff passed. Actual REPORT: 2,678 collected; unit 689, integration 352, gui 583, e2e 5, runtime_smoke 6, contract 555, audit 137, reporting 20, release 78; 253 zero-primary and 0 multi-primary. Qualifiers: semantic=742, regression=139, performance=13, resource=94, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=448, qt=259, packaging=4. After 2,279 reviewed nodes, 2,242 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 253-node allowlist. Focused successful newly reviewed-node passes total 2,245. Collection remains 2,678.


Latest Packet M checkpoint: All four nodes in `tests/test_master_password_dialog.py` operate real Qt `LoginWidget` and `QDialog` objects, inspect localized form state, and dispatch accept/reject actions. They are `gui`/`qt`/`semantic`. All four passed; Ruff passed. Actual REPORT: 2,678 collected; unit 689, integration 352, gui 587, e2e 5, runtime_smoke 6, contract 555, audit 137, reporting 20, release 78; 249 zero-primary and 0 multi-primary. Qualifiers: semantic=746, regression=139, performance=13, resource=94, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=448, qt=263, packaging=4. After 2,283 reviewed nodes, 2,246 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 249-node allowlist. Focused successful newly reviewed-node passes total 2,249. Collection remains 2,678.


Latest Packet M checkpoint: All four `tests/test_ssh_directory_listing_wire.py` nodes run the production Paramiko client and `SSHFilesBackend` over a disposable loopback mock SSH/SFTP server, so they are `integration`/`semantic`. Channel reuse and abandoned-generator recovery also carry `resource`; no real remote host is contacted. All four passed in 4.46s (individual calls 1.00–1.06s); Ruff passed. Actual REPORT: 2,678 collected; unit 689, integration 356, gui 587, e2e 5, runtime_smoke 6, contract 555, audit 137, reporting 20, release 78; 245 zero-primary and 0 multi-primary. Qualifiers: semantic=750, regression=139, performance=13, resource=96, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=448, qt=263, packaging=4. After 2,287 reviewed nodes, 2,250 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 245-node allowlist. Focused successful newly reviewed-node passes total 2,253. Collection remains 2,678.


Latest Packet M checkpoint: All four `tests/test_linux_release_smoke.py` nodes test the Linux release-smoke gate's decision and process-control logic, using mocked binary checks and subprocess outcomes. They are `release`/`semantic`; these tests do not execute a real application binary and do not claim `runtime_smoke`. All four passed; Ruff passed. Actual REPORT: 2,678 collected; unit 689, integration 356, gui 587, e2e 5, runtime_smoke 6, contract 555, audit 137, reporting 20, release 82; 241 zero-primary and 0 multi-primary. Qualifiers: semantic=754, regression=139, performance=13, resource=96, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=448, qt=263, packaging=4. After 2,291 reviewed nodes, 2,254 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 241-node allowlist. Focused successful newly reviewed-node passes total 2,257. Collection remains 2,678.


Latest Packet M checkpoint: All four nodes in `tests/test_linux_update_handoff.py` exercise isolated AppImage path/staging/replacement and Flatpak handoff logic with temporary files and an injected runner. They are `unit`/`semantic`; actual replacement and recovery-copy behavior also carries `resource`. Three passed; the symlink-source rejection test took its existing environment guard because this Windows host cannot create the symlink; Ruff passed. Actual REPORT: 2,678 collected; unit 693, integration 356, gui 587, e2e 5, runtime_smoke 6, contract 555, audit 137, reporting 20, release 82; 237 zero-primary and 0 multi-primary. Qualifiers: semantic=758, regression=139, performance=13, resource=97, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=448, qt=263, packaging=4. After 2,295 reviewed nodes, 2,258 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 237-node allowlist. Focused successful newly reviewed-node passes total 2,260. Collection remains 2,678.


Latest Packet M checkpoint: All six `tests/test_wx_remote_move_undo.py` nodes exercise real wx frame/list controls and Ctrl+Z key events with observable remote-model/backend effects, so they are `gui`/`wx`/`semantic`. They cover successful and failed moves, copy exclusion, history absence, multi-file restoration, and failed undo history. No `concurrency` qualifier was added because the tests wait for each operation before asserting. All six passed in 2.22s; Ruff passed. wxWidgets emitted the open-window `UnregisterClass` teardown warning despite pytest exit 0; carry it into Packet J resource review. Actual REPORT: 2,678 collected; unit 693, integration 356, gui 593, e2e 5, runtime_smoke 6, contract 555, audit 137, reporting 20, release 82; 231 zero-primary and 0 multi-primary. Qualifiers: semantic=764, regression=139, performance=13, resource=97, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=454, qt=263, packaging=4. After 2,301 reviewed nodes, 2,264 are classified and 37 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 231-node allowlist. Focused successful newly reviewed-node passes total 2,266. Collection remains 2,678.


Latest Packet M checkpoint: Six of seven nodes in `tests/test_wx_local_files.py` are classified: four local-browser/action-policy behaviors are `unit`/`wx`/`semantic`, the file-URL escaping behavior is `contract`/`wx`/`semantic`, and the view-wiring source scan is `audit`/`wx`. The remaining toolkit-import source check only excludes literal `from PySide6` and would miss `import wx`, so it remains zero-primary. All seven passed; Ruff passed. Actual REPORT: 2,678 collected; unit 697, integration 356, gui 593, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 20, release 82; 225 zero-primary and 0 multi-primary. Qualifiers: semantic=769, regression=139, performance=13, resource=98, concurrency=106, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=460, qt=263, packaging=4. After 2,308 reviewed nodes, 2,270 are classified and 38 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 225-node allowlist. Focused successful newly reviewed-node passes total 2,273. Collection remains 2,678.

Unclassified carry-forward nodeid (1; toolkit check misses a supported import form):
- `tests/test_wx_local_files.py::test_local_browser_model_has_no_toolkit_import`


Latest Packet M checkpoint: All seven `tests/test_wx_remote_editor_flow.py` nodes operate real wx remote-list and editor frames, dispatch list activation, and verify visible editor content/window ownership; they are `gui`/`wx`/`semantic`. Close-during-read and stale-read-vs-newer-edit cases also carry `concurrency` because they block reads and assert stale-result discard/ownership. All seven passed in 3.38s; individual calls were under 0.6s, so none received `slow`; Ruff passed. Actual REPORT: 2,678 collected; unit 697, integration 356, gui 600, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 20, release 82; 218 zero-primary and 0 multi-primary. Qualifiers: semantic=776, regression=139, performance=13, resource=98, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=467, qt=263, packaging=4. After 2,315 reviewed nodes, 2,277 are classified and 38 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 218-node allowlist. Focused successful newly reviewed-node passes total 2,280. Collection remains 2,678.


Latest Packet M checkpoint: Seven of eight nodes in `tests/test_wx_files_sync_compare.py` are `gui`/`wx`/`semantic`: real shell controls and checkbox/button events lead to observable sync/comparison results. The stale-completion node does not get `concurrency` because it lacks a deterministic worker-start barrier. The in-flight-close node remains zero-primary because its only close-state assertion is tautological (`... is True or True`). Validation: all eight passed in 20.25s, with every individual call under 2.7s; Ruff passed. Actual REPORT: 2,678 collected; unit 697, integration 356, gui 607, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 20, release 82; 211 zero-primary and 0 multi-primary. Qualifiers: semantic=783, regression=139, performance=13, resource=98, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=474, qt=263, packaging=4. After 2,323 reviewed nodes, 2,284 are classified and 39 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 211-node allowlist. Focused successful newly reviewed-node passes total 2,288. Collection remains 2,678.

Unclassified carry-forward nodeid (1; close-in-flight assertion is tautological):
- `tests/test_wx_files_sync_compare.py::test_wx_compare_close_in_flight_is_safe`


Latest Packet M checkpoint: All four nodes in `tests/test_wx_migration.py` are classified from their actual behavior; the filename is historical and no wx runtime is involved. Three config migration/recovery cases are `unit`/`semantic`/`resource`, because they create real isolated config/back-up files and assert preservation/idempotence. The diagnostic-bundle secret exclusion case is `reporting`/`semantic`. All four passed; Ruff passed. Actual REPORT: 2,678 collected; unit 700, integration 356, gui 607, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 207 zero-primary and 0 multi-primary. Qualifiers: semantic=787, regression=139, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=474, qt=263, packaging=4. After 2,327 reviewed nodes, 2,288 are classified and 39 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 207-node allowlist. Focused successful newly reviewed-node passes total 2,292. Collection remains 2,678.


Latest Packet M checkpoint: All six `tests/test_wx_connection_71_2.py` nodes use real wx connection panels/dialogs and button events, observe profile selection/connection/master-password behavior, and use isolated storage plus mocked backend seams. They are `gui`/`wx`/`semantic`; the duplicate typed-password case remains intact for the later D7 ownership packet. All six passed in 3.69s; Ruff passed. Actual REPORT: 2,678 collected; unit 700, integration 356, gui 613, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 201 zero-primary and 0 multi-primary. Qualifiers: semantic=793, regression=139, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=480, qt=263, packaging=4. After 2,333 reviewed nodes, 2,294 are classified and 39 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 201-node allowlist. Focused successful newly reviewed-node passes total 2,298. Collection remains 2,678.


Latest Packet M checkpoint: All seven nodes in `tests/test_wx_connection_71_3.py` use real wx connection panels/dialogs and connect or Test Cluster button events to observe profile, credential-prompt, cancellation, and controller state. They are `gui`/`wx`/`semantic`; remote HPC/cluster backends remain mocked. All seven passed in 6.98s; Ruff passed. Actual REPORT: 2,678 collected; unit 700, integration 356, gui 620, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 194 zero-primary and 0 multi-primary. Qualifiers: semantic=800, regression=139, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=487, qt=263, packaging=4. After 2,340 reviewed nodes, 2,301 are classified and 39 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 194-node allowlist. Focused successful newly reviewed-node passes total 2,305. Collection remains 2,678.


Latest Packet M checkpoint: Six of eight nodes in `tests/test_wx_ansys_view.py` create real wx ANSYS frames and observe rendered results, event-driven file/folder actions, detail/copy/docs controls, or list limits; they are `gui`/`wx`/`semantic`. The close-in-flight node remains zero-primary because it ends with `assert True` and has no worker-start/result barrier. The i18n-refresh node remains zero-primary because `... or True` makes its language-change assertion vacuous. Validation: all eight passed in 3.56s; Ruff passed. Actual REPORT: 2,678 collected; unit 700, integration 356, gui 626, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 188 zero-primary and 0 multi-primary. Qualifiers: semantic=806, regression=139, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=493, qt=263, packaging=4. After 2,348 reviewed nodes, 2,307 are classified and 41 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 188-node allowlist. Focused successful newly reviewed-node passes total 2,313. Collection remains 2,678.

Unclassified carry-forward nodeids (2; close-in-flight no-op assertion and tautological i18n assertion):
- `tests/test_wx_ansys_view.py::test_wx_ansys_close_in_flight_safe`
- `tests/test_wx_ansys_view.py::test_wx_ansys_i18n_refresh`

Latest Packet M checkpoint: All nine nodes in `tests/test_wx_embedded_terminal.py` were reviewed by behavior. Six wx control/event cases are `gui`/`wx`/`semantic`; the two tests that call keyboard model methods directly are `unit`/`wx`/`semantic`. The runtime-language case remains zero-primary because it only asserts labels are nonempty before and after, without proving a language change. Validation: 2 passed and 7 failed in the focused run; the exact same seven failure signatures were reproduced on the clean frozen-baseline worktree at `12ce79935bf076e1062c57dc7dbd148bad2bfae1` (six `_wx_terminal_model is None` errors and one missing PTY resize callback, with the same WebView2 abort diagnostic). These are confirmed baseline failures and test bodies were left untouched. Ruff passed. Actual REPORT: 2,678 collected; unit 702, integration 356, gui 632, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 180 zero-primary and 0 multi-primary. Qualifiers: semantic=814, regression=139, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=501, qt=263, packaging=4. After 2,357 reviewed nodes, 2,315 are classified and 42 reviewed false/skip gates remain zero-primary. RATCHET records the exact 180-node allowlist. Focused successful newly reviewed-node passes total 2,315. Collection remains 2,678.

Unclassified carry-forward nodeid (1; labels stay nonempty without evidence that locale refresh occurred):
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Seven previously zero-primary nodes in `tests/test_connection_profile_service.py` exercise isolated connection-profile save, rename, unknown-field/secret preservation, or keychain decryption behavior; they are `unit`/`regression`/`semantic`. The already-classified typed-password precedence test was left unchanged. `test_mfa_transient_not_stored` remains zero-primary because it never supplies an MFA response to prove transient handling. All nine module tests passed; Ruff passed. Actual REPORT: 2,678 collected; unit 709, integration 356, gui 632, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 173 zero-primary and 0 multi-primary. Qualifiers: semantic=821, regression=146, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=501, qt=263, packaging=4. After 2,365 reviewed nodes, 2,322 are classified and 43 reviewed false/skip gates remain zero-primary. RATCHET records the exact 173-node allowlist. Focused successful newly reviewed-node passes total 2,323. Collection remains 2,678.

Unclassified carry-forward nodeids (2; weak MFA test input and non-observable terminal language refresh):
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All nine previously zero-primary tests in `tests/test_wx_connection_71_5.py` drive real wx connection-panel/dialog controls or events and assert connection failure recovery, selection/worker outcomes, or protected master-password persistence. They are `gui`/`wx`/`semantic`; remote backends and prompt widgets remain mocked seams. No `concurrency` qualifier was added because the tests do not assert ordering or ownership invariants. All nine passed in 3.82s (individual calls below 1.1s); Ruff passed. Actual REPORT: 2,678 collected; unit 709, integration 356, gui 641, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 164 zero-primary and 0 multi-primary. Qualifiers: semantic=830, regression=146, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=510, qt=263, packaging=4. After 2,374 reviewed nodes, 2,331 are classified and 43 reviewed false/skip gates remain zero-primary. RATCHET records the exact 164-node allowlist. Focused successful newly reviewed-node passes total 2,332. Collection remains 2,678.

Unclassified carry-forward nodeids (2; weak MFA test input and non-observable terminal language refresh):
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All eight `tests/test_wx_connection_71_4.py` nodes are `gui`/`wx`/`semantic`; they build real connection panels/dialogs, dispatch control events, and observe protected-secret persistence, cancellation, worker outcomes, or visible control state. The no-selection test is classified only for the real disabled Connect button state; despite its name, it does not call the handler or prove a false return. Remote/self-test services and password prompt widgets are mocked seams. All eight passed in 5.30s; Ruff passed. Actual REPORT: 2,678 collected; unit 709, integration 356, gui 649, e2e 5, runtime_smoke 6, contract 556, audit 138, reporting 21, release 82; 156 zero-primary and 0 multi-primary. Qualifiers: semantic=838, regression=146, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=518, qt=263, packaging=4. After 2,382 reviewed nodes, 2,339 are classified and 43 reviewed false/skip gates remain zero-primary. RATCHET records the exact 156-node allowlist. Focused successful newly reviewed-node passes total 2,340. Collection remains 2,678.

Unclassified carry-forward nodeids (2; weak MFA test input and non-observable terminal language refresh):
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All eight `tests/test_wx_connection.py` nodes were reviewed without claiming GUI execution: five `WxConnectionModel` logic cases are `unit`/`wx`/`semantic`, the `ssh_info_from_profile` mapping is `contract`/`wx`/`semantic`, and two source-only view-wiring checks are `audit`/`wx`. The mixed unknown-profile/import test uses `unit` as its primary because it exercises the model; its source assertion remains supplementary. The security test's `hasattr` assertion does not prove MFA persistence behavior; its unit classification is for host-key rejection and one-time response forwarding. All eight passed in 0.49s; Ruff passed. Actual REPORT: 2,678 collected; unit 714, integration 356, gui 649, e2e 5, runtime_smoke 6, contract 557, audit 140, reporting 21, release 82; 148 zero-primary and 0 multi-primary. Qualifiers: semantic=844, regression=146, performance=13, resource=101, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=526, qt=263, packaging=4. After 2,398 reviewed nodes, 2,355 are classified and 43 reviewed false/skip gates remain zero-primary. RATCHET records the exact 148-node allowlist. Focused successful newly reviewed-node passes total 2,356. Collection remains 2,678.

Unclassified carry-forward nodeids (2; weak MFA test input and non-observable terminal language refresh):
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Five of six `tests/test_wx_file_actions_stress.py` nodes are classified: two direct context-selection policy cases are `unit`/`semantic`; the actual wx context-menu event case and local/remote mutation panel cases are `gui`/`wx`/`semantic`; local filesystem mutation also carries `resource`. The remaining mock-backend-only test exercises `MockRemoteFilesBackend` directly and does not involve production code, so it remains zero-primary. All six passed in 13.53s; the two longest calls were 6.63s and 6.25s, so none received `slow`; Ruff passed. wxWidgets emitted an `UnregisterClass` teardown warning despite pytest exit 0; carry it into Packet J resource review. Actual REPORT: 2,678 collected; unit 716, integration 356, gui 652, e2e 5, runtime_smoke 6, contract 557, audit 140, reporting 21, release 82; 143 zero-primary and 0 multi-primary. Qualifiers: semantic=849, regression=146, performance=13, resource=102, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=529, qt=263, packaging=4. After 2,404 reviewed nodes, 2,360 are classified and 44 reviewed false/skip gates remain zero-primary. RATCHET records the exact 143-node allowlist. Focused successful newly reviewed-node passes total 2,362. Collection remains 2,678.

Unclassified carry-forward nodeids (3; test helper only, weak MFA input, non-observable terminal language refresh):
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All three `tests/test_connection_controller.py` nodes are classified by behavior: the state-machine/cancel transition sequence is `unit`/`semantic`; the framework-neutral host-key and keyboard-interactive request shape is `contract`/`semantic`; and mutable secret-buffer wiping is `unit`/`semantic`/`resource`. The source inspection for the no-Qt constraint remains a supplementary static assertion; no GUI or concurrency claim is made. All three passed in 0.28s; Ruff passed. Actual REPORT: 2,678 collected; unit 718, integration 356, gui 652, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 140 zero-primary and 0 multi-primary. Qualifiers: semantic=852, regression=146, performance=13, resource=103, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=529, qt=263, packaging=4. After 2,407 reviewed nodes, 2,363 are classified and 44 reviewed false/skip gates remain zero-primary. RATCHET records the exact 140-node allowlist. Focused successful newly reviewed-node passes total 2,365. Collection remains 2,678.

Unclassified carry-forward nodeids (3; test helper only, weak MFA input, non-observable terminal language refresh):
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: The three remaining zero-primary nodes in `tests/test_wx_jobs_behavior.py` each call `pytest.skip` unconditionally before reaching their assertions, so their pause/follow, minimize/restore, and stale-result claims are not exercised. No markers or test bodies were changed. Focused module validation: 2 passed, 3 skipped in 2.02s; Ruff passed. Actual REPORT remains 2,678 collected; unit 718, integration 356, gui 652, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 140 zero-primary and 0 multi-primary. Qualifiers remain semantic=852, regression=146, performance=13, resource=103, concurrency=108, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=529, qt=263, packaging=4. After 2,410 reviewed nodes, 2,363 are classified and 47 reviewed false/skip gates remain zero-primary. RATCHET records the exact 140-node allowlist. Focused successful newly reviewed-node passes remain 2,365. Collection remains 2,678.

Unclassified carry-forward nodeids (6; three unconditional skips plus test-helper-only, weak MFA input, non-observable terminal language refresh):
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All three `tests/test_wx_local_mutation_snapshot.py` nodes dispatch keyboard actions to real wx file-list controls, block mutations with deterministic events, switch tabs, and verify that paste/rename/delete remain owned by the originating tab. They are `gui`/`wx`/`semantic`/`concurrency`/`resource`. All three passed in 1.03s (calls below 0.25s); Ruff passed. wxWidgets emitted the same `UnregisterClass` open-window teardown warning despite pytest exit 0; carry the repeated warning into Packet J resource review. Actual REPORT: 2,678 collected; unit 718, integration 356, gui 655, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 137 zero-primary and 0 multi-primary. Qualifiers: semantic=855, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=532, qt=263, packaging=4. After 2,413 reviewed nodes, 2,366 are classified and 47 reviewed false/skip gates remain zero-primary. RATCHET records the exact 137-node allowlist. Focused successful newly reviewed-node passes total 2,368. Collection remains 2,678.

Unclassified carry-forward nodeids (6; three unconditional skips plus test-helper-only, weak MFA input, non-observable terminal language refresh):
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All three `tests/test_wx_shell_i18n.py` nodes exercise real wx shell/menu/notebook and open Jobs window state. The language menu and checked language state, live window retranslation, and navigation/terminal controls are `gui`/`wx`/`semantic`. All three passed in 10.29s; the largest single setup was 3.02s, so no `slow` qualifier was added; Ruff passed. wxWidgets emitted the repeated `UnregisterClass` open-window teardown warning despite pytest exit 0; retain it for Packet J resource review. Actual REPORT: 2,678 collected; unit 718, integration 356, gui 658, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 134 zero-primary and 0 multi-primary. Qualifiers: semantic=858, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=535, qt=263, packaging=4. After 2,416 reviewed nodes, 2,369 are classified and 47 reviewed false/skip gates remain zero-primary. RATCHET records the exact 134-node allowlist. Focused successful newly reviewed-node passes total 2,371. Collection remains 2,678.

Unclassified carry-forward nodeids (6; three unconditional skips plus test-helper-only, weak MFA input, non-observable terminal language refresh):
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All three `tests/test_wx_term002.py` nodes are `gui`/`wx`/`semantic`: two real shell view actions route a script to the SSH seam, and the fallback terminal receives actual wx character events for Unicode, arrow, and shifted-Tab input. They remain GUI cases rather than E2E because the SSH boundary is mocked and the tests cover individual view actions. All three passed in 4.82s; largest setup was 1.91s, so no `slow` qualifier was added; Ruff passed. wxWidgets again emitted the `UnregisterClass` open-window teardown warning despite pytest exit 0; keep tracking it for Packet J. Actual REPORT: 2,678 collected; unit 718, integration 356, gui 661, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 131 zero-primary and 0 multi-primary. Qualifiers: semantic=861, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=538, qt=263, packaging=4. After 2,419 reviewed nodes, 2,372 are classified and 47 reviewed false/skip gates remain zero-primary. RATCHET records the exact 131-node allowlist. Focused successful newly reviewed-node passes total 2,374. Collection remains 2,678.

Unclassified carry-forward nodeids (6; three unconditional skips plus test-helper-only, weak MFA input, non-observable terminal language refresh):
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All five `tests/test_slurm_ssh.py` nodes invoke `SSHSlurmBackend` directly with a fake SSH seam and assert the exact `sbatch` command/directory behavior, shell quoting, stderr/exit-code fallback, or configured system-command allowlist. They are `unit`/`semantic`; no real SSH host is contacted. All five passed in 0.27s; Ruff passed. Actual REPORT: 2,678 collected; unit 723, integration 356, gui 661, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 126 zero-primary and 0 multi-primary. Qualifiers: semantic=866, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=538, qt=263, packaging=4. After 2,424 reviewed nodes, 2,377 are classified and 47 reviewed false/skip gates remain zero-primary. RATCHET records the exact 126-node allowlist. Focused successful newly reviewed-node passes total 2,379. Collection remains 2,678.

Unclassified carry-forward nodeids (6; three unconditional skips plus test-helper-only, weak MFA input, non-observable terminal language refresh):
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: In `tests/test_wx_a11y.py`, `test_wx_a11y_focus_order_and_labels` creates a real wx shell and asserts tab/menu/button labels plus notebook selection changes, so it is `gui`/`wx`/`semantic`. Its `files_page.IsShown() or True` assertion remains tautological and proves no visibility behavior. `test_wx_a11y_terminal_limits_documented` only checks that `audit/A11Y_AUDIT.md` exists and contains the word “terminal”; it remains zero-primary because this does not establish an accessibility limit or usable documentation contract. Both tests passed in 3.64s; Ruff passed. Actual REPORT: 2,678 collected; unit 723, integration 356, gui 662, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 125 zero-primary and 0 multi-primary. Qualifiers: semantic=867, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=539, qt=263, packaging=4. After 2,426 reviewed nodes, 2,378 are classified and 48 reviewed false/skip gates remain zero-primary. RATCHET records the exact 125-node allowlist. Focused successful newly reviewed-node passes total 2,381. Collection remains 2,678.

Unclassified carry-forward nodeids (7; three unconditional skips, weak docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_wx_transfer_workspace.py` nodes exercise the pure `WxTransferWorkspace` model, not wx widgets/events. Double-click/cross-pane clipboard/drag-operation mappings and storage/conflict/checksum settings are `unit`/`wx`/`semantic`; unknown-storage fallback is also model behavior. The path values are not used to create files, so no `resource` qualifier was added. Both passed in 0.33s; Ruff passed. Actual REPORT: 2,678 collected; unit 725, integration 356, gui 662, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 123 zero-primary and 0 multi-primary. Qualifiers: semantic=869, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=541, qt=263, packaging=4. After 2,428 reviewed nodes, 2,380 are classified and 48 reviewed false/skip gates remain zero-primary. RATCHET records the exact 123-node allowlist. Focused successful newly reviewed-node passes total 2,383. Collection remains 2,678.

Unclassified carry-forward nodeids (7; three unconditional skips, weak docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_wx_logs.py` nodes exercise `WxLogsModel` directly, so they are `unit`/`wx`/`semantic`, not GUI tests. The large-log case checks tail truncation, secret redaction, copy selection, and export delegation; the missing-log case checks empty output, with its no-Qt source assertion supplementary. Both passed in 0.36s; Ruff passed. Actual REPORT: 2,678 collected; unit 727, integration 356, gui 662, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 121 zero-primary and 0 multi-primary. Qualifiers: semantic=871, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=543, qt=263, packaging=4. After 2,430 reviewed nodes, 2,382 are classified and 48 reviewed false/skip gates remain zero-primary. RATCHET records the exact 121-node allowlist. Focused successful newly reviewed-node passes total 2,385. Collection remains 2,678.

Unclassified carry-forward nodeids (7; three unconditional skips, weak docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_wx_directories.py` nodes exercise the pure `WxDirectoriesWorkspace` model with callback seams: storage lookup, double-click navigation/edit choices, submit/run dispatch, and deterministic batch-submit ordering. They are `unit`/`wx`/`semantic`; the no-Qt source assertion is supplementary and no wx widget/event is used. Both passed in 0.37s; Ruff passed. Actual REPORT: 2,678 collected; unit 729, integration 356, gui 662, e2e 5, runtime_smoke 6, contract 558, audit 140, reporting 21, release 82; 119 zero-primary and 0 multi-primary. Qualifiers: semantic=873, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=545, qt=263, packaging=4. After 2,432 reviewed nodes, 2,384 are classified and 48 reviewed false/skip gates remain zero-primary. RATCHET records the exact 119-node allowlist. Focused successful newly reviewed-node passes total 2,387. Collection remains 2,678.

Unclassified carry-forward nodeids (7; three unconditional skips, weak docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_wx_terminal.py` nodes were classified from actual scope: direct `TerminalModel` control-code/resize/find/font/clear behavior is `unit`/`wx`/`semantic`; the optional SSH/WebView wiring source check is `audit`/`wx` and makes no runtime GUI claim. Both passed in 0.68s; Ruff passed. Actual REPORT: 2,678 collected; unit 730, integration 356, gui 662, e2e 5, runtime_smoke 6, contract 558, audit 141, reporting 21, release 82; 117 zero-primary and 0 multi-primary. Qualifiers: semantic=874, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=547, qt=263, packaging=4. After 2,434 reviewed nodes, 2,386 are classified and 48 reviewed false/skip gates remain zero-primary. RATCHET records the exact 117-node allowlist. Focused successful newly reviewed-node passes total 2,389. Collection remains 2,678.

Unclassified carry-forward nodeids (7; three unconditional skips, weak docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_wx_ansys.py` nodes compose `WxAnsysModel`, `AnsysToolPresentation`, and `LinterTool`, with a fake plugin engine seam; they are `integration`/`wx`/`semantic`. They cover multi-file/folder result handling and containment of an unavailable engine. No GUI runtime is exercised. Both passed in 0.57s; Ruff passed. Actual REPORT: 2,678 collected; unit 730, integration 358, gui 662, e2e 5, runtime_smoke 6, contract 558, audit 141, reporting 21, release 82; 115 zero-primary and 0 multi-primary. Qualifiers: semantic=876, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=549, qt=263, packaging=4. After 2,436 reviewed nodes, 2,388 are classified and 48 reviewed false/skip gates remain zero-primary. RATCHET records the exact 115-node allowlist. Focused successful newly reviewed-node passes total 2,391. Collection remains 2,678.

Unclassified carry-forward nodeids (7; three unconditional skips, weak docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: `tests/test_wx_65a_stress.py::test_wx_65a_integrated_stress` is classified `gui`/`wx`/`semantic` because it creates a real wx shell, dispatches controls/events, and observes GUI state. No `concurrency` qualifier is used: the “race” sections do not establish deterministic competing operations or ownership barriers. No `resource` qualifier is used because the lifecycle assertions are not reached. The focused run failed after 37.62s at `KeyError: 'language_button'`. The unchanged test was run in a clean detached worktree at the exact frozen SHA `12ce79935bf076e1062c57dc7dbd148bad2bfae1` and failed after 36.34s at the same `language_button` lookup with the same preceding 500 tab switches, 300 checkbox dispatches, and 300 refreshes; duplicate wx image-handler and WebView2 abort diagnostics also reproduced. This is a supplemental confirmed frozen-baseline failure beyond the seven in the original Packet A inventory. The test body and production code were not changed; later stress/lifecycle assertions did not run. Ruff passed. Actual REPORT: 2,678 collected; unit 730, integration 358, gui 663, e2e 5, runtime_smoke 6, contract 558, audit 141, reporting 21, release 82; 114 zero-primary and 0 multi-primary. Qualifiers: semantic=877, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=550, qt=263, packaging=4. After 2,437 reviewed nodes, 2,389 are classified and 48 reviewed false/skip gates remain zero-primary. RATCHET records the exact 114-node allowlist. Focused successful newly reviewed-node passes remain 2,391. Collection remains 2,678.

Unclassified carry-forward nodeids (7; three unconditional skips, weak docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist` remains zero-primary. It “simulates cancel” by never calling save and then checks the already-empty profile list again; it dispatches no dialog or cancel action and proves no cancellation behavior. The exact node passed in 0.58s; Ruff passed. No test or production code changed. Actual REPORT remains 2,678 collected; unit 730, integration 358, gui 663, e2e 5, runtime_smoke 6, contract 558, audit 141, reporting 21, release 82; 114 zero-primary and 0 multi-primary. Qualifiers remain semantic=877, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=550, qt=263, packaging=4. After 2,438 reviewed nodes, 2,389 are classified and 49 reviewed false/skip gates remain zero-primary. RATCHET records the exact 114-node allowlist. Focused successful newly reviewed-node passes total 2,392. Collection remains 2,678.

Unclassified carry-forward nodeids (8; three unconditional skips, weak cancel/docs/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure` remains zero-primary. It repeatedly toggles `MockHPCJobs.missing` and checks the test helper's own `read_output` exception/recovery behavior; it does not call a production jobs model, view, or backend. The node passed in 0.53s; Ruff passed. No markers or bodies changed. Actual REPORT remains 2,678 collected; unit 730, integration 358, gui 663, e2e 5, runtime_smoke 6, contract 558, audit 141, reporting 21, release 82; 114 zero-primary and 0 multi-primary. Qualifiers remain semantic=877, regression=146, performance=13, resource=106, concurrency=111, slow=13, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=550, qt=263, packaging=4. After 2,439 reviewed nodes, 2,389 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET records the exact 114-node allowlist. Focused successful newly reviewed-node passes total 2,393. Collection remains 2,678.

Unclassified carry-forward nodeids (9; three unconditional skips, weak documentation/cancel/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: `tests/test_wx_layout_resize.py::test_wx_layout_resize` is `gui`/`wx`/`semantic`/`slow`. It resizes a real wx shell through 400 deterministic English/Turkish sizes and measures required-control geometry, clipping, overflow, minimum size, and detached-frame invariants across 2,800 tab selections. It passed in 140.32s; Ruff passed. No performance benchmark or concurrency invariant is claimed. Actual REPORT: 2,678 collected; unit 730, integration 358, gui 664, e2e 5, runtime_smoke 6, contract 558, audit 141, reporting 21, release 82; 113 zero-primary and 0 multi-primary. Qualifiers: semantic=878, regression=146, performance=13, resource=106, concurrency=111, slow=14, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=263, packaging=4. After 2,440 reviewed nodes, 2,390 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET records the exact 113-node allowlist. Focused successful newly reviewed-node passes total 2,394. Collection remains 2,678.

Unclassified carry-forward nodeids (9; three unconditional skips, weak documentation/cancel/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_presentation_models.py` nodes were classified by scope. The `StatusViewModel`/`ProgressViewModel` and `EventBus` weak-subscriber lifetime case is `unit`/`semantic`/`resource`; it allocates/subscribes an adapter, releases it, and verifies it is no longer called. The source-only no-toolkit-import check is `audit`. No GUI runtime is exercised. Both passed in 0.29s; Ruff passed. Actual REPORT: 2,678 collected; unit 731, integration 358, gui 664, e2e 5, runtime_smoke 6, contract 558, audit 142, reporting 21, release 82; 111 zero-primary and 0 multi-primary. Qualifiers: semantic=879, regression=146, performance=13, resource=107, concurrency=111, slow=14, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=263, packaging=4. After 2,442 reviewed nodes, 2,392 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET records the exact 111-node allowlist. Focused successful newly reviewed-node passes total 2,396. Collection remains 2,678.

Unclassified carry-forward nodeids (9; three unconditional skips, weak documentation/cancel/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: All three `tests/test_wiki_docs.py` nodes exercise the repository wiki policy checker and are `audit`: repository documentation acceptance, a minimal synthetic wiki, and explicit reporting of broken links/assets/headings/sidebar/orphan/forbidden-term violations. The repository check and eight synthetic violation cases passed; pytest reports 3 passed plus 8 subtests in 1.22s. Ruff passed. Actual REPORT: 2,678 collected; unit 731, integration 358, gui 664, e2e 5, runtime_smoke 6, contract 558, audit 145, reporting 21, release 82; 108 zero-primary and 0 multi-primary. Qualifiers: semantic=879, regression=146, performance=13, resource=107, concurrency=111, slow=14, subprocess=43, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=263, packaging=4. After 2,445 reviewed nodes, 2,395 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET records the exact 108-node allowlist. Focused successful newly reviewed-node passes total 2,399. Collection remains 2,678.

Unclassified carry-forward nodeids (9; three unconditional skips, weak documentation/cancel/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_mock_cluster_roundtrip.py` nodes are `e2e`/`semantic`/`subprocess`. They launch the actual CLI in child processes and exercise file upload/list/checksum/download/remove plus jobs commands across Paramiko SSH/SFTP wire connections to a local disposable fake server; they do not contact a real cluster. Both passed in 12.77s; individual calls were 7.40s and 5.11s, so no `slow` qualifier was added. Ruff passed. The test was run with a unique task-private `TEMP`/`TMP` directory because its local upload/download files sit beside the temporary cluster root; the wrapper removed that isolated directory after the run. Actual REPORT: 2,678 collected; unit 731, integration 358, gui 664, e2e 7, runtime_smoke 6, contract 558, audit 145, reporting 21, release 82; 106 zero-primary and 0 multi-primary. Qualifiers: semantic=881, regression=146, performance=13, resource=107, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=263, packaging=4. After 2,447 reviewed nodes, 2,397 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET records the exact 106-node allowlist. Focused successful newly reviewed-node passes total 2,401. Collection remains 2,678.

Unclassified carry-forward nodeids (9; three unconditional skips, weak documentation/cancel/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_deb_installer.py` nodes are `release`/`semantic`; staging/private-mode/symlink safety also carries `resource`. PackageKit support probing distinguishes an unsupported local-install command from a missing filename. The stage test verified the copied payload, private mode, and install command, then took its existing environment skip because this Windows host could not create a symlink. Validation: 1 passed, 1 skipped in 0.31s; Ruff passed. Actual REPORT: 2,678 collected; unit 731, integration 358, gui 664, e2e 7, runtime_smoke 6, contract 558, audit 145, reporting 21, release 84; 104 zero-primary and 0 multi-primary. Qualifiers: semantic=883, regression=146, performance=13, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=263, packaging=4. After 2,449 reviewed nodes, 2,399 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET records the exact 104-node allowlist. Focused successful newly reviewed-node passes total 2,402. Collection remains 2,678.

Unclassified carry-forward nodeids (9; three unconditional skips, weak documentation/cancel/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`

Latest Packet M checkpoint: Both `tests/test_help_search.py` nodes exercise the in-memory `HelpSearchIndex` service directly and are `unit`/`semantic`: cross-context shortcut/action lookup, context disambiguation, no-result behavior, and Turkish search. No GUI object is created. Both passed in 0.29s; Ruff passed. Actual REPORT: 2,678 collected; unit 733, integration 358, gui 664, e2e 7, runtime_smoke 6, contract 558, audit 145, reporting 21, release 84; 102 zero-primary and 0 multi-primary. Qualifiers: semantic=885, regression=146, performance=13, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=263, packaging=4. After 2,451 reviewed nodes, 2,401 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET records the exact 102-node allowlist. Focused successful newly reviewed-node passes total 2,404. Collection remains 2,678.

Unclassified carry-forward nodeids (9; three unconditional skips, weak documentation/cancel/MFA assertions, plus helper-only and terminal-language gates):
- `tests/test_wx_jobs_stress.py::test_wx_jobs_stress_missing_output_recovery_pressure`
- `tests/test_wx_connection_profiles.py::test_cancel_does_not_persist`
- `tests/test_wx_a11y.py::test_wx_a11y_terminal_limits_documented`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_pause_keeps_refreshing_but_stops_live_follow`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_minimize_suspends_follow_and_restore_resumes_it`
- `tests/test_wx_jobs_behavior.py::test_wx_job_output_discards_stale_result_after_job_selection_changes`
- `tests/test_wx_file_actions_stress.py::test_remote_mutation_stress_has_no_lost_operations`
- `tests/test_connection_profile_service.py::ConnectionProfileServiceTests::test_mfa_transient_not_stored`
- `tests/test_wx_embedded_terminal.py::test_embedded_terminal_runtime_language_refresh`


## Packet M checkpoint — Help shortcut and macOS release documentation

Latest Packet M checkpoint: The two `tests/test_help_shortcut_reference.py` tests directly exercise `HELP_CATALOG.shortcut_reference` with platform and caller-supplied keymaps, including context duplicates and active-map replacement; both are `unit`/`semantic`, with no GUI renderer involved. The two `tests/test_macos_docs.py` tests assert required macOS installer names and installation prerequisites in the English/Turkish guides and README, so they are release-surface checks (`release`). All four passed in 0.67s; Ruff passed. Actual REPORT: 2,678 collected; unit 735, integration 358, gui 664, e2e 7, runtime_smoke 6, contract 558, audit 145, reporting 21, release 86; 98 zero-primary and 0 multi-primary. Qualifiers: semantic=887, regression=146, performance=13, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=263, packaging=4. After 2,455 reviewed nodes, 2,405 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 98-node allowlist; collection remains 2,678.


## Packet M checkpoint — output buffer, diagnostics, and trusted-tool disclosure

The two `tests/test_wave37_output_buffer.py` tests check the bounded newest-line marker and preservation of active ANSI state through the pure output-buffer helpers; they are `unit`/`semantic`, with no widget instantiated. In `tests/test_wave37_diagnostics.py`, the injected connection-stage runner is `unit`/`semantic`; the config conflict preference test exercises persistence against an isolated temporary config path and is `integration`/`semantic`. `tests/test_trusted_tool_disclosure.py` separates translation-string coverage (`contract`/`semantic`) from a real Qt plugin card and details action with the disclosure visible in the card and intercepted dialog (`gui`/`qt`/`semantic`). All six passed in 0.82s; Ruff passed. Actual REPORT: 2,678 collected; unit 738, integration 359, gui 665, e2e 7, runtime_smoke 6, contract 559, audit 145, reporting 21, release 86; 92 zero-primary and 0 multi-primary. Qualifiers: semantic=893, regression=146, performance=13, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=264, packaging=4. After 2,461 reviewed nodes, 2,411 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 92-node allowlist; collection remains 2,678.


## Packet M checkpoint — macOS product surface, bundle spec, and SBOM

`tests/test_macos_product_surface.py` separates a mocked application identity helper (`unit`/`semantic`) from a real settings dialog that asserts the macOS-only control is hidden (`gui`/`qt`/`semantic`). The macOS bundle-spec test checks the actual bundle identifier, minimum OS, native icon, entitlements, and Qt hook surface, so it is `release`. The two `tests/test_sbom.py` cases run SBOM generation and platform-marker selection against synthetic lock files and temporary outputs; they are `unit`/`semantic`, without building an installer. All five passed in 0.92s; Ruff passed. Actual REPORT: 2,678 collected; unit 741, integration 359, gui 666, e2e 7, runtime_smoke 6, contract 559, audit 145, reporting 21, release 87; 87 zero-primary and 0 multi-primary. Qualifiers: semantic=897, regression=146, performance=13, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=265, packaging=4. After 2,466 reviewed nodes, 2,416 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 87-node allowlist; collection remains 2,678.


## Packet M checkpoint — performance probe and release-surface checks

The three `tests/test_performance_probe.py` checks validate event-loop delay detection, threshold recording, and the fast-tick no-report case against the probe itself; they are `unit`/`semantic`/`performance`, not app GUI performance benchmarks. `tests/test_release_surface.py` separates the live repository release-surface audit (`audit`/`semantic`) from the synthetic stale-tag validator case (`unit`/`semantic`). The `build_commands` checks in `tests/test_release_test_suite.py` are `unit`/`semantic`; the manual GUI-plan content check is `audit`. All eight passed in 1.56s; Ruff passed. Actual REPORT: 2,678 collected; unit 747, integration 359, gui 666, e2e 7, runtime_smoke 6, contract 559, audit 147, reporting 21, release 87; 79 zero-primary and 0 multi-primary. Qualifiers: semantic=904, regression=146, performance=16, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=551, qt=265, packaging=4. After 2,474 reviewed nodes, 2,424 are classified and 50 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 79-node allowlist; collection remains 2,678.


## Packet M checkpoint — Qt accordion, tracking settings, and weak assertions

Both `tests/test_remote_accordion.py` cases create a real Qt accordion, assert one visible body and both headers, and send Space to activate the second section (`gui`/`qt`/`semantic`). `tests/test_live_tracking_settings.py` persists the default, disabled, and clamped warning interval through isolated config storage (`integration`/`semantic`). `test_wave78_jobs_details.py::test_cluster_servers_hidden_for_unsupported_provider` creates a real wx jobs panel and asserts the five-tab state (`gui`/`wx`/`semantic`); its name’s “hidden for unsupported provider” claim is not asserted and remains a strengthening candidate. In `tests/test_wx_ansys_view.py`, `test_wx_ansys_close_in_flight_safe` ends with `assert True`, and `test_wx_ansys_i18n_refresh` uses an `or True` assertion and never requires a translated label change; both remain zero-primary and are recorded as false/skip gates. The four focused classified tests passed in 1.08s; Ruff passed. Actual REPORT: 2,678 collected; unit 747, integration 360, gui 669, e2e 7, runtime_smoke 6, contract 559, audit 147, reporting 21, release 87; 75 zero-primary and 0 multi-primary. Qualifiers: semantic=908, regression=146, performance=16, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=552, qt=267, packaging=4. After 2,480 reviewed nodes, 2,428 are classified and 52 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 75-node allowlist; collection remains 2,678.


## Packet M checkpoint — Wave7 editor, terminal, logs, and ZIP inventory

In `tests/test_wave7_editor_terminal_logs.py`, `test_full_editor_workflow_unicode` calls `DocumentModel` and asserts dirty-state transitions, and `test_full_terminal_unicode_flow` calls `TerminalModel.receive`; both are `unit`/`semantic`, not E2E. The remaining 13 reviewed candidates do not exercise their named production behavior: direct `pathlib` reads/writes, direct stdlib incremental codecs, a `WxLogsModel` object whose methods are never called, and direct stdlib ZIP operations. The path-traversal case merely confirms a malicious entry is present and explicitly does not extract it; it does not validate protection. These 13 remain zero-primary pending later evidence-backed rewrites. All 22 module tests passed in 0.47s with one expected `zipfile` duplicate-name warning; Ruff passed. Reviewed zero-primary nodeids left in the ratchet:
- `tests/test_wave7_editor_terminal_logs.py::TestScriptEditor::test_editor_file_roundtrip_unicode`
- `tests/test_wave7_editor_terminal_logs.py::TestScriptEditor::test_editor_bom_handling`
- `tests/test_wave7_editor_terminal_logs.py::TestScriptEditor::test_editor_no_bom_before_shebang`
- `tests/test_wave7_editor_terminal_logs.py::TestTerminal::test_shell_session_incremental_decoder`
- `tests/test_wave7_editor_terminal_logs.py::TestTerminal::test_shell_session_split_bytes`
- `tests/test_wave7_editor_terminal_logs.py::TestLogs::test_logs_model_unicode_read`
- `tests/test_wave7_editor_terminal_logs.py::TestLogs::test_logs_no_mojibake_patterns`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_unicode_filename_roundtrip`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_extract_unicode`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_nested_unicode`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_path_traversal_protection`
- `tests/test_wave7_editor_terminal_logs.py::TestZipArchives::test_zip_conflict_handling`
- `tests/test_wave7_editor_terminal_logs.py::TestIntegration::test_full_archive_unicode_flow`

Actual REPORT: 2,678 collected; unit 749, integration 360, gui 669, e2e 7, runtime_smoke 6, contract 559, audit 147, reporting 21, release 87; 73 zero-primary and 0 multi-primary. Qualifiers: semantic=910, regression=146, performance=16, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=552, qt=267, packaging=4. After 2,495 reviewed nodes, 2,430 are classified and 65 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 73-node allowlist; collection remains 2,678.


## Packet M checkpoint — wx i18n and platform audit checks

`tests/test_wx_i18n.py::test_translation_key_sets_match_for_wx_surfaces` checks actual English/Turkish key parity and lookup behavior, so it is `contract`/`wx`/`semantic`. `tests/test_wx_local_files.py::test_local_browser_model_has_no_toolkit_import` is a genuine optional-toolkit architecture audit (`audit`/`wx`). The Linux environment helper is `unit`/`wx`/`semantic`; the macOS migration audit is `audit`/`wx`. Three `TestNormalizationPolicy` candidates in Wave1 remain zero-primary: NFC/NFD and Turkish casing assertions compare Python literals/`unicodedata`, and the path check asserts `pathlib.Path` round-trips strings; they do not test repository behavior. All seven selected nodes passed in 0.56s; Ruff passed. Actual REPORT: 2,678 collected; unit 750, integration 360, gui 669, e2e 7, runtime_smoke 6, contract 560, audit 149, reporting 21, release 87; 69 zero-primary and 0 multi-primary. Qualifiers: semantic=912, regression=146, performance=16, resource=108, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=556, qt=267, packaging=4. After 2,502 reviewed nodes, 2,434 are classified and 68 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 69-node allowlist; collection remains 2,678.


## Packet M checkpoint — plugin labels, raw results, and transfer speed

The two plugin UI contribution candidates call `validate_ui_contributions_dict` directly and check localized label acceptance; they are schema `contract`/`semantic`, with no GUI rendering. `TestSec36_RawSourceIsolation::test_each_viewer_shows_only_its_source` constructs production `RawCommandResult` values and checks their source/stdout fields, so its actual behavior is `unit`/`semantic`; it does not test viewer widgets. `test_transfer_speed_test_round_trip_verifies_and_cleans_up` executes the real transfer-speed service against an in-memory fake backend, allocates an isolated local file, validates both directions, and checks remote cleanup (`integration`/`semantic`/`resource`). All five selected nodes passed in 0.66s; Ruff passed. `TestSec23_RawViewerClose::test_raw_viewer_refresh_callback_is_called` remains zero-primary: its nested callback is called directly by the test and never routed through the production viewer refresh action. It passed but does not substantiate the named production behavior. Actual REPORT: 2,678 collected; unit 751, integration 361, gui 669, e2e 7, runtime_smoke 6, contract 562, audit 149, reporting 21, release 87; 65 zero-primary and 0 multi-primary. Qualifiers: semantic=916, regression=146, performance=16, resource=109, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=556, qt=267, packaging=4. After 2,507 reviewed nodes, 2,438 are classified and 69 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 65-node allowlist; collection remains 2,678.


## Packet M checkpoint — wx shell P0 stress baseline failure

`test_wx_shell_p0_stress_real_wx_paths` was probed after source review because it intends to assert real shell language, lifecycle, worker completion, stale-notification, cleanup, and leak invariants. Current governance run failed before those stress checks at `KeyError: 'update'` while looking up a missing shell-control key (1 failed in 3.09s). A clean detached worktree at the frozen SHA `12ce79935bf076e1062c57dc7dbd148bad2bfae1` failed at the same lookup with the same WebView2 abort diagnostic (1 failed in 3.49s). This is a supplemental confirmed baseline-signature failure beyond the original seven archived in Packet A. The temporary taxonomy markers were removed and the test file is unchanged; no concurrency/resource claims were added because those assertions are unreachable. The node remains zero-primary and its baseline failure is preserved. Ruff passed. Actual REPORT: 2,678 collected; unit 751, integration 361, gui 669, e2e 7, runtime_smoke 6, contract 562, audit 149, reporting 21, release 87; 65 zero-primary and 0 multi-primary. Qualifiers: semantic=916, regression=146, performance=16, resource=109, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=556, qt=267, packaging=4. After 2,508 reviewed nodes, 2,438 are classified and 70 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 65-node allowlist; collection remains 2,678.


## Packet M checkpoint — localized sort and weak lifecycle assertions

`tests/test_hardening_additional.py::test_localized_sort` exercises production `get_display_label` with English and Turkish labels, so it is `unit`/`semantic`; its ordering lambda is copied into the test, so it does not prove the real MainWindow menu sort path. `test_wx_separator_lifecycle_offscreen` is unconditionally skipped before its subprocess GUI assertions and remains zero-primary. `tests/test_wx_files_sync_compare.py::test_wx_compare_close_in_flight_is_safe` reaches a real wx close while work is pending, but its only explicit state check ends in `or True`; it remains zero-primary. Validation: 2 passed, 1 skipped in 2.83s; Ruff passed on the changed module. Actual REPORT: 2,678 collected; unit 752, integration 361, gui 669, e2e 7, runtime_smoke 6, contract 562, audit 149, reporting 21, release 87; 64 zero-primary and 0 multi-primary. Qualifiers: semantic=917, regression=146, performance=16, resource=109, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=556, qt=267, packaging=4. After 2,511 reviewed nodes, 2,439 are classified and 72 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 64-node allowlist; collection remains 2,678.


## Packet M checkpoint — directory-comparison parent row

`tests/test_directory_comparison.py::SnapshotAndUiTests::test_parent_row_blank_and_sort_roles_intact` creates and interacts with the real Qt directory-comparison widget, reads a parent-row cell, and invokes tree sorting, so its primary category is `gui`/`qt`/`semantic`. Its parent-row lookup is optional and its sorting assertion is only a call followed by `assert True`; those weak assertions remain queued for later GUI strengthening. The exact node passed in 0.33s; Ruff passed. Actual REPORT: 2,678 collected; unit 752, integration 361, gui 670, e2e 7, runtime_smoke 6, contract 562, audit 149, reporting 21, release 87; 63 zero-primary and 0 multi-primary. Qualifiers: semantic=918, regression=146, performance=16, resource=109, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=556, qt=268, packaging=4. After 2,512 reviewed nodes, 2,440 are classified and 72 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 63-node allowlist; collection remains 2,678.


## Packet M checkpoint — splash policy, signing guard, and directory benchmark

`tests/test_startup_i18n.py::test_splash_text_never_depends_on_translation_catalog` is an allowed source-based startup architecture audit (`audit`). `tests/test_macos_signing.py::test_signing_requires_darwin_before_secret_access` verifies the release signer rejects Windows before secret/tool access (`release`/`semantic`). The 120-entry remote-directory check runs a real offscreen `RemoteDirPanel` against fabricated entries and asserts exact rendered row count, first-batch/first-row timing, and bounded termination (`gui`/`qt`/`semantic`/`performance`); its 30-second gate is a broad operability ceiling, not a product performance target. All three passed in 0.49s; Ruff passed. Actual REPORT: 2,678 collected; unit 752, integration 361, gui 671, e2e 7, runtime_smoke 6, contract 562, audit 150, reporting 21, release 88; 60 zero-primary and 0 multi-primary. Qualifiers: semantic=920, regression=146, performance=17, resource=109, concurrency=111, slow=14, subprocess=45, windows=3, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=556, qt=269, packaging=4. After 2,515 reviewed nodes, 2,443 are classified and 72 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 60-node allowlist; collection remains 2,678.


## Packet M checkpoint — security and platform audit checks

`tests/test_security_hardening_wave.py::test_plugin_runtime_has_no_dynamic_source_execution` is a static security-policy check (`audit`), but its only assertion confirms the approved `create_plugin(` token exists; it does not itself prove dynamic execution is absent. `tests/test_transfer_directory_controllers.py::test_controllers_have_no_qt_imports` is an architecture audit (`audit`/`qt`) whose namespace check only detects a binding named exactly `PySide`, so its guarantee is narrow. `tests/test_wx_windows_audit.py::test_windows_audit_core_checks` runs the offline audit function and verifies its geometry, file-URL, terminal-interrupt, and wx-import checks (`audit`/`wx`/`windows`). All three passed in 0.41s; Ruff passed. Actual REPORT: 2,678 collected; unit 752, integration 361, gui 671, e2e 7, runtime_smoke 6, contract 562, audit 153, reporting 21, release 88; 57 zero-primary and 0 multi-primary. Qualifiers: semantic=920, regression=146, performance=17, resource=109, concurrency=111, slow=14, subprocess=45, windows=4, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=557, qt=270, packaging=4. After 2,518 reviewed nodes, 2,446 are classified and 72 reviewed false/skip gates remain zero-primary. RATCHET passes with the exact 57-node allowlist; collection remains 2,678.


## Packet M checkpoint — remaining zero-primary review ceiling

Current Packet M remainder has no additional safe marker-only classifications in the reviewed scope. The 57 actual zero-primary nodes consist of 24 nodes in baseline-evidence files explicitly held untouched (`test_connection_advanced_settings.py` 3, `test_macos_ci.py` 2, `test_macos_release_workflow.py` 14, `test_workflow_action_pins.py` 4, `test_wx_packaged_smoke.py` 1); 32 reviewed nodes that skip before assertions, test only standard-library/fixture behavior, or contain tautological/no-op assertions; and one supplemental confirmed baseline-signature failure, `tests/test_wx_shell_p0_stress.py::test_wx_shell_p0_stress_real_wx_paths` (same `KeyError: 'update'` at frozen SHA). Their exact nodeids remain in `taxonomy-ratchet.json`. Full collection passed: exit 0, 2,678 collected in 2.73s. RATCHET passed: 57 current allowlisted zero-primary, 0 new zero-primary, 0 multi-primary. `git diff --check` passed; no production, workflow, migration-ledger, or protected-guidance paths were changed. The original dirty checkout remains untouched. This is a progress checkpoint; it does not claim full-suite completion. Actual REPORT: 2,678 collected; unit 752, integration 361, gui 671, e2e 7, runtime_smoke 6, contract 562, audit 153, reporting 21, release 88; 57 zero-primary and 0 multi-primary. Qualifiers: semantic=920, regression=146, performance=17, resource=109, concurrency=111, slow=14, subprocess=45, windows=4, linux=0, macos=0, hardware=0, synthetic_hardware=0, license=2, acceptance=1, artifact_dependent=25, wx=557, qt=270, packaging=4. Packet M review: 2,518 nodes; 2,446 classified; 72 cumulative reviewed false/skip gates. The false/skip total includes retired nodes from earlier cleanup; 57 zero-primary nodes remain currently collected.

## Packet M checkpoint — invalid probes removed and actual zero-primary exceptions registered

Repository state at this checkpoint: governance worktree D:\Projeler\hpc-client-gui-test-governance, branch test-suite-governance-20260912, HEAD 13f1f9a8bd3a093c76e5034ef56e3140fd50d878; remote develop remains 12ce79935bf076e1062c57dc7dbd148bad2bfae1. No push was performed.

The current actual pytest REPORT collects 2,654 nodes: unit 754, integration 362, gui 679, e2e 7, runtime_smoke 6, contract 562, audit 156, reporting 21, release 101. It reports 6 zero-primary and 0 multi-primary; qualifiers are semantic 946, regression 157, performance 17, resource 112, concurrency 115, slow 14, subprocess 46, windows 4, linux 0, macos 0, hardware 0, synthetic_hardware 0, license 2, acceptance 1, artifact_dependent 25, wx 567, qt 270, packaging 4. REPORT uses actual pytest collection and markers.

The six remaining nodes are baseline exceptions in docs/testing/ZERO_PRIMARY_DISPOSITION.md and audit/test-taxonomy/zero-primary-disposition.json. The ratchet was reduced to those exact six nodeids and passes with 0 new zero-primary and 0 multi-primary.

Packet M cleanup removed three fake transfer-parallelism tests from test_connection_advanced_settings.py (covered by real TransferDialog/backend tests in test_ftp_widget.py and test_transfer_concurrency.py); one import-only favorites persistence stub from test_wave10_release_gate.py (canonical store owner is test_wave4_favorites_history_persistence.py::TestFavorites::test_survive_reload); three Wave1 stdlib-only normalization/pathlib probes; and 12 Wave7 direct pathlib/codecs/zipfile probes. Wave7 log Unicode now calls production WxLogsModel.refresh. A real Qt EditorWidget active-document save owner now verifies Unicode filename/content, and shell byte-boundary ownership remains in test_ssh_terminal_stream.py::SshTerminalStreamTests::test_utf8_split_at_each_byte_boundary. These deleted claims had no independent application behavior, and exact node references plus selector/report searches found no live invocation references.

Thirteen meaningful macOS release-policy nodes are now release/semantic; three surviving GitHub action pin policy nodes are audit/semantic. The known missing-CI baseline failures remain unmarked. Their non-failing owner selectors passed: 13 release policy tests and 3 action pin audit tests; the known baseline nodes were deselected. The rewritten log model, editor save, and shell stream owner tests passed (3). Transfer backend owner tests passed (7). Wave4 favorites persistence passed (20). The hard-coded skip was removed from test_wx_separator_lifecycle_offscreen; it now uses an unbuffered bounded child, fails on incomplete GUI assertions or teardown exit, and passed once (1 passed in 2.26s). The first pre-instrumentation attempt timed out at its 30-second child bound; no skip was retained.

A truthful rewrite of test_wx_job_output_does_not_overlap_remote_reads now blocks the first actual output read, requests a second refresh, and measures maximum simultaneous reads. It fails because max_active_reads is 2. The frozen-baseline node passes its weaker len(calls) >= 1 assertion, and src/hpc_gui/wx_jobs.py is byte-identical to the frozen baseline. This is a latent production concurrency defect present in the baseline behavior, newly exposed by a stronger test; it is not counted as an archived baseline test failure because the old body did not assert the invariant. The truthful failing test remains. No production fix is authorized for this separate defect; stop this concurrency sub-check and keep Packet M IN_PROGRESS.

Collection comparison against the frozen 2,673 nodeids: current 2,654; 17 added and 36 removed across the previously completed Packet B–M work. No unsupported full-suite result is claimed. The original seven archived failures, deterministic settings-dialog setup hang, and missing pytest-cov limitation remain untouched.

## Checkpoint — 2026-09-13: Packet F closeout, Packet N decision, and current taxonomy

Packet F is DONE. The scoped GUI review combined run passed **89 tests** and intentionally deselected two nodes: the exact Wave9 frozen CI-result failure and `test_generate_parity_evidence`, which writes the protected migration evidence JSON. The current `test_wave2_wx_ui_parity.py` has **17 passed**. It now dispatches actual wx context-menu/button/path actions and verifies visible or production state. Removed the no-op status-bar test and the nonexistent Forward-button gate. Renamed five Wave2 nodes and ten terminal bridge nodes for accurate behavior claims. Old→new examples:

| Old node | Current node / disposition |
| --- | --- |
| `TestContextMenuEventWiring::test_context_menu_open_wired` | `TestContextMenuEventWiring::test_context_menu_edit_opens_editor` |
| `TestPaneLabels::test_toolbar_button_tooltips` | `TestPaneLabels::test_toolbar_buttons_are_present_and_labeled` |
| `TestSelectionPreservation::test_rename_error_shows_unicode_filename` | `TestSelectionPreservation::test_invalid_rename_preserves_unicode_filename` |
| `TestStatusFeedback::test_listing_shows_file_count` | `TestDirectoryListing::test_listing_displays_all_files` |
| `TestToolbarAlignment::test_up_button_disabled_at_root` | `TestToolbarAlignment::test_parent_button_navigates_to_parent` |
| `TestStatusFeedback::test_status_bar_exists` | deleted: no status-bar behavior or assertion exists |
| `TestToolbarAlignment::test_forward_button_disabled_when_no_forward` | deleted: production UI has no Forward button |

The ten terminal parity evidence nodes were renamed to the current `test_panel_*`/`test_repeated_*` names and reclassified: four `contract`, six `unit`; all carry `wx` and `subprocess`. They intercept the JS runner and fake SSH seam, so they do not prove visible xterm rendering. The reporting evidence writer was not executed. The other reviewed files retain reporting/audit/release/integration ownership. Ruff passed on all `src`, `tests`, and `scripts` files.

Packet N decision: **NOT APPLICABLE**. `.github/workflows/` contains only `release.yml`; its trigger is `workflow_dispatch`. `scripts/ci.py` uses explicit file selectors (and Windows adds two unittest-discovery invocations); `scripts/release_test_suite.py` intentionally selects `not packaging` and isolates three wire-heavy files. Taxonomy markers do not express those platform/artifact boundaries more clearly. Preserve explicit selectors, the packaging exclusion, and the known packaged wx smoke holdout. No selector/workflow changed; automatic GitHub CI remains disabled.

Current actual taxonomy REPORT collects **2,652** tests: unit 760, integration 362, gui 667, e2e 7, runtime_smoke 6, contract 566, audit 156, reporting 21, release 101; **6 zero-primary**, **0 multi-primary**. Qualifiers: semantic 956, regression 157, performance 17, resource 114, concurrency 115, slow 14, subprocess 57, windows 4, linux 0, macos 0, hardware 0, synthetic_hardware 0, license 2, acceptance 1, artifact_dependent 25, wx 565, qt 270, packaging 4. Direct test-call and catch-all filename warnings are both 0. The zero-primary registry is valid and matches the exact six baseline failure exceptions. RATCHET passes: 6 allowlisted, 0 new, 0 multi-primary.

Collection passed: **2,652**, exit 0. Frozen baseline comparison: 2,673 baseline nodes; 2,652 current; 32 current additions and 53 removals. Relative to the immediately previous 2,654-node checkpoint, the F review introduces 15 renamed-node additions and 17 old-node removals (five Wave2 names changed, two invalid Wave2 nodes removed, and ten terminal bridge names changed). No other nodes changed in this F checkpoint.

### Packet M defects kept visible

`tests/test_wx_job_output_does_not_overlap_remote_reads` still exposes overlapping remote output reads (`max_active_reads == 2`). Its truthful failure remains and the affected concurrency sub-check is stopped; `src/hpc_gui/wx_jobs.py` matches the frozen baseline, whose old test asserted only that at least one read occurred.

`tests/test_hardening_additional.py::test_wx_separator_lifecycle_offscreen` now runs the actual menu lifecycle rather than unconditionally skipping. It repeatedly times out at the real `frame._wx_rebuild_plugins_menu()` call for the connected→hidden plugin transition: steps 1–3 finish, step 4 starts, and the function does not return within the bounded child timeout. The frozen test body was an unconditional skip, so this is not a confirmed baseline outcome. Keep the failing test and stop this sub-check. No `wx_shell.py` production change is authorized here.

### Teardown and xfail evidence

The teardown fixtures destroy all top-level wx windows, process pending events, yield once, and assert no top-level windows remain before `wx.App.Destroy()`. The four initial warning modules each passed without an UnregisterClass warning; the isolated `test_wx_transfer_ui_lifecycle.py` rerun passed 14 tests without the warning. A prior combined process run overlapped GUI subprocesses and coincided with a separator timeout, so the teardown results are recorded per-module rather than as a broad parallel run.

No xfail markers remain in `tests/`. The two stale terminal xfails previously produced six XPASS outcomes across three runs; after removing the decorators, both exact tests passed together (2 passed). Full-suite status remains incomplete: the archived seven isolated failures, known settings-dialog setup hang evidence, and missing `pytest-cov` limitation remain preserved. No full-suite pass/fail totals are asserted.

### Follow-up verification — sequential wx teardown sweep

To remove uncertainty from the earlier overlapping GUI-process run, the five warning-associated modules were rerun sequentially: `test_wx_file_context_i18n.py` (5 passed), `test_wx_remote_move_undo.py` (6), `test_wx_shell_i18n.py` (3), `test_wx_term002.py` (3), and `test_wx_transfer_ui_lifecycle.py` (14). All 31 tests passed; no `UnregisterClass` or open-window teardown warnings appeared. This supports the test-harness cleanup finding for those modules only; it does not resolve the separate Packet M plugin-menu hang or remote-read concurrency defect.

Final checkpoint state: Packet F DONE; Packet M IN_PROGRESS with those two truthful product-defect findings kept visible; Packet N NOT APPLICABLE; Packet O NOT STARTED. The governance worktree is still dirty and contains uncommitted Phase 2 work. Local HEAD is `13f1f9a8bd3a093c76e5034ef56e3140fd50d878`; remote `develop` remains at `12ce79935bf076e1062c57dc7dbd148bad2bfae1`; no push or new commit was made. The original dirty checkout remains untouched by this work.

### Push checkpoint — 2026-09-13

At the user's direction, the partial Wave checkpoint was committed as `9121f447b2645d46534b74befb6d51268fbc3393` (`test: checkpoint wave with exposed product defects`) and pushed to `origin/test-suite-governance-20260912`. The remote governance branch now matches that commit. Remote `develop` remains at the frozen baseline; no direct update to `develop` was made. The governance worktree was clean after the push. The commit documents Packet M as in progress and retains both truthful failing defect tests.


## Packet M checkpoint — Defect 1 Jobs output serialization

The user authorized focused production remediation for the Jobs output overlap. Before editing, the unchanged truthful node `tests/test_wx_jobs_behavior.py::test_wx_job_output_does_not_overlap_remote_reads` failed with `max_active_reads == 2` (expected 1). Root causes were: forced refresh bypassed the in-flight guard; redundant selection of the already-selected row advanced selection generations; the legacy output adapter mutated the shared follower from worker threads before stale-generation validation; and worker exceptions could leave request state reserved.

`src/hpc_gui/wx_jobs.py` now reserves one reader per selected job/output owner, coalesces requests during an active read into one follow-up, applies legacy snapshots only after the UI completion verifies the owner is still current, and releases the reservation through normal, error, and thread-start failure paths. A same-job selection event no longer invalidates a valid in-flight owner. The existing stale A→B test now also checks that A cannot overwrite B's follower state. Coverage was added for coalescing, the production remote follower path, read-error recovery, and close with a queued refresh. The contradictory pause/resume stress assertion was rewritten to verify 100 real pause/freeze and resume/catch-up transitions and the correct all-output button labels.

Validation: the four Jobs modules (`test_wx_jobs_behavior.py`, `test_wx_jobs_files_outputs.py`, `test_wx_jobs_final_fix.py`, `test_wx_jobs_stress.py`) passed 50 tests in 82.66s; the focused seven-node set passed; Ruff passed on the touched files; `git diff --check` and `py_compile` passed. Taxonomy REPORT collected 2,656 tests: unit 760, integration 362, gui 671, e2e 7, runtime_smoke 6, contract 566, audit 156, reporting 21, release 101; zero-primary 6 and multi-primary 0. RATCHET passed with 6 allowed baseline exceptions, 0 new zero-primary, and 0 multi-primary; direct-test-call and catch-all filename warnings are both 0. Four new test nodes cover coalescing, the production remote follower path, read-error recovery, and close with a queued refresh. Defect 1 verdict: PASS. Defect 2 remains IN_PROGRESS and has not yet been edited.


## Packet M checkpoint — Defect 2 wx plugin menu lifecycle

The lifecycle subprocess originally failed before reaching the menu transition because its `Path` import was nested below `sys.exit()` in the `ImportError` branch. After moving that import to the normal path, the exact test reproduced the connected→hidden rebuild timeout twice: Steps 1–3 completed, Step 4 entered `_wx_rebuild_plugins_menu()`, and the child exceeded its existing 15-second bound. Temporary trace points narrowed the failure to rebuilding after an attached submenu had been removed and destroyed.

The root cause was wx menu ownership misuse: the rebuild called `wx.Menu.Remove`, which detaches but retains the C++ `MenuItem`, then separately destroyed its submenu and discarded the item reference. wxPython documents `DestroyItem` as deleting a menu item and its submenu; rebuild now uses that single operation. It also uses the documented `Menu.Insert(pos, id, text, submenu)` path instead of manually constructing a `MenuItem` and setting its submenu twice. Dynamic frame-level action handlers are tracked and unbound before old menus are destroyed, preventing stale handlers from accumulating across rebuilds. These API semantics are documented by [wx.Menu](https://docs.wxpython.org/wx.Menu.html).

The existing lifecycle node was strengthened in place: it now checks 25 visible/hidden cycles, Unicode root/action labels (`Çalışma Araçları`, `日本語ツール`, `İş_日本語`), stable root ordering, no duplicate separators or roots, correct `hide` and `disable` conditions, and normal shell-close teardown with no open top-level windows or `UnregisterClass` warning. The original node passed twice in isolation (3.20s and 3.12s). `tests/test_hardening_additional.py` passed 12 tests; plugin contribution/menu modules passed 75 tests; Ruff and `git diff --check` passed. RATCHET passed: 6 current allowed baseline exceptions, 0 new zero-primary, 0 multi-primary. Defect 2 verdict: PASS. Packet M remains IN_PROGRESS pending sequential wx resource sweep and final registry/report reconciliation.


## Packet M closeout — 2026-09-13

### Defect 1 — Jobs output overlap

The unchanged truthful node `tests/test_wx_jobs_behavior.py::test_wx_job_output_does_not_overlap_remote_reads` reproduced `max_active_reads == 2` before the production edit. The wx refresh path previously started another output worker while a prior read for the same selected-job generation was active. It also allowed redundant same-row selection to invalidate a valid generation, and the legacy adapter could mutate follower state before stale-owner validation. `src/hpc_gui/wx_jobs.py` now reserves one in-flight read for the active output owner, coalesces refreshes into one pending read, validates the owner before applying output, rejects stale A→B results, and clears reservations on normal completion, errors, thread-start failures, and close.

The original overlap node now passes and retains its max-active-read assertion. The four Jobs modules passed 50 tests; the focused concurrency/error/close set passed; RUFF passed. The pause/resume stress node was rewritten to check freeze while paused and catch-up after resume. Four new Jobs nodes cover coalescing, the remote follower path, error recovery, and close with queued refresh. Defect 1: PASS.

### Defect 2 — Plugin menu connected→hidden hang

The exact bounded subprocess test reproduced the hang twice at Step 4. Root cause was wx menu ownership: `Menu.Remove` detached but retained the native `MenuItem`, then code separately destroyed its submenu and discarded the item reference. Rebuild now destroys the dynamic item through `DestroyItem`, inserts submenu items through `Menu.Insert(..., submenu)`, and unbinds tracked dynamic action handlers before replacing the menu.

The unchanged lifecycle node now checks 25 connected/hidden/visible cycles, exact Unicode labels and root order, `hide`/`disable` conditions, duplicate separators/roots, and shell teardown. It passed twice in isolation; the full hardening module passed 12, plugin contribution/menu modules passed 75, and RUFF passed. Defect 2: PASS.

### Sequential wx resource sweep

The seven required modules ran in one non-overlapping pytest process: `test_wx_jobs_behavior.py`, `test_hardening_additional.py`, `test_wx_file_context_i18n.py`, `test_wx_remote_move_undo.py`, `test_wx_shell_i18n.py`, `test_wx_term002.py`, and `test_wx_transfer_ui_lifecycle.py`. Result: **52 passed**, no warning output, and no `UnregisterClass`/open-window teardown warning. `tests/test_jobs_outputs_scroll.py` also passes alone (20 passed, 4 subtests passed); it exhibits an order-dependent Windows native test-process termination inside the broad release suite. The raw exit code is recorded below without diagnosing its native cause or attributing it to product behavior.

### Actual taxonomy and baseline comparison

REPORT at taxonomy snapshot `6cad198dca6c0f69bc8196f5030a7aebcba9d696` collects **2,656** nodes: unit 760, integration 362, gui 671, e2e 7, runtime_smoke 6, contract 566, audit 156, reporting 21, release 101. It records **6 zero-primary** and **0 multi-primary**; direct test-call and catch-all filename warnings are both zero. Qualifiers are semantic 958, regression 161, performance 17, resource 118, concurrency 119, slow 14, subprocess 57, windows 4, linux 0, macos 0, hardware 0, synthetic_hardware 0, license 2, acceptance 1, artifact_dependent 25, wx 569, qt 270, packaging 4.

The six zero-primary nodeids exactly match the disposition registry and RATCHET allowlist: five `ARCHIVED_BASELINE_FAILURE` entries and one `SUPPLEMENTAL_BASELINE_FAILURE`. None is passing or unreviewed evidence. RATCHET passes with six allowed, zero new zero-primary, and zero multi-primary. Full collection passes at 2,656. The archived RATCHET JSON retains its historical metadata (`repository_sha` `13f1f9a8…`, `collection_total` 2,654); that collection count describes the older snapshot, while current REPORT and collection both reconcile at 2,656. RATCHET consumes the exact six-node allowlist and checks current multi-primary state; the archived total is not a current collection claim. Against the frozen 2,673-node list, current collection has **36 added nodeids** (17 new and 19 renamed) and **53 removed old nodeids** (19 renamed and 34 deleted). The exact per-node mapping, replacement owners, deletion reasons, active selector search, and 64 test-body rewrites are in `audit/test-taxonomy/taxonomy-final-mapping.json`.

### Full-suite limitation and failure classification

`pytest-cov` is installed. The official `python scripts/release_test_suite.py --coverage` run passed its compile, i18n, and smoke preflight, then its broad pytest child terminated with Windows native exit code `3221226525` while entering `tests/test_jobs_outputs_scroll.py::JobsOutputsScrollTests::setUp`. The same native termination occurs in the official uninstrumented release-suite run. The affected module passes alone, so this is recorded as an **order-dependent native test-process failure**, not as an archived baseline failure or a proven product defect. The code is preserved as observed; no diagnosis of the native exit type is asserted. The interrupted full run produced no authoritative suite totals.

A fail-fast diagnostic after repairing a separate test-owned `wx.App` lifetime setup passed **345**, skipped **1**, deselected **4**, then reproduced the archived `tests/test_gui_audit_screenshots.py::test_manifest_exists_and_commit_current` failure (`manifest commit 3a729407... != current HEAD`), matching its known stale-manifest assertion. The one skip was `tests/test_deb_installer.py::test_stage_is_private_and_rejects_symlink`: this Windows host cannot create the symlink, after the private staging behavior had passed. The callback test itself now passes alone and the whole `test_corrective_jobs_details.py` module passes 17 tests. This diagnostic is not a full-suite result. Ten `test_plugin_contract.py` nodes were separately skipped because `HPC_GUI_CONTRACT_REPO` was unset; other conditional WebView/platform skip totals are unknown because the full suite terminated early. No xfail declarations remain in `tests/`. The seven archived baseline failures and original settings-dialog setup-hang evidence remain preserved; the settings test’s MagicMock-home setup was repaired in Packet H and no longer hangs. No full-suite PASS is claimed.

### Packet M verdict

Both authorized product defects are fixed; the truthful defect tests pass; the sequential wx resource sweep is clean; the exact six exceptions match the baseline registry and allowlist; REPORT and RATCHET reconcile; and no marker was added to hide a false gate. Packet M: **DONE**. The broad release suite remains incomplete due to the separately reported order-dependent native failure and known archived baseline failure. Packet N remains **NOT APPLICABLE**.

## Packet O closeout — 2026-09-13

Packet O produced the exact per-node mapping in `audit/test-taxonomy/taxonomy-final-mapping.json`, the current actual-marker REPORT in `audit/test-taxonomy/taxonomy-final.json`, and the human-readable closeout in `docs/testing/TEST_TAXONOMY_FINAL_REPORT.md`. The mapping was independently reconciled against fresh pytest collection: all 2,656 current nodeids match; primary and qualifier counts match; six zero-primary exceptions match the human registry and RATCHET allowlist; zero multi-primary; frozen/current delta is 36 current-only nodeids, 53 frozen-only nodeids, 19 renamed mappings, and 17 new nodes.

The separate read-only audit verdict is **PASS WITH FOLLOW-UP**. It confirmed both production invariants and behavioral tests, the exact exception list, actual marker counts, and disabled automatic CI. The two documentation corrections it requested are complete: the broad-run exit `3221226525` is stated as a raw native process termination without diagnosing its type, and `taxonomy-ratchet.json`’s `collection_total: 2654` / `repository_sha: 13f1f9a8…` are explicitly identified as historical snapshot metadata. The archived baseline artifact itself was not rewritten.

At review time, local HEAD `44d41d448c7e769ff1c6ab76c57ffb91570fe612` was 14 commits ahead and 0 behind `origin/develop` at frozen SHA `12ce79935bf076e1062c57dc7dbd148bad2bfae1`. `origin/test-suite-governance-20260912` was at `7a45496b4929a578db9a77b8a051e822fadc9011`, five commits behind the local closeout after Packet O. The diff from `develop` contains 267 test files, 3 production files limited to the authorized Packet F/M remediations, 6 documentation files, 13 audit files, one script, and `pyproject.toml`; no workflow, selector, migration/parity ledger, protected guidance, or `.tmp/` changes. Push is limited to the governance branch; no develop merge or push is performed.

Local validation passes for compileall, full Ruff, i18n, smoke, taxonomy REPORT/RATCHET, full collection, exact mapping reconciliation, checker tests, both truthful defect tests, and focused wx lifecycle/contribution/resource suites. The official broad release suite remains **INCOMPLETE**: both coverage and uninstrumented runners terminate natively during Jobs scroll setup; no full-suite pass/fail/skip/duration totals are claimed. Develop integration remains **NOT READY TO MERGE** pending a supported complete broad run. Packet O: **DONE**; Wave: **COMPLETE**, with this limitation explicit.

## Integration-readiness remediation checkpoint — 2026-09-13

The broad release run reached native termination after a Qt command-palette test and a wx Jobs-details module shared a process before the Qt Jobs-scroll module initialized. A temporary process monitor reproduced the access violation at `tests/test_jobs_outputs_scroll.py::JobsOutputsScrollTests::setUp`. Moving the complete `tests/test_corrective_jobs_details.py` module into its own release-runner pytest process kept all module tests enabled and removed the unsafe Qt→wx→Qt sequence. The runner partition is covered by `tests/test_release_test_suite.py`; the formerly crashing ordered sequence passed 3/3 repetitions, and the Jobs-scroll module passed alone (20 tests plus 4 subtests). This is classified as a GUI runtime isolation defect, not a proven production behavior defect.

The official uninstrumented runner then completed all pytest partitions without the prior native termination, but returned failure: its broad partition reported 23 failures, 2,406 passed, 20 skipped, 5 deselected, and 29 subtests passed in 1,231.71 seconds. The isolated WebView node passed, the corrective Jobs-details module passed 17, FTP widget passed 168, and editor flow passed 14. One isolated download-wire module reported 3 passed and one failure in that run. The complete `tests/test_download_cancel_wire.py` module passed 4/4 on a later isolated rerun, so that failure’s order/environment cause remains unresolved. The normal release suite must be repeated after this checkpoint; this result is historical evidence, not final validation.

Frozen-baseline exact checks reproduced the same failure signatures for `tests/test_wx_remote_file_actions_behavior.py::test_wx_remote_navigation_sort_and_provider_filter_are_visible` and `tests/test_wx_shell_p0.py::test_wx_shell_completion_states_and_deduplication`; each also fails in isolation on the current branch. These remain known baseline-signature failures. The exact download-wire node and `test_transfer_key_release` node pass on both frozen baseline and current branch in isolation; they do not explain the earlier release-run failure.

The 65A stress test had failed because it looked up a nonexistent language-button control and left the process-wide locale changed when it aborted. It now validates the real language-menu selection and current locale, restores locale in `finally`, and cleans up windows created by that test. The two connection-test fixtures set English while checking English-only messages and restore the previous locale afterward. The exact stress node passed, and its same-process sequence with both connection modules passed 16/16. This closes the reproduced locale contamination path without changing production code.

Two local commits were created: `50f4b001` isolates the order-sensitive GUI modules in the release runner, and `e0495b7a` restores wx language/window state between tests. They have not been pushed. Current actual taxonomy REPORT collects **2,657** nodes (unit 761, integration 362, gui 671, e2e 7, runtime_smoke 6, contract 566, audit 156, reporting 21, release 101), with the same six explicit zero-primary exceptions, zero multi-primary nodes, zero direct-test-call/catch-all warnings, and RATCHET PASS. The single added node is the runner-continuation regression test.

Compileall, full Ruff, i18n, smoke, runner tests (3 passed), and RATCHET passed at this checkpoint. Another worktree was running a separate GUI suite on the shared Windows host during some checks; those results are not included here. Still required: a non-overlapping final normal release run, coverage run only after the normal-run outcome is reconciled with the documented baseline policy, repeated focused regressions and wx resource sweep, package evidence if current Windows packaging is supported, updated final reports, and an independent final audit. Integration remains **NOT READY TO MERGE** until those checks finish.

### Additional release blocker — remote provider Logs filter

The exact remote navigation/filter node failed on both the frozen baseline and current governance code: selecting the Logs provider filter left `run.log` out of the visible listing. The backend entry had an empty `name` field but a populated remote `path`; the registry treated the present-but-empty `name` as authoritative, so suffix matching had no filename. `_entry_name` now uses the path basename when the supplied name is empty or whitespace, normalizing remote separators. A new unit regression covers both object and dictionary entries, including a Unicode/backslash path. `tests/test_file_filter_registry.py` passes 23 tests; Ruff and `py_compile` pass. The exact wx navigation node still needs a post-fix run once the unrelated GUI release process on the shared host exits.

Taxonomy REPORT after this new test collects **2,658** nodes (unit 762, integration 362, gui 671, e2e 7, runtime_smoke 6, contract 566, audit 156, reporting 21, release 101). The same six baseline exceptions remain; zero multi-primary, direct-test-call warnings, and catch-all warnings; RATCHET passes with zero new debt. The active external run is in `D:\Projeler\hpc-client-gui-integration-closeout`, not this governance worktree, and is not counted as branch validation. Its test process became unresponsive during the broad release run; do not use its result as evidence for this branch.

The first post-product-fix isolated wx navigation run passed its assertion (1 passed) but still emitted wx `UnregisterClass`/open-window teardown warning. Its fixture previously destroyed a live top-level collection while iterating and did not dispatch the host-close lifecycle. The fixture now closes a stable snapshot, pumps pending/native events without sleeps, destroys any remaining windows, and asserts no top-level window survives before `wx.App.Destroy()`. Exact-node/module verification was pending at that checkpoint; the follow-up below records its result. A 65A stress rerun was interrupted after an unrelated release suite began in another worktree; no result was recorded.

Follow-up: the remote action module passed **28/28** after the fixture repair, including the formerly failing provider Logs-filter node, and emitted no UnregisterClass warning. The fixture repair was committed as `2104d7db`. A real-wx-frame check of the revised 65A cleanup helper passes and leaves no top-level window; Ruff passes. The full 65A stress node remains unvalidated because prior attempts overlapped with separate worktree wx runs and were stopped. No results from the other worktree are counted for this branch.

### Integration-readiness release attempt — 2026-09-13

On governance HEAD `70e6005dcd6cec35cdf02438adc24f3c0a577372`, the official `python scripts/release_test_suite.py` runner started at 22:32:45 +03:00. Its compile, i18n, and smoke preflight passed. A second full release runner started in another worktree at 22:35:25 +03:00 while the local pytest child was active. The local console reached 78%, then its execution session ended with tool exit `-1`; the captured log has no pytest summary or runner end marker. No matching Windows Application Error event was found. Since another wx suite overlapped and the native exit signature is unavailable, this is an **incomplete, contaminated attempt**, not a valid product-failure or full-suite verdict. It provides no pass/fail/skip totals and does not replace a clean, non-overlapping rerun. No second attempt was started while the external suite remained active.

### Normal release runner — second attempt, 2026-09-13

From the new local branch/worktree at `108cf4e8a9f65cce143ad49542b28a9dde9b2465`, `python scripts/release_test_suite.py` started at 23:20:56 +03:00 and ended at 23:38:23 +03:00 (17m 26s). No long-lived GUI pytest process was present at start. Compile, i18n, and smoke preflight passed. The broad child collected 2,450 nodes and stopped without a pytest summary at about 90%; the release runner reported child exit `4294967295` (`0xFFFFFFFF`) and continued the later isolated partitions: WebView navigation 1 passed, corrective Jobs details 17 passed, FTP widget 168 passed, download-cancel wire 4 passed, and editor flow 14 passed. Runner exit was `-1`. No Windows Application Error event or more specific native exception was recorded. A short-lived unrelated Python process appeared near the final progress interval; its command line was unavailable, so possible interference cannot be excluded.

The last visible progress interval and the broad collection order place the termination in the wx Jobs cluster, near `tests/test_wx_jobs_final_fix.py` / `tests/test_wx_jobs_stress.py`; the exact running node and pytest phase are **unknown** because the runner uses quiet output and the child emitted no summary. The earlier `tests/test_jobs_outputs_scroll.py` collection block had already been passed. Do not attribute this exit to the product or call it an access violation without a matching node/exception. Next reproduction should use verbose pytest output and a bounded sequence around the Jobs cluster. Normal release result: **FAIL (broad pytest child terminated; totals unavailable)**. Coverage remains unrun until this is resolved.

## Post-consolidation final validation — 2026-09-14

The governance remediation line was consolidated with the integration-closeout line on `test-suite-governance-20260912`: uncommitted lifecycle hardening committed as `bee43f44`, closeout merged as `006982d0`, order-sensitive owner isolation and layout ownership as `39ae798c`, single-App fixture ownership as `01225f59`, and the stabilized Qt event-loop delay probe as `b018f10b`. `origin/develop` remains at the frozen baseline `12ce7993`; nothing was pushed to `develop`.

### Roaming PyNoAppError root cause

Repeated broad-process failures (`PyNoAppError` in `test_wx_jobs_behavior`, `test_wx_layout_resize`, then `test_wx_files_sync_compare`) were caused by multiple `wx.App` instances in one process. Reproduced mechanism: with app A orphaned and app B global, destroying or garbage-collecting A invalidates the global app (`wx.App.Get()` becomes `None`), so the next widget creation fails. Thirty-one test modules now adopt the existing App and create one only if none exists; `test_wx_layout_resize.py` no longer destroys an app it did not create. Jobs-behavior and layout-owner modules are also process-isolated in the release runner; all nodes still run.

### Final results

- Normal release suite `python -X faulthandler scripts/release_test_suite.py`: **exit 0**. Broad pytest: **2,352 passed, 0 failed**, 20 skipped, 6 deselected, 29 subtests in 899.04 s (14m 59s). Isolated partitions all passed: file-actions node 1, shell-p0 stress node 1 (258.87 s), corrective Jobs 17, WebView 29, jobs behavior 9, layout resize 1 (158.60 s), jobs files outputs 16, jobs final fix 15, jobs stress 10, FTP 168, download-cancel 4, editor flow 14. Final gate: `[release-test-suite] all release preflight gates passed`.
- Coverage release suite `python -X faulthandler scripts/release_test_suite.py --coverage`: **exit 0**. Broad pytest: **2,352 passed, 0 failed**, 20 skipped, 6 deselected, 29 subtests in 778.73 s (12m 58s). Final partition coverage: `Required test coverage of 65% reached. Total coverage: 66.41%`.
- No `python.exe` Windows Application Error events and no new crash dumps during 2026-09-14 14:34–15:39. The earlier native signatures (`0xC000041D`, `0xC0000005`, `0xC0000374`) did not recur; the broad process completed both times.
- Taxonomy: **2,661 collected**, zero-primary 0, multi-primary 0, direct-call/catch-all warnings 0; strict zero-debt RATCHET PASS. The six prior zero-primary exceptions were directly resolved through passing successor tests with exactly one primary marker.
- compileall, full Ruff, i18n, and smoke all exit 0.

Integration verdict: **READY TO MERGE INTO DEVELOP** for the governance/remediation branch. Release readiness (packaged, cross-platform, cluster, manual GUI evidence) remains a separate NO-GO and is unchanged.
