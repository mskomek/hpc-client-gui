# Wave and migration ownership review — Packet G

Reviewed on `test-suite-governance-20260912-v2` at `e4e1745e`.

No production code, CI selector, or migration ledger was changed. No test node was removed in this packet. The review found no direct test-calling-test pattern in the candidate files. Historical Wave names are provenance; they do not by themselves justify deletion.

| Candidate | Current ownership and evidence | Decision |
|---|---|---|
| `tests/test_wave0_unicode_baseline.py` | Golden Unicode fixture integrity, real temporary-filesystem name roundtrips, locale/catalog shape, and static encoding-boundary audits. Its old SFTP `"utf-8"` source-presence test was removed in D2; the distinct lossy SSH replacement-policy audit remains. | Retain as baseline and static audit provenance. The SFTP byte behavior now belongs to `test_ssh_files_byte_preservation.py`. |
| `tests/test_wave1_unicode_core_policy.py` | Translation correction, Unicode roundtrips, path normalization/casing policy, and lossy-decoding policy. D1 removed its duplicate SSH decode source assertion; D2 removed its ineffective SFTP UTF-8 source assertion. | Retain the remaining unique checks. Do not count the static source-policy cases as runtime SFTP behavior. |
| `tests/test_wave2_directories_local_files.py` | Local and framework-neutral directory navigation, CRUD, filtering, clipboard behavior, conflict/error cases, and path encoding. These are distinct model/service owners from wx widget tests. | Retain as local-files/domain behavior owner. |
| `tests/test_wave2_wx_ui_parity.py` | Real wx list/menu/button events, selection preservation, clipboard operations, visible status, and widget layout behavior. This is GUI evidence and is distinct from framework-neutral Wave2 tests. | Retain as wx GUI behavior owner. |
| `tests/test_wave3_remote_sftp_ssh.py` | Mock SFTP operations, remote directory controller behavior, incremental SSH decoding, and integration-shaped Unicode path operations. `test_ssh_backend_uses_shlex_quote` inspects source and is static shell-policy evidence; the adjacent shell-quoting assertions exercise `shlex.quote` itself. | Retain. Label the source inspection as audit/security policy during classification; it is not runtime proof. The D2 byte-preservation owner adds behavior the Wave3 text-focused cases did not prove. |
| `tests/test_wave9_ci_unicode_matrix.py` | Audit inventory, fixture-composition checks, false-green/source checks, and a broad wave-coverage inventory. These tests inspect repository/test assets and are not application runtime coverage. | Retain as audit/reporting ownership. Reclassify by actual assertions in Packets K/M; do not infer behavior from the filename or its historical CI title. |
| `tests/test_wave10_release_gate.py` | Real config serialization/atomic-write and encrypted-navigation temporary-path checks, alongside static migration, encoding, documentation, and “no known P0” assertions. The static checks do not prove packaged runtime or live-cluster behavior. | Retain unique upgrade and policy evidence. Reclassify static items as audit/contract and temporary storage checks by their actual paths in Packets K/M. |
| `tests/test_wave78_jobs_details.py` | Real Jobs wx workspace selection, empty/no-selection state, visible details/status, accounting controls, raw-result display, and callback behavior. D3 repaired the no-selected-job status owner and renamed the distinct raw-status action owner truthfully. | Retain as GUI behavior ownership. Keep the no-selection empty-state node distinct from provider status without selection. |
| `tests/test_wave79_audit.py` | Parser boundary/error behavior, schema rejection/compatibility, provider contracts, raw-command result formatting, and narrow Wave78 regression assertions. These exercise independent parser/config/service cases; a cross-wave label does not make them copies of GUI behavior. | Retain as contract/audit and service behavior. Reclassify individual nodes from evidence in Packets K/M. |
| `tests/test_wave79_provider_contract.py` | Provider parser/adapter registry behavior, Slurm/TRUBA input shapes, contract validation, and parser error/raw preservation. These are direct contract and parser behavior cases separate from GUI owners. | Retain as contract/service ownership; do not treat the file's “integration” wording as live-cluster evidence. |
| `tests/test_wave80_files_outputs.py` | Translation catalog integrity and Jobs/Files behavior. D removed duplicate catalog assertions. Packet F replaced the Files translation-only context-menu assertion with a visible toolbar localization test and rewrote the Outputs runtime-switch node to expose the live label defect. | Retain catalog tests as contract evidence and real control tests as GUI evidence. The F Outputs node intentionally remains failing; it is not runtime PASS evidence. |

The historical containers Wave0, Wave1, Wave9, and Wave10 contain multiple evidence types, but the reviewed cases have distinct audit, policy, storage, or fixture assertions. No canonical replacement covered all of those assertions, so splitting or deleting them here would only move or erase provenance. Their mixed ownership must be represented at class/node level when Packet M classifies the suite.

## Validation and outcome

The D1–D7 review records file-level targeted validation for the repaired duplicate owners. Packet F's GUI neighborhood passed 4 tests (no-selection/status, both actual fallback paths, and visible Files localization); its truthful Outputs runtime localization node failed as documented in the Wave plan. Packet G changed documentation only and introduced no node delta.

**Packet G: PASS.** No historical owner was deleted. Packet F's recorded product defect remains open and keeps the overall execution status at `DEFECT_FOUND`.
