# CI intentionally disabled

GitHub Actions CI is intentionally disabled while the migration and release
audit is completed. The former automatic workflow is preserved in this
directory for reference; it is not executable from `.github/workflows/`.

Repository validation is currently performed locally. The remaining release
workflow is manual-only and is not triggered by pushes or pull requests.
