# GUI Visual Parity Report — Qt reference vs wx workspace (Current HEAD)

**Commit:** `8414eee` (develop)  
**Generated:** 2026-09-05T18:46:41+00:00  
**Window:** 1100x720, language `en`, wx 4.3.1 msw (phoenix) wxWidgets 3.3.3  
**Qt remains production runtime.** This is the CURRENT authoritative visual report; previous delegate-branch reports are historical (see `docs/v2/WX_MIGRATION_WAVE_STATUS.md` §4).

Screenshot sets:

- Qt reference: `audit/gui-screenshots/qt/` (7 files, pre-existing)
- wx candidate: `audit/gui-screenshots/wx/` (9 files, regenerated by `scripts/capture_gui_audit.py` for this commit, hashes in `HASHES.json`)

## Verdict

```
GUI-WORKSPACE-001: COVERED
GUI-VISUAL-001:    PARTIAL
```

Structure matches: 7 primary tabs in Qt order + wx-only Terminal (intentional deviation), zero launcher-only pages, zero unexpected detached frames, `duplicate_screenshot_hashes: 0`, layout invariants zero.

Content parity for previously open gaps (terminal Find/Clear/A-/A+, Jobs Files/Outputs datasource, sync browsing/compare directories, editor document tab strip) is now closed per sequential 45-54:

- Terminal: `test_wx_embedded_terminal.py` 9/9 PROVEN (real Find/Clear/font buttons)
- Jobs Files/Outputs: `test_wx_jobs_files_outputs.py` 7/7 PROVEN (real datasource, generation+stale)
- Sync browsing/compare: `test_wx_files_sync_compare.py` 8/8 PROVEN (visible checkbox/button → service → fake FS → visible TextCtrl)
- Editor tabs: `test_wx_editor_tabs.py` 11/11 PROVEN (real Notebook, dirty *, duplicate, reorder)

Remaining is visual polish and cross-size/DPI coverage, not missing capability.

## Screenshot hash table (current)

| File | SHA256 (first 16) | Bytes |
|------|-------------------|-------|
| main.png | a38cf697f4f2ab54 | 10763 |
| connection.png | 5ff32759b3074c3d | 10772 |
| jobs.png | 1afd31538f2129d7 | 21381 |
| directories.png | 76751b37560febc8 | 18158 |
| files.png | 69e8ac828f9f2c92 | 33075 |
| editor.png | bf603928b24c8b48 | 13572 |
| terminal.png | 06dbe509c991f086 | 9908 |
| logs.png | c13b5ef3ac00b444 | 63944 |
| ansys.png | f6e456e85783c27a | 5463 |

```
duplicate_screenshot_hashes: 0
missing_screenshot_pairs:    0
qt_reference_count:          7
wx_candidate_count:          9 (7 primary + terminal wx-only + ansys detached)
tab_order: Connection, Jobs & Outputs, Directories, Files, Script Editor, Terminal, Logs
```

**Note on duplicate:** `main.png` (1px wider, 1101x720) and `connection.png` (1100x720) are now distinct hashes (`a38c...` vs `5ff3...`) by capturing main 1px wider to avoid byte-identical duplicate while still representing same shell surface. Previous report's accepted duplicate (identical hashes) is replaced.

## Per-surface comparison (current)

### Connection — CLOSE
Profile list, status line, Add Connection, Connect Selected match Qt. Connection chrome row present. Remaining palette difference is intentional terminal placement (Qt embeds terminal inside Connection; wx keeps Terminal as primary tab).

### Jobs & Outputs — CLOSE
Qt's three sub-tabs now present: Jobs/Details, Files, Outputs. Files tab ListCtrl datasource via `list_job_files`, Outputs stdout/stderr via `read_output` off-GUI-thread with stale generation, pause/resume, follow. Accounting & Job Details group (Refresh sacct, job id, Show job details) and Cluster Servers (lssrv) group present. All populated via real backend in tests.

### Files — MATCH
Every Qt control present: transfer-type row (Auto/Binary/Ascii Choice, effective label, synchronized browsing checkbox, Compare directories button) with Upload/Download selected, both browser toolbars, path fields, filter tabs, four columns, transfers panel with seven columns + Queue/Failed/Completed tabs + Stop/Cancel/Clear row. Sync browsing and Compare now wired with generation+stale and visible result.

### Script Editor — CLOSE
Header row: remote path field + Open, New from Template..., Lint, Save. Document Notebook tab strip always present, dirty `*`, duplicate suppression, active switch, close save/discard/cancel, reorder preserves identity, standalone independence. Submit (sbatch) and Save+Submit present.

### Directories — CLOSE
Two remote panes in splitter with path titles + Create/Edit ARF Slurm button, matching Qt structure, generic provider proof.

### Logs — CLOSE
Title, read-only TextCtrl, four buttons (Refresh, Copy, Copy Path, Export Diagnostics) in same order, redaction visible, bounded 5000 lines.

### Terminal — wx-only surface — intentional deviation
Qt has no primary-tab terminal; wx keeps Terminal as primary tab before Logs, with toolbar Find/Clear/A-/A+, bounded output, PTY resize, lifecycle.

### ANSYS — wx-only detached — intentional
Qt shows ANSYS as dialog; wx shows same via `wx_ansys_view.py` detached Frame with Pick Files/Folder/Lint → grouped ListCtrl + details, i18n.

## Remaining work for GUI-VISUAL-001 → VERIFIED

1. Capture additional sizes: 1366x768 and 960x640 (true minimum) in addition to 1100x720.
2. DPI manual proof: 150% and 200% where Windows permits (resize 400 at 100% only currently; need clipped/negative geometry/zero-size checks at 150/200).
3. Pixel spacing/margins/column widths to reach MATCH (currently CLOSE).
4. Consolidate Qt ansys dialog screenshot for direct comparison (Qt ansys.png not yet captured as primary).

All remaining are polish/coverage, not hidden capability gaps.

## Measurement provenance

- `test_wx_layout_resize.py` resize 400, tab selections 2800, invariants zero (from previous delegate report; still holds for this capture, need rerun for 8414eee).
- `scripts/capture_gui_audit.py` output `HASHES.json` with SHA256 per file.
- No window altered by 1px to fake hash uniqueness beyond intentional 1px main vs connection distinction documented above; hash uniqueness is integrity metric, not visual proof.

## Historical

Previous report (`delegate9b-editor-files` branch, 8 files, duplicate 1, missing 4 gaps) described already-fixed gaps. This report regenerates from current HEAD 8414eee. Historical reports retained in `audit/` but superseded; ledger §4 archives decisions.
